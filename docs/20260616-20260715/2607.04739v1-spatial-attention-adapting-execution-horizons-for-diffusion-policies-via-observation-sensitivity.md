---
title: "Spatial Attention: Adapting Execution Horizons for Diffusion Policies via Observation Sensitivity"
title_zh: 空间注意力：通过观测敏感性自适应调整扩散策略的执行时长
authors: "Che-Sang Park, Junsu Ha, Jianlong Fu, Frank C. Park"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04739v1"
tags: ["query:rmgp"]
score: 9.0
evidence: 基于空间注意力与观测敏感性的扩散策略自适应执行时域
tldr: 机器人从演示中学习时，扩散策略常生成动作块并以固定步长执行，这在复杂环境中易导致响应迟缓或计算浪费。针对此问题，我们定义Spatial Attention为动作对数似然关于观察梯度的期望平方范数，量化策略对观察扰动的敏感度。理论表明，在动作块采样次数固定的条件下，最小化扰动导致的对数似然累积下降的最优执行步长，是Spatial Attention的递减函数。据此，我们提出一种自适应执行机制，在生成动作块的同时预测未来Spatial Attention，然后动态设定执行步长：高Attention区使用短步长，低Attention区使用长步长。在仿真与真实机器人的多项实验中，包括标准任务和添加干扰的扰动任务，我们的方法均显著提高了任务成功率，同时平均执行步长与固定基线相近。这项工作为扩散策略在动态环境中的实用部署提供了关键的理论指导和灵活的执行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04739v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1774, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04739v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1766, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04739v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 273, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04739v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 458, \"height\": 281, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04739v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 868, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04739v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1772, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04739v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 434, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04739v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 555, \"height\": 148, \"label\": \"Table\"}]"
motivation: 现有扩散策略使用固定步长执行动作块，导致在响应速度和计算效率之间无法权衡，尤其面对环境扰动时。
method: 提出Spatial Attention作为策略对观察敏感度的度量，通过预测其未来值动态调整执行步长：高敏感阶段缩短步长、低敏感阶段延长步长。
result: 在仿真和真实机器人扰动任务上，成功率显著优于固定步长基线，且平均执行步长保持近似不变。
conclusion: 该自适应执行框架为扩散策略提供了理论基础，显著提升了机器人在动态环境中的成功率和鲁棒性。
---

## 摘要
通过生成模型采样动作片段已成为从演示中学习机器人技能的广泛采用的方法。然而，现有方法通常难以平衡响应性和计算成本，因为它们以固定的执行时长执行每个动作片段。本文中，我们自适应地调整采样动作片段的执行时长，以平衡响应性和计算效率。我们引入空间注意力——定义为动作对数似然关于观测的梯度的期望平方范数——它表明了策略动作分布对观测变化的敏感性。我们证明，在固定的动作片段采样预算下，使干扰引起的累积似然下降最小化的执行时长随着空间注意力的增加而减小。通过在与动作片段一起预测未来的空间注意力值，我们的框架动态地为具有高空间注意力的阶段分配较短的执行时长，为具有低空间注意力的阶段分配较长的执行时长。在仿真和真实机器人的标准任务和扰动任务上的实验表明，与固定时长的基线方法相比，我们的方法显著提高了成功率，同时保持了平均执行时长。

## Abstract
Sampling action chunks via generative models has become a widely adopted methodology for robotic learning from demonstration. However, existing methods often struggle to balance responsiveness and computational cost because they execute each action chunk for a fixed execution horizon. In this paper, we adaptively adjust the execution horizon of sampled action chunks, balancing responsiveness and computational efficiency. We introduce Spatial Attention -- defined as the expected squared norm of the gradient of the action log-likelihood with respect to the observation -- which indicates the sensitivity of the policy's action distribution to variations in the observation. We show that, under a fixed budget of chunk samplings, the execution horizon that minimizes the cumulative likelihood drop induced by disturbances decreases as Spatial Attention increases. By forecasting future Spatial Attention values alongside the action chunk, our framework dynamically assigns shorter execution horizons to phases with high Spatial Attention, and longer horizons to phases with low Spatial Attention. Experiments on standard and perturbed tasks, in both simulation and on a real robot, show that our method significantly improves success rates over fixed-horizon baselines while maintaining the average execution horizon.

---

## 论文详细总结（自动生成）

好的，这里是对论文《Spatial Attention: Adapting Execution Horizons for Diffusion Policies via Observation Sensitivity》的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：当前基于扩散模型的机器人学习策略（如扩散策略）普遍采用“动作块”方法，即一次采样生成多步动作序列，并执行一个固定步长（执行时长）。这种固定执行时长带来了一个根本性的权衡困境：
    *   **短执行时长**：响应快，能及时根据新观测修正动作，但对环境干扰鲁棒性高，代价是频繁调用计算量大的生成模型，效率低下。
    *   **长执行时长**：计算效率高，调用模型次数少，但在执行期间无法接收新观测，对外部干扰（如物体被移动）极为脆弱，可能导致任务失败。
*   **整体含义与动机**：一个完整的机器人操作任务包含不同阶段（如接近物体、抓取、精细插入），这些阶段对响应性的需求截然不同。因此，为整个任务设定一个固定的执行时长是次优的。本文的核心动机是提出一种动态、自适应的执行时长调整机制，在保持总体计算成本（平均采样频率）不变的前提下，最大化策略对干扰的鲁棒性和任务的成功率。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

*   **核心思想**：定义一个名为“空间注意力”的度量，用于量化当前策略对观测变化的敏感度。理论推导证明，在固定采样预算下，最优的执行时长应与空间注意力成反比，即在敏感的区域缩短执行时长以提升响应性，在不敏感的区域延长执行时长以节省计算。
*   **关键技术细节**：
    *   **空间注意力的定义**：
        $$Att(o) \triangleq \mathbb{E}_{a \sim \pi(\cdot|o)}[\|\nabla_o \log \pi(a|o)\|^2]$$
        它表示动作对数似然关于观测的梯度的期望平方范数。当其值高时，微小的观测扰动也会导致动作分布的剧烈变化；反之亦然。
    *   **理论推导（公式(8)和(14)）**：
        1.  **问题建模**：将外部干扰建模为随时间增长的观测噪声，导致动作对数似然下降。
        2.  **优化目标**：在总采样次数 `N` 固定的约束下，找到一个最优的重新采样率函数 `ρ(t)`，以最小化整个任务周期内的累积似然下降。
        3.  **最优解**：通过变分法求解，得到最优采样率 `ρ(t)` 与空间注意力 `Att(o(t))` 的 `1/(2γ+1)` 次方成正比，其中 `γ` 是控制干扰增长速率的超参数。这证明了执行频率应随空间注意力增加而提高。
    *   **算法实现流程**：
        1.  **离线估值器训练**：由于扩散策略本身不提供显式的概率密度，使用贝叶斯规则分解 `∇o log π(a|o)` 为 `∇o log π(o|a) - ∇o log π(o)`，并训练两个独立的分数网络（NCSN）分别估计这两项。对于视觉输入，在VAE的潜空间而非像素空间计算梯度，以聚焦任务相关信息。
        2.  **在线预测与决策**：部署时，使用一个训练好的序列到序列Transformer模型 `Attϕ(At, ot)`，根据当前观测 `ot` 和采样的完整动作块 `At`，预测未来每一步的空间注意力值。
        3.  **动态设定执行时长**：根据公式(20) `Ta = min{k | Σ_{i=0}^{k} Att_{t+i}^{1/(2γ+1)} / Catt >= 1}`，对预测的注意力序列进行累加，当累加值首次超过预定义阈值 `Catt` 时，停止执行。注意力高时，累加速率快，`Ta` 就短；反之则长。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

*   **实验环境与任务**：
    *   **标准仿真环境**：`Robomimic` 基准测试。包含四个单臂操作任务（`Lift`, `Can`, `Square`, `Tool Hang`），分为简单和复杂任务，使用熟练人类演示数据（PH）。
    *   **扰动仿真环境**：对 `Lift` 和 `Can` 任务进行修改，在机械手接近目标物体时，人为移动该物体，模拟外部干扰。
    *   **真实世界环境**：在一个相机滑轨上放置一个红色方块并使其匀速移动，要求机器人抓取该移动物体，以测试对动态干扰的实时响应能力。
*   **对比方法**：将本文提出的自适应方法 (`+SA`) 与多种固定执行时长的基线策略进行对比，以证明其普适性。
    *   **DDPM (Denoising Diffusion Probabilistic Model) 扩散策略**：100步扩散采样。
    *   **DDIM (Denoising Diffusion Implicit Model) 扩散策略**：16步加速采样。
    *   **CP (Consistency Policy) 一致性策略**：3步和1步极端加速采样。
    所有对比实验中，基线方法使用固定且最优的执行时长 `Tavg`，而 `+SA` 方法调整阈值 `Catt` 使其平均执行时长与 `Tavg` 匹配，以保证对比的公平性。

### 4. 资源与算力

*   **论文中并未明确提及训练策略模型、分数网络或注意力预测模型所需的GPU型号、数量和训练总时长。**
*   **仅有关于推理的算力信息**：在真实世界实验中，推理使用的是一台配备24GB显存的RTX 4090 GPU的远程桌面电脑，通过TCP协议与机器人平台通信。

### 5. 实验数量与充分性

*   **实验数量**：实验覆盖范围广泛，涉及三个主要场景（标准仿真、扰动仿真、真实世界），共8种任务配置（4个Robomimic任务，2个扰动任务，1个真实世界任务），并对比了5种基础策略（DDPM, DDIM, 3-step CP, 1-step CP 以及它们的+SA变体）。在Robomimic实验中，每种方法使用4个不同训练种子下的共20个检查点进行评估，每个检查点评估100次。
*   **充分性与公平性**：实验设计较为充分和严谨。通过严格控制平均执行时长，确保了对比的公平性，清晰地剥离出“自适应分配”本身带来的性能增益。在多种具有不同采样开销（从100步到1步）的底层策略上进行了验证，证明了方法的普适性。对仿真和真实世界、标准任务和扰动任务都进行了评估，全面验证了方法的鲁棒性和实用性。结果以均值和标准差报告，增强了统计可信度。

### 6. 论文的主要结论与发现

*   **性能显著提升**：在标准Robomimic仿真任务上，使用空间注意力的自适应方法在所有基线之上都取得了成功率的提升。尤其是在对精度要求高的复杂任务（如 `Square` 和 `Tool Hang`）上，提升尤为显著（最高达19%）。
*   **鲁棒性显著增强**：在存在外部扰动的仿真和真实世界抓取移动物体的任务中，`+SA` 方法展现出远超基线的鲁棒性。例如，在真实机器人实验中，成功率从 **0.42 大幅提升至 0.92**。
*   **动态分配执行时长**：实验可视化（图2）清晰地展示了空间注意力在抓取、插入等关键精密阶段会升高，而本文的方法成功地在此类阶段自动缩短了执行时长，实现了更快的响应和纠错。

### 7. 优点：方法或实验设计上的亮点

*   **坚实的理论基础**：并非启发式设计，而是从一个明确定义的、最小化累积似然下降的优化问题中，数学推导出了最优执行时长与所提度量的关系。
*   **模型无关的即插即用模块**：空间注意力的计算依赖于单独训练的分数网络，与主策略架构解耦，因此理论上适用于任何生成动作块的策略。
*   **计算高效**：通过预测而非生成未来状态，避免了在决策时进行高维观测的生成，实现了实时自适应的执行。
*   **实验全面且公平**：多种策略、多种任务、核心变量（平均采样频率）严格控制，结果具有强说服力。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **依赖离散动作块**：框架的核心限制是要求策略生成离散的动作块，因此无法直接应用于基于动作流或逐帧输出的策略（如一些流匹配或自回归策略）。
*   **额外的训练成本**：该方法需要额外训练三个网络——两个用于估计空间注意力的分数网络和一个用于预测未来注意力的序列模型，增加了离线训练阶段的复杂度。
*   **超参数敏感性**：引入了新的超参数 `Catt`（阈值）和 `γ`（扰动增长率），这些参数需要根据具体任务进行调整，可能增加部署调优的工作量。
*   **未在大模型验证**：实验主要在相对小规模的机器人数据集上进行，尚未在训练视觉-语言-行动（VLA）大模型所需的海量、多样化数据集上验证，其在大模型时代的可扩展性有待探索。
*   **潜空间近似的局限性**：对于视觉输入，使用VAE潜空间作为观测敏感性的代理，虽然提升了计算效率和任务相关性，但损失了原像素空间的部分信息，其近似效果依赖于VAE的训练质量。

（完）

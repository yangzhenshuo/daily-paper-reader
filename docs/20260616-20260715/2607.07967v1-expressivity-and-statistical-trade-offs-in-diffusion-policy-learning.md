---
title: Expressivity and Statistical Trade-offs in Diffusion Policy Learning
title_zh: 扩散策略学习中的表达能力与统计权衡
authors: "Viet Vu, Renyuan Xu, Jiacheng Zhang, Yufei Zhang"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07967v1"
tags: ["query:rmgp"]
score: 9.0
evidence: 通过漂移Lipschitz预算分析扩散策略的表达性
tldr: "强化学习中的扩散策略通过扩散过程建模状态条件动作分布，能表达复杂的多模态行为，但有限样本下的统计学习特性尚不明确。本文引入漂移网络的Lipschitz预算K作为核心量，揭示了表现力与统计效率的内在权衡：近似误差随K增大以O(1/K)下降，且下界证明该阶最优；但K增大会提高统计复杂度，导致有限样本下价值函数误差的收敛速率放缓。对一般神经网络漂移，速率为Õ(n^{-2/(m+6)})，而对单边耗散漂移类，速率可加速至Õ(n^{-2/(m+4)})。该理论为实践提供依据：应根据可用样本量n选择K，并设计对应Lipschitz常数的网络架构。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07967v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1681, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07967v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1693, \"height\": 463, \"label\": \"Figure\"}]"
motivation: 揭示扩散策略在强化学习中表现力与统计效率的权衡，为有限样本下策略学习提供理论指导。
method: 以漂移网络Lipschitz常数K为核心，分析其近似误差下界及有限样本泛化误差的收敛速率。
result: "近似误差与K成反比；通用网络收敛率Õ(n^{-2/(m+6)})，耗散类加速至Õ(n^{-2/(m+4)})，实验验证K与样本量的权衡。"
conclusion: 提出依据样本量选择K和相应Lipschitz网络架构的实用原则，为扩散策略超参选择提供理论指导。
---

## 摘要
基于扩散的策略最近作为强化学习中强大的策略参数化方法出现，将状态条件动作分布表示为参数化漂移项的扩散过程的终态分布。这种终态分布表示在实践中表现出显著的表达灵活性，使扩散策略能够对复杂、多模态和高度非高斯的动作分布进行建模；然而，尚不清楚是什么数学因素驱动了这种表达能力，以及如何在从有限数据中学习策略时充分利用它。在本文中，我们确定了漂移利普希茨预算 $K$ 是控制扩散策略表达能力和统计行为的关键量。我们通过近似来量化表达能力：具有 $K$-利普希茨漂移项的扩散策略可以集中到最优确定性策略附近，并实现 $1/K$ 量级的值函数近似误差；此外，我们在非退化扩散噪声下证明了一个匹配的下界。这种增强的表达能力伴随着统计成本。当漂移项由神经网络参数化时，增加 $K$ 会改善近似，但会增加统计复杂性。平衡这两项得到：对于一般的神经网络漂移，有限样本性能差距为 $\tilde{O}(n^{-2/(m+6)})$ 量级；对于单边耗散漂移类，得到更快的收敛速率 $\tilde{O}(n^{-2/(m+4)})$，其中 $n$ 是样本大小，$m$ 是状态空间的维度。数值实验为依赖于样本的 $K$ 权衡提供了经验证据，支持了两种理论情况。我们的框架还提出了一个实际实现原则：根据可用的样本大小选择扩散预算 $K$，然后选择具有相应固定利普希茨系数的神经网络架构。

## Abstract
Diffusion-based policies have recently emerged as powerful policy parameterizations for reinforcement learning, representing state-conditioned action distributions as terminal laws of diffusion processes with parameterized drifts. This terminal-law representation has shown substantial expressive flexibility in practice, enabling diffusion policies to model complex, multimodal, and highly non-Gaussian action distributions; however, it remains unclear what mathematically drives this expressivity and how to fully exploit it when the policy is learned from finite data. In this paper, we identify the drift Lipschitz budget $K$ as a central quantity governing the expressivity and statistical behavior of diffusion policies. We quantify expressivity through approximation: diffusion policies with $K$-Lipschitz drifts can concentrate near optimal deterministic policies and achieve value approximation error of order $1/K$; moreover, we prove a matching lower bound under nondegenerate diffusion noise. This increased expressivity comes with a statistical cost. When the drift is parameterized by neural networks, increasing $K$ improves approximation but increases statistical complexity. Balancing these two terms yields a finite-sample performance gap of order $\tilde{O}(n^{-2/(m+6)})$ for generic neural-network drifts, and a sharper rate $\tilde{O}(n^{-2/(m+4)})$ for one-sided dissipative drift classes, where $n$ is the sample size and $m$ is the dimension of the state space. Numerical experiments provide empirical evidence for the sample-dependent trade-off in $K$, supporting both theoretical regimes. Our framework also suggests a practical implementation principle: choose the diffusion budget $K$ according to the available sample size, and then select a neural-network architecture with the corresponding fixed Lipschitz coefficient.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：扩散模型作为强化学习的策略参数化方法，在实践中表现出强大的表达力，能捕捉复杂、多模态的动作分布。但理论上尚不清楚：
  - 哪些数学量决定了扩散策略的近似能力（表达性）。
  - 在有限数据下学习时，这种强表达性会带来怎样的统计代价（偏差–方差权衡）。
- **整体含义**：本文首次指出**漂移项的利普希茨预算$K$** 是控制上述权衡的核心量。$K$ 越大，扩散过程的漂移越“陡”，策略越能集中到最优确定性动作附近，从而近似误差以$O(1/K)$ 下降；但$K$ 增大会同时增加策略类的统计复杂度，导致泛化误差上升。该理论为实际中如何根据样本量选择$K$ 和对应的网络结构提供了原则性指导。

### 2. 方法论
- **核心思想**：将扩散策略的近似误差和统计误差统一表示为$K$ 的函数，并寻找使有限样本性能间隙最小的最优$K$。
- **关键技术细节**：
  - **表达性分析**（第3节）：
    - 构造一个均归漂移项 $f(s,a,x)=K\bigl(\frac{c}{1-e^{-K\tau}} - a\bigr)$，证明扩散终态 ${\bar a}_T$ 与目标动作 $c$ 的均方距离上界为 $O(1/K)$（引理3.1）。进而推出值函数逼近误差的上界 $O(1/K)$（定理3.2）。
    - 在椭圆型扩散噪声下证明一个匹配的下界：任何$K$-利普希茨漂移不能以快于$O(1/K)$ 的速度使终态集中到有限个目标动作附近（命题3.3）。结合贝尔曼差距的二次下界，得到值函数下界（定理3.4）。
  - **有限样本统计学习**（第4节）：
    - 将漂移参数化为满足特定规模约束的ReLU神经网络类 $\mathcal{F}_{K,s}$，并假设最优确定性策略是利普希茨的，且奖励和转移核满足利普希茨条件。
    - 导出经验值函数最大化的 oracle 不等式（定理4.2），将误差分解为：
      1. 扩散近似误差：$d/K$；
      2. 网络实现误差：$s^{-2/m}$；
      3. 统计估计误差：$\sqrt{\tilde{O}(s K)/n}$（通用类）或 $\sqrt{\tilde{O}(s)/n}$（单边耗散类）。
    - 平衡这三项得到 $K$ 和网络规模 $s$ 的最优选择：
      - 通用类：$K \asymp n^{2/(m+6)}$，最终值函数间隙 $\tilde{O}(n^{-2/(m+6)})$。
      - 耗散类（满足 $\langle a-a', f(t,a,x)-f(t,a',x)\rangle \le -\lambda K\|a-a'\|^2$）：统计项不含 $K$，收敛速率提升至 $\tilde{O}(n^{-2/(m+4)})$，且 $K$ 有最小充分尺度 $K \asymp (n/\log n)^{2/(m+4)}$。
  - **策略梯度公式**（第5节）：利用吉尔萨诺夫定理将不可显式求得的终端对数密度替换为路径量，给出连续时间扩散策略的策略梯度表达式：
    $$\nabla_\theta V^{\pi_\theta}(\rho) = \frac{1}{1-\gamma}\int_{\mathcal{X}} \mathbb{E}\Bigl[\Bigl(\int_0^T \nabla_\theta f_\theta(t,\bar{a}_t^x,x)(\sigma^\top)^{-1} \mathrm{d}B_t\Bigr) Q^{\pi_\theta}(x,\bar{a}_T^x)\Bigr] \mathrm{d}\mu^{\pi_\theta}_\rho(x).$$

### 3. 实验设计
- **实验1 – 通用神经漂移（非耗散）**：
  - **场景**：二维随机非线性多步 MDP，状态和动作均连续，代价由平滑参考反馈 $g_{\text{ref}}$ 定义，动态方程包含三角函数、$\tanh$ 等非线性项。折扣因子 $\gamma=0.95$，长度 $H=10$。
  - **策略**：漂移网络为两层 ReLU MLP 并经过 $\tanh$ 归一化，宽度随 $K$ 增长 $w(K) \sim \min(256,\max(16, \operatorname{round}(16+2K)))$，以匹配理论中 $s \asymp K^{m/2}$ 的引导。
  - **对比**：固定样本量 $n$（每轮策略梯度更新使用的轨迹数），考察不同 $K$ 下的折扣代价。样本量 $n \in \{64, 256, 2048\}$，$K$ 在 $[2, 512]$ 间取多个值，每个设置重复 50 个随机种子，用 1024 回合评估。基线为参考反馈 $g_{\text{ref}}$ 对应的代价。
- **实验2 – 耗散漂移**：
  - **场景**：单步上下文老虎机，状态 $X\sim \text{Unif}([0,1]^m)$，动作空间 $\mathbb{R}^d$，奖励 $r(x,a)=-\|a-g^\star(x)\|^2$，最优值为 0。
  - **策略**：动作中心由含 $s(n)$ 个傅立叶特征的线性模型拟合（岭回归），然后以 $K$ 为参数的均归扩散下采样。$s(n) \asymp (n/\log n)^{m/(m+4)}$ 固定后，单独改变 $K$。
  - **对比**：$m=4$，$d=2$，$n \in \{128, 256, 512, 1024, 2048\}$，$K \in [1, 512]$。评估期望平方距离、扩散误差及中心估计误差。考察策略遗憾随 $K$ 单调下降并趋于中心估计误差平台的现象。
- **评价指标**：两个实验分别报告带置信区间的代价或距离，并与理论标度律对照。

### 4. 资源与算力
- **论文中未提及所用 GPU 型号、数量或训练时长**。实验部分仅说明了训练超参数（Adam 学习率等）和评估方式，没有提供任何关于计算资源的信息。因此无法判断计算开销或实验的硬件要求。

### 5. 实验数量与充分性
- **实验总数**：两组精心设计的实验，覆盖了通用漂移和耗散漂移两种理论情形。
- **充分性**：
  - 每组实验均对多个 $n$ 和 $K$ 进行扫描，涵盖样本大小和利普希茨预算的广泛范围。
  - 采用了多次独立种子（50 种子和5 种子）并报告置信区间/标准误差，统计可靠性较高。
  - 实验直接验证了理论所得标度律（如 $K \asymp n^{2/(m+6)}$ 的经验最优值趋势、耗散情形下遗憾的单调性及平台），且与理论预期定性一致。
- **局限性**：两个实验均属于较大规模的玩具问题（m=2 或 m=4），且未与其它扩散策略或非扩散策略方法进行横向对比；重点在验证理论，而非追求性能最优。因此虽然充分验证了核心理论发现，但在横向对比的广泛性上有所欠缺。

### 6. 主要结论与发现
- **利普希茨预算 $K$ 的核心作用**：$K$ 决定了扩散策略的表达能力与统计复杂度的折中。
- **表达性**：$K$-利普希茨漂移可近似最优确定性策略，且值误差的最优收敛阶为 $1/K$，此阶是紧的。
- **统计权衡**：对于一般 ReLU 漂移类，最优有限样本值误差速率为 $\tilde{O}(n^{-2/(m+6)})$；若漂移类满足单边耗散条件，速率可提高至 $\tilde{O}(n^{-2/(m+4)})$。
- **实践指导**：应当依据可用样本量 $n$ 选择适当的 $K$，再选择利普希茨系数匹配的神经网络架构；在耗散情形下，$K$ 只需大到某个阈值即可，无需进一步增大。
- **策略梯度推导**：为连续时间扩散策略提供了一个紧凑、可实现的路径梯度表达式，支撑了所提算法类的训练。

### 7. 优点
- **理论深度**：同时提供了值逼近的上、下界，且下界匹配上界，整体分析非常完整。
- **可操作的指导**：将理论结果转化为明确的超参数选择原则（按样本量定 $K$ 及网络规模），具有实际价值。
- **不同情形区分**：明确区分了通用类和耗散类两种漂移结构，并给出各自不同的收敛速率和 $K$ 选择策略，使理论覆盖面更广。
- **实验设计精确**：两个实验分别验证两种理论情形，且实验设置（如宽度随 $K$ 变化、耗散结构通过构造实现）高度贴合理论假设，验证逻辑清晰、可信度高。
- **算法贡献**：提供了适用于连续时间扩散策略的实用策略梯度公式，补齐了从理论分析到算法实现的桥梁。

### 8. 不足与局限
- **实验规模有限**：实验均在低维玩具问题上进行（状态维度2和4），未在大规模、高维任务（如图像输入、机器人操作）上证明理论与实践的贴合度。
- **缺乏横向对比**：实验未与其他扩散策略方法或经典强化学习算法（如 PPO、SAC）进行对比，无法评估其在实际问题中的绝对性能。
- **理论假设较强**：定理依赖最优确定策略的利普希茨性、奖励与转移的利普希茨条件、噪声均匀椭圆性等；耗散类条件的约束更强，实际应用中可能难以严格满足。
- **未考虑优化误差**：分析仅关注统计误差与近似误差，未涉及 SGD 类算法带来的优化误差，而实验中确实出现了大 $K$ 下优化不稳定的现象。
- **算力信息缺失**：文中完全未报告实验所用计算资源，使得读者难以评估方法的计算开销与可复现性。
- **策略梯度公式可能方差高**：尽管给出了梯度表达式，但其中涉及随机积分，实际使用中可能面临高方差问题，文章未做深入讨论或对比。

（完）

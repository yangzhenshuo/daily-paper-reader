Title: An exact information theory of generalization phase transitions in Bayesian diffusion models

URL Source: https://arxiv.org/pdf/2607.08041v1

Published Time: Fri, 10 Jul 2026 00:28:30 GMT

Number of Pages: 50

Markdown Content:
# An exact information theory of generalization phase transitions in Bayesian diffusion models 

Henry Hunt ∗

Department of Physics Stanford University Stanford, CA 94305 

hshunt@stanford.edu 

Mason Kamb ∗

Department of Applied Physics Stanford University Stanford, CA 94305 

kambm@stanford.edu 

Surya Ganguli 

Department of Applied Physics Stanford University Stanford, CA 94305 

sganguli@stanford.edu 

## Abstract 

How diffusion models circumvent the curse of dimensionality to learn complex distributions over high dimensional spaces from a finite training set, instead of memorizing it, remains a fundamental mystery. To address this, we introduce analytically tractable Bayesian information restricted diffusion (BIRD) models, in which each pixel observes restricted information about noisy data. A BIRD model time-reverses diffusion by inferring which past training sample produced its current restricted observation using the Bayesian posterior. This model class generalizes existing analytical diffusion models that use spatially local informa-tion restriction. We show that spatially local BIRD models closely approximate trained diffusion models early in training , across different architectures such as UNets and DiTs. Under minimal assumptions on the data distribution, we identify an information-theoretic phase boundary between memorization and generaliza-tion in the joint space of amount of training data, time in the reverse generative process, and amount of information restriction: a BIRD model memorizes when the mutual information between its restricted noisy observations and the training data exceeds the log number of training points, and it generalizes otherwise. Ex-periments across a range of datasets confirm our theoretically predicted location for the transition. We find that generation proceeds near the edge of memoriza-tion: both spatially local BIRD models and early-training diffusion models track the memorization-generalization phase boundary by increasingly restricting infor-mation over time. Overall, our results reveal a fundamental role for information restriction in generative AI to circumvent the curse of dimensionality. 

## 1 Introduction and related work 

Remarkably, generative AI can now learn complex distributions over high dimensional spaces with tractable amounts of training data. For example, diffusion models [1–3] robustly and consistently generalize: when trained on entirely disjoint subsets of data, two independent models will converge to the same general model when the data subset size is moderately large [4]. Below this modest data threshold, the two models may instead memorize their own training data. Naively, however the curse of dimensionality would suggest that an intractable (e.g. exponential in ambient dimension) amount of data should be required to transition from memorization to generalization for arbitrary data distributions [5]. This raises a fundamental mystery as to when the transition from memoriza-tion to generalization occurs for diffusion models, and why it does not require that much training data. Several theoretical approaches have been taken to explore this mystery. These approaches often involve analyzing the behavior of simplified diffusion models on simplified datasets. Diffu-sion models reverse a forward diffusion process that converts data into noise. There exists an exact mathematical reversal of this process that starts from noise and follows the ideal, or empirical score 

> Preprint. arXiv:2607.08041v1 [cs.LG] 9 Jul 2026

function for the finite training set (App. B, [6]). However, such empirical score models require an exponential in dimension amount of data to avoid memorization [6, 7]. While several papers have explored the hypothesis that the low dimensionality of the data distribution might rescue generaliza-tion of empirical score models [8–10], extensive experimental work shows empirical score models fail to generalize on datasets of realistic dimensionality under realistic modeling conditions [11–17]. 

Figure 1: A schematic figure showing different transitions from memorizing to generalizing phases in BIRD models. (a) A depiction of the Bayesian guessing game performed by a pixel as it assigns posterior probabilities to training set images. The forward process starts with a training image of a cat, adds noise to it, then the pixel observes a local patch (e.g. the eye). Based on this restricted observation, the pixel must guess whether this noisy patch was generated from the truck or the cat. The pixel memorizes if the posterior con-centrates on the cat. (b) The phase diagram of the gener-alization/memorization transition for a BIRD model. Con-sider the blue point in the memorization phase (top phase diagram). Increased noise (pink arrow) and decreased ob-servation capacity (blue arrow) each increase posterior en-tropy, making Bayesian guessing harder, and drive a transi-tion from memorization to generalization. Also, increased dataset size (yellow arrow) can shift the phase transition boundary, and increase posterior entropy, so the original lo-cation (blue) dot is now in the generalization phase (bottom phase diagram). (c) In each of the 4 plots, red indicates the distribution of a noised training set, the green x indicates a particular observed image, and the blue bars depict the posterior distribution over the training data conditioned on the observed image. The upper left plot corresponds to the blue point in the top phase diagram of panel b. The pos-terior concentrates on a single training point and so it is in the memorization phase. The 3 colored arrows again corre-spond to the 3 distinct ways one can increase posterior en-tropy and transition to the generalizing phase as in panel (b): by increasing noise (pink), by decreasing observation capac-ity (by projecting out a dimension) (blue), or by increasing the amount of training data (yellow). The inability of empirical score models to generalize with tractable amounts of training data suggests that successful generalization in dif-fusion models may originate from limiting inductive biases that prevent learning the empirical score function, such as smoothness [12, 18], linear-ity [19, 20], low-dimensionality [21, 22], geometry-adaptive bases [4], early training [23–26], and model pa-rameterization/architecture [27–31]. However, none of these works de-rived a highly accurate predictive the-ory for what trained, nonlinear diffu-sion models actually converge to in the generalizing phase on a wide va-riety of benchmark datasets. One such theory to do so arose in [13, 14] which posited local score models (LS), which are optimal dif-fusion models subject to locality con-straints in which each pixel can only use a local spatial neighborhood of the image to compute the score. [13] also introduced equivariant (ES) and (boundary-broken) equivariant-local 

score models (ELS), which incorpo-rated the additional inductive bias of (translation) group equivariance and its breaking via boundaries. In par-ticular [13] showed that such analytic models could predict image outputs of trained convolution only diffusion models on a case by case basis with high quantitative accuracy. [32] then provided a method for obtaining lo-cality scales and patch geometry for LS models via optimal linear denois-ing. Interestingly, these works found that image generation proceeds start-ing from a large coarse spatial scale to a small fine one. Local generaliza-tion mechanisms have also since been investigated in later works [33–38]. Importantly, while [13] developed a predictive analytic theory of convo-lutional diffusion models deep in the generalization phase, this and other works left open the mystery of when and where the memoriza-tion to generalization transition occurs on complex benchmark datasets for more powerful models. Answering this question in general is extremely challenging as it could depend a priori in a joint 2fashion on explicit biases from architecture, implicit biases from training dynamics, the structure of the data distribution, time or noise in the generative process, and of course the amount of data. In order to understand this question theoretically, in this paper we introduce a general class of dif-fusion models which we call Bayesian information restricted diffusion (BIRD) models. They are inspired by and encompass all the equivariant/local score models of [13, 14]. BIRD models pos-sess a fortuitous combination of intuitive appeal, predictive capacity, and theoretical tractability, yielding significant conceptual insights into the memorization-generalization transition. Our main contributions are: (1) We introduce an intuitively appealing general concept of BIRD models in which each image pixel 

x can be thought of as an agent which observes at time t in the reverse generation process restricted information Cx,t about a noisy image ϕt and uses optimal Bayesian inference over the training data to reverse the forward diffusion process (Fig. 1). This constitutes a well-defined diffusion model obtained from a finite training set without the need for any explicit architectural assumptions or learning dynamics. (2) We show spatially local BIRD models in which the restricted observation Cx,t corresponds to a local image patch around x, yield exceedingly good predictions for what many neural diffusion models learn early in training : for early epochs, they can predict the individual image outputs of neural diffusion models on a case by case basis for a wide variety of architectures, including UNets with self-attention and diffusion transformers (DiTs), achieving high median r2 ∼ 0.85 − 0.93 . This latter result goes beyond that of [13] which showed similarly high predictivity for small CNNs only. (3) Theoretically, under very mild assumptions on the data distribution, we develop a precise in-formation theoretic criterion for the location of a memorization to generalization phase transition for general BIRD models in the joint space of amount of training data, time (or noise level in the reverse process), and amount of information restriction: if, under the forward diffusion process, the mutual information between the observation Cx,t and the true data distribution, is greater (less) than the logarithm of the amount of data, then the BIRD model will memorize (generalize), because Bayesian guessing of the underlying image will be to easy (hard). We experimentally confirm the location of our theoretically predicted phase boundary on a variety of datasets. These results reveal that the amount of data need only be exponential in the mutual information to avoid memorization, and highlight a fundamental role of information restriction in promoting generalization. (4) Focusing on spatially local BIRD models in which information restriction is obtained by each pixel observing a location patch of patch scale L, we use various information theoretic analyses, to reveal the shape of the memorization to generalization phase transition boundary in the joint space of reverse generation time t (or noise level σt) and patch scale L. We find a time dependent critical patch scale Lc(t) that monotonically decreases in the reverse generation process as both time t and noise σt decrease from t = 1 to t = 0 . For any t, L(t) > L c(t) (L(t) < L c(t)) then BIRD models will memorize (generalize). Optimal generation and denoising proceeds along this phase transition boundary: in essence successful generation proceeds at the edge of memorization (Fig. 1b). (5) We explicitly compute the phase transition boundary between memorization and generalization for BIRD models on image data with power law fall off in the power spectral density P (k) ∼ k−2−ϵ

with spatial frequency k. Here ϵ = 0 corresponds to scale invariant images [39]. We then show that a BIRD model will be in a generalization phase and effectively denoise for all time and noise levels if the amount of data grows with linear image size LI (e.g. LI by LI images) as exp LϵI .This reveals the striking finding that BIRD models do not suffer from a curse of dimensionality for scale invariant images. Moreover, for small departures from scale invariance (e.g. ϵ ∼ 0.1 to 0.3 as observed in practice), the data requirements are only exponential in LϵI , not the total dimensionality of the space of images, which is O(L2 

> I

).Together, these results provide fundamental insights into the phase transition between memorization and generalization in a general class of diffusion models, and highlight the role of information restriction in allowing generalization with relatively modest amounts of data. We summarize our results below, with much more detailed exposition in the Appendix (see e.g. App. A for a summary of all mathematical notation and background). 32 Overall framework 

Diffusion models sample from a complex data distribution φ ∼ P0(φ) by time reversing a forward diffusion process that transports P0(φ) to a simple isotropic Gaussian η ∼ N (0 , I ) through a time dependent family of distributions of noised data ϕt ∼ Pt(ϕt) as time proceeds from t = 0 to 

t = 1 . A single sample at any fixed time t can be obtained via the interpolation ϕt(φ, η ) = √¯αtφ +√1 − ¯αtη. The temporal schedule of the signal power is chosen to monotonically decrease from 

¯α0 = 1 at time t = 0 to ¯α1 = 0 . We define an important noise-to-signal ratio σ2 

> t

= 1− ¯αt 

> ¯αt

that monotonically increases from t = 0 to t = 1 .Diffusion models generally aim to reverse this flow through a reverse process SDE or ODE that evolves pure noise ϕ1 ∼ N (0 , I ) backwards in time from t = 1 to t = 0 , matching the marginals 

Pt(ϕt) of the forward process. These reverse processes use the score function ∇ log Pt of Pt(ϕt),which must be computed or modeled. The score computation can be reframed as optimal denoising, via Tweedie’s theorem: 

∇ log Pt(ϕt) = − E[η|ϕt]

√1 − ¯αt

=

√¯αt

1 − ¯αt

E[φ|ϕt] − ϕt

1 − ¯αt

. (1) One can approximate the posterior mean E[φ|ϕt] by training a neural denoiser model Mt,θ [ϕt] to predict φ from ϕt. In training this model, we usually do not have direct access to the true data distribution P0(φ); instead, we only have a finite dataset D consisting of |D| training samples φ ∈ D 

each drawn i.i.d from the true data distribution P0(φ). For this finite dataset, the Bayes optimal MMSE denoiser (also known as the empirical score function) is given by 

Mt[ϕt] = E[φ|ϕt] = X

> φ∈D

φ P train (φ|ϕt). (2) Here Ptrain (φ|ϕt) is the posterior probability that a clean training data point φ ∈ D at time t = 0 

led to ϕt in the forward process (Fig. 1b). The structure of this analytic model has an appealing interpretation in terms of a Bayesian guessing game [13] where the model guesses or forms beliefs about which past data point φ ∈ D led to the noised image ϕt using the posterior Ptrain (φ|ϕt), then reverse flows towards the posterior mean. While the Bayes optimal diffusion model is appealing in terms of both its analytic and conceptual simplicity, it unfortunately always memorizes the training data by eventually flowing to one of the training points φ ∈ D (see App. B and [6, 13]). In essence, the Bayesian guessing game, given the 

entire noised image ϕt becomes too easy at small enough times t, corresponding to small enough noise σt. Thus this model cannot generalize and is inconsistent with the behavior of trained neural diffusion models. 

2.1 Bayes Information Restricted Diffusion (BIRD) Models 

The unfortunate ease of the Bayesian guessing game for the optimal denoiser raises the question of whether one can obtain alternate Bayesian diffusion models that retain analytic tractability and conceptual simplicity, but also generalize, thereby behaving more like trained neural network diffu-sion models. We show that we can simultaneously achieve both theoretical tractability and realistic generalization, by combining two key approaches: 1) making the Bayesian guessing game harder by restricting the image information the model uses to guess the training image; and 2) allowing different parts of the image to use different pieces of restricted information. We call such models Bayesian Information Restricted Diffusion (BIRD) models, which are inspired by and generalize [13, 14]. In the general class of BIRD models (see App. C for more details) we imagine that each pixel x

is only allowed to observe the noisy image ϕt through a restricted, possibly stochastic, observation 

Cx,t ∈ Rnx,t of ϕt ∈ Rd. The observation dimension nx,t is less than the image dimension d. Then the pixel x computes a denoised pixel value Mx,t [Cx,t ]. The optimal Bayesian MMSE individual pixel denoiser has a structure similar to that of Mt in (2): 

Mx,t [Cx,t ] = X

> φ∈D

φx Ptrain (φ|C x,t ). (3) 4D2 D1D2 D1D2 D1   

> (a) Celeba64 (b) Cifar10 (c) MNIST (d) FashionMNIST

Figure 2: The consistent and robust generalization of diffusion models early in training, across various datasets and architectures, is well-predicted by spatially local BIRD models with a patch size near the critical scale, as described below. Here UNets and DiTs are trained on disjoint dataset subsets D1 and D2, but produce nearly identical images when fed the same noise input. These images are both similar to the corresponding BIRD model outputs. Many more such samples are shown in App. H.2 in figs. 7-11, along with details of the experiment and training procedure in App. H. Here Ptrain (φ|C x,t ) is pixel x’s posterior belief distribution that any past training sample φ ∈ D led to the pixel’s observation Cx,t in the forward training Markov chain φ → ϕt → C x,t (see App. A for notation and App. C for explicit expressions for Ptrain (φ|C x,t )). The pixel’s observation restriction 

Cx,t at the end of this forward information channel makes the Bayesian guessing game harder, and ideally prevents memorization. The MMSE denoiser for pixel x in (3) simply returns the posterior mean training image pixel value at pixel x.A simple channel restriction Cx,t is a deterministic linear projection operator Px,t applied to ϕt.The LS model of [13] is a special case where Px,t projects an image ϕt onto a coordinate subspace of pixel components within a local spatial patch Ωx,t surrounding pixel x, yielding the restricted observation Cx,t [ϕt] = ϕΩx,t , where ϕΩx,t is a vector of all pixel values of ϕt restricted to the local patch Ωx,t (see App.A for notation). The BIRD framework also generalizes ES and ELS machine of [13] (see App.C.1 for details). We call such models spatially local BIRD models, indicating that spatial locality is the specific form of information restriction employed, though BIRD models and our theory of them are more general. We next show in Sec. 3 that spatially local BIRD models are highly relevant in that they describe well the early learning phase of many diffusion model architectures. And after that in Sec. 4 we show that the general class of all BIRD models provides a highly useful abstraction, in that a general theory of the memorization-generalization phase transition can be derived for all such BIRD models for essentially any data distribution. 

## 3 BIRD models describe the early learning phase of diffusion models 

[13] showed that spatially local BIRD models could accurately predict the individual image outputs of fully trained small CNN based diffusion models on a case by case basis for a range of datasets with a very high median r2 ≈ 0.9 between theory and experiment. Here we extend this result by showing that spatially local BIRD models can also predict individual image outputs of more sophisticated architectures, like UNets with self-attention and diffusion transformers (DiTs), when they are in the earliest stages of the training process .5Figure 3: Comparison of theory and experiment for the memorization-generalization phase tran-sition. Experimental curves plot numerical estimates of the average entropy deficit ( ln |D| − 

S[P (φ|ϕΩ)] ) in a spatially local BIRD model with a patch size 5 × 5. σ2 

> t

is the noise strength. Theory curves plot mutual information or its Gaussian upper bounds. As noise σ2 

> t

decreases, the theoretically derived mutual information curves rise in the generalizing phase then saturate to ln |D|

in the memorizing phase. Moreover the mutual information theory curves closely track, or upper bound the entropy deficit experimental curves. Each sub-figure corresponds to a different true data distribution: (a) Translationally invariant Gaussian data with α = 1 .7 power law spectrum (see Sec.5.2) (b) An isotropic hierarchical mixture of Gaussians model (see App. E.4 for theory and experiment); (c) CIFAR10; (d) CelebA32. The theory curves for CIFAR10 and CelebA32 are given by a Gaussian upper bound on mutual information (see App. E.2). See App.F.5 for further details. In our experiments on four standard datasets (Celeba64, CIFAR10, FashionMNIST, and MNIST), and two architectues (UNets and DiTs), we find that the agreement between spatially local BIRD theory and early trained diffusion models peaks at values of r2 ∼ 0.9 around 10 to 30 epochs of training before slowly declining thereafter. See tables 1 and 2 for detailed numerical values of the agreement, and see App. H for more experimental parameters, and for our approach to patch selection and scale calibration, and further samples. We also show, in this early training regime, both BIRD models and trained architectures exhibit the phenomenon of consistent and robust generalization, where different models trained on disjoint data subsets nevertheless generate the same images when fed the same input noise (Fig. 2). In our experiments, we compare the outputs of the 3 distinct models (BIRD, UNets, DiTs) each obtained from two disjoint datasets D1 and D2. We find that all 3 × 2 = 6 images are highly similar (Fig. 2). This indicates BIRD models provide a good description of the generalization phase of both DiTs and UNets early in training, and that such BIRD models themselves are in a generalizing phase. More samples are included in App. H, as well as quantitative validations in Fig. 6 and tables 1 and 2. This strong agreement between BIRD model theory and neural experiments motivates the use of BIRD models as a theoretical laboratory within which to study the nature of the memorization-generalization phase transition. We elucidate this theory next in Sec.4. 

## 4 The memorization-generalization phase transition in BIRD models 

We first show, via Fano’s inequality, that perfect guessing in the pixel Bayesian guessing game (i.e. memorization) implies I(Cx,t ; φ) ≥ ln |D|, where I(Cx,t ; φ) is the mutual information between a 

test data point φ drawn from the true data distribution P0(φ) and the pixel observation Cx,t under the forward testing Markov chain φ → ϕt → C x,t (see App.A for notation and App. E.1 for a proof). However, an approach via Fano’s inequality only proves that in the memorization phase, we have 

I(Ct,x ; φ) ≥ ln |D| . It does not prove the converse, i.e. that in the generalizing phase, the opposite holds: I(Ct,x ; φ) < ln |D| .To actually locate the phase transition between memorization and generalization, in App. D.5, we prove for posteriors with sub-exponential tails, in large dataset size and high observation dimension limit, that 

S[Ptrain (φ|C x,t )] = 

ln |D| − I(φ; Cx,t ) ln |D| > I (φ; Cx,t ) (generalization )0 ln |D| ≤ I(φ; Cx,t ) (memorization ). (4) Thus the phase transition boundary is exactly given by the equation 

ln |D| = I(φ; Cx,t ). (5) 6(a) (b) 

Figure 4: Effect of varying the patch scale L (for square patches) in a spatially local BIRD model. (a) The top (bottom) shows the outcome of denoising of a single test (train) image using BIRD models of different patch sizes L starting from noise σt = 1 . Optimal denoising occurs when 

L = 7 (in general this optimal denoising scale coincides with Lc). For L = 5 < L c, from theory we know the denoiser is robust, but denoising performance is poor both on the training and test image. For L = 15 > L c, the model is in the memorization phase, and it poorly denoises the test image (compare rightmost to leftmost image in top row), but perfectly denoises (i.e. memorizes) the training image (compare rightmost to leftmost image in bottom row). (b) Comparisons between the anisotropic Gaussian bound (Lemma 5.1), the numerically computed values for the critical scales, and the spectral scale for BWCeleba32, BWCIFAR10, and MNIST, which are generally very close (see App. F.6 for details). Intuitively, more mutual information than the entropy ln |D| of the uniform prior on the training data makes Bayesian guessing easy with zero posterior entropy S[Ptrain (φ|C x,t )] leading to memoriza-tion, while less mutual information makes Bayesian guessing hard with positive posterior entropy leading to generalization. We can experimentally test (4) because S[Ptrain (φ|C x,t )] can be numerically computed for any finite dataset, while the mutual information I(φ; Cx,t ) can be exactly calculated for simple data dis-tributions and upper bounded for real data sets. In order to collapse the curves from different dataset sizes, and to compare theory and experiment, we plot ln |D| − S[Ptrain (φ|ϕΩ)] , obtained from nu-merical experiments, and compare it either to: (1) the theoretically obtained mutual information (which it should be equal to in the generalizing phase), or (2) ln |D| (which it should be equal to in the memorizing phase). For real datasets, we cannot calculate the exact mutual information, so we use the Gaussian upper bound derived in App.E.2 which only depends on the second order statistics of the data. Fig. 3 exhibits numerical validation of (4) for both real and toy datasets. Our results above generalize previous work [6], which studied the memorization to generalization phase transition for the empirical score function (in which each pixel observes the entire image). They defined the transition time t to occur when S[P (ϕt)] = S|D|  

> sep

where S|D|  

> sep

is entropy of |D| 

separated Gaussians and computed this transition time for the training set only, when the data came from an isotropic Gaussian distribution. We include information restriction, generalize to essentially arbitrary data distributions, and find a general information theoretic criterion for the memorization-generalization transition for both training and testing data, as well as a pointwise condition for any single observation (App. D.5). 

## 5 How BIRD models circumvent the curse of dimensionality 

We now explain how, for scale invariant natural images, spatially local BIRD models can effectively denoise while avoiding memorization, even for large images. 

5.1 The critical patch scale at the memorization-generalization transition 

A very natural information restriction approach, studied in [13], is to allow each pixel x to observe a local patch ΩL,x , where we define the patch scale L = p|ΩL,x |. Following [13 ] we denote this projection as ϕt;Ω L,x . For square patches [13] L is simply the side length. We consider a one parameter family of patches, indexed by scale L, obeying ΩL′,x ⊃ ΩL,x if L′ > L . The fact that larger patches include smaller patches implies that I(φ, ϕ t, Ωx,L ) is monotonically increasing with the patch scale L. This implies, according to (4) that there is a critical patch scale Lc(σt), for any fixed noise level σt, obeying the equality 

ln |D| = I(φ; ϕt, ΩL,x ), (6) 7such that if L < L c (L > L c) the spatially local BIRD model is in the generalizing (memorizing) phase. Lc(σt) then defines a phase boundary between memorization and generalization in the joint space of patch scale L and noise σt (or time t in the reverse process). In high dimensions, the critical scale also generally sets the optimal scale (with respect to denoising error on the test set) for the spatially local BIRD model. The reason for this is as follows. If the model uses a scale L > L c, denoising will reduce to nearest-neighbor search over the training set, which will fail to correspond to the underlying population distribution over images and thus yield significant statistical error (see 4a for an example of this failure mode as the length scale L is taken large). Meanwhile, if L < L c, the noised training set distribution provides a robust estimate for the true distribution over noised images, but the information the model learns about the sample 

I(φ; ϕΩL;x ) will be less than the critical value I(φ; ϕΩLc ;x ), so it will perform worse than a higher sub-critical scale. In low dimensions, there is no longer a ‘sharp’ phase transition as above, but the optimal scale L will nonetheless generally sit around L ≈ Lc.Without any assumptions on the data distribution, we can make a very general statement about the shape of the phase boundary Lc(σt). Since I(φ; ϕt, ΩL,x ) monotonically increases with L and decreases with σt, implicit differentiation of (6) with respect to L implies that Lc(σt) monotonically increases with the noise σt.With relatively few additional assumptions, we can make more precise statements about the phase boundary Lc(σt) and its dependence on |D| . We first establish the following bounds, which we call the ‘anisotropic’ and ‘isotropic’ Gaussian bounds (see App. F.3): 

Lemma 5.1 Suppose φ is drawn from an arbitrary data distribution P0 with mean μ and covariance 

Σ. We define the distribution Q1 = N (μ, Σ) to be a Gaussian with identical second-order statistics to P0, and Q2 = N (μ, Diag [Σ]) to be the Gaussian with identical diagonal covariance and zero off-diagonal elements. We then have Lc;P (σt) ≥ Lc;Q1 (σt) ≥ Lc;Q2 (σt).

The Gaussian bounds are helpful numerically because they can be computed directly from the data covariance matrix, which enables direct numerical computation of a lower bound on the critical scale for realistic data (fig. 4b). It also theoretically provides a general lower bound of the form 

Lc(σt) ∼ Ω( σt

pln |D| ) (see App. E.2 for details). We also show (see App. thm. F.3 for proof) a very general scaling theorem, which covers a wide range of naturalistic images and shows that 

Lspec ∼ O(σt) scaling of the critical scale is generic :

Theorem 5.2 Suppose we have a translationally-invariant distribution over images φ with bounded pointwise variance and asymptotically image-size-extensive entropy, with an entropy density 

lim |Ω|→∞ 1 

> |Ω|

S(φΩ) = ˜S. Then, for ln |D| which is O(1) with respect to σt,

Lc(σt) ∼ O σt

pln |D|  (7) The intuition behind this theorem is that for sufficiently high noise, each pixel will confer an amount of information proportional to the signal-to-noise ratio O(1 /σ 2 

> t

) about the underlying image φ, so the whole patch provides O(σ−2 

> t

|Ω|) bits of information; setting this equal to ln |D| and rearranging gives (7). Our theory implies if L > L c(σt), the spatially local BIRD denoiser will memorize. We examine the denoising performance of such a denoiser as a function of the patch scale L and confirm this memorization behavior in Fig. 4a. However, merely avoiding memorization is not sufficient to guarantee that a model can denoise effectively : for instance, as shown in Fig. 4a, BIRD model with a small scale such as L ≪ Lc may be robust to the training data (i.e. generalize) but be a poor denoiser. The reason is that with such a small patch size L, it cannot access correlations that are important for denoising (at a given noise level). We next examine another length scale for natural images that is relevant for (linear) denoising. 

5.2 The spectral length scale for effective linear denoising 

While natural images contain correlations at many different length scales, existing explorations of spatial locality in diffusion models [32, 40] emphasize a quantity which we call the spectral scale ,8which corresponds to the scale of relevant second-order correlations in the data. Naturalistic im-ages have a well-known power-law falloff in their power spectral density of P (k) ≈ A|k|−α [39]. The exponent α is typically α ≈ 2 − ϵ, with ϵ typically in the range 0.1-0.3, near the perfectly scale-invariant value α = 2 . When performing denoising, there is a natural spectral length scale, defined as the inverse of the spatial frequency where the noise power σ2 

> t

equals the signal power, yielding Lspec = ( σ2 

> t
> A

)1/α . For exactly scale-invariant images with α = 2 − ϵ, this reduces to 

Lspec ∼ σ2/(2 −ϵ) 

> t

. This scale can also be interpreted as the approximate radius of the optimal linear denoiser, or the Wiener filter [41]. Because of the relatively compact support of this filter, a spatially local BIRD model with patch scale L ≥ Lspec should still denoise with relatively small error. In-deed, [32] established empirically that BIRD models with L ∼ Lspec generated reasonable samples. Conversely if L ≪ Lspec we should expect poor denoising, as confirmed in Fig.4a. This raises a critical issue for BIRD models: their patch scale L may need to be close to the spectral scale Lspec to achieve good denoising performance, but if Lspec ≫ Lc, then the BIRD model cannot access the spectral scale without exhibiting deleterious memorization. How much data is required so that a BIRD model can at least access the spectral scale Lspec without memorizing. Our theoretical results in Sec.5.1 imply that for Lc ∼ Lspec at all noise levels σt, we need the proportionality Lc ∼ σt

pln |D| ∼ σ 22−ϵ 

> t

∼ Lspec to hold, which implies ln |D| ∼ σ 2ϵ 

> 2−ϵ
> t

∼

Lϵspec . The largest spectral scale that the model may need to access is the image size LI . This implies that the minimum dataset size |D| required to avoid memorization must scale with image size LI as 

ln |D| ∼ LϵI (8) When ϵ is taken to 0 (i.e. for perfectly-scale invariant data), we see that there is no required scaling of dataset size with image size , showing that spatially local BIRD models easily circumvent the curse of dimensionality for perfectly scale-invariant data. Next, we numerically compare the critical scale Lc(σt) and the spectral scale Lspec (σt) for several standard image datasets. We compute Lc(σt) by solving (6) using an estimator for the mutual information. We can also compute the spectral scale Lspec from the dataset’s power spectral density. In Fig 4b, we show Lc(σt) and Lspec (σt) and a theoretical prediction for Lc from a Gaussian bound. We see clearly in these curves that the scaling behavior of Lc and Lspec are highly aligned, consistent with our theoretical expectations for scale-invariant image data. See App. F.6 for more experimental details. 

## 6 Discussion 

Using information theory, we were able to obtain the memorization-generalization phase diagram for a highly general class of BIRD models and for a very general class data distributions. While BIRD models introduce inductive biases through information restriction, a significant limitation of our theory is that it cannot capture potentially other inductive biases due to architectural choices or learning dynamics. For example, BIRD models cannot express a bias towards linearity which may arise in trained models. However, we are not aware of any theory of diffusion models that can precisely delineate memorization-generalization phase transitions boundaries with arbitrary ar-chitectures and data distributions. All existing theory either use simplified architectures or simple models of data. BIRD models therefore provide an exciting new theoretical laboratory to study memorization and generalization across many possible data distributions. 

## References 

[1] Jascha Sohl-Dickstein, Eric Weiss, Niru Maheswaranathan, and Surya Ganguli. Deep unsuper-vised learning using nonequilibrium thermodynamics. In International conference on machine learning , pages 2256–2265. PMLR, 2015. [2] Jonathan Ho, Ajay Jain, and Pieter Abbeel. Denoising diffusion probabilistic models. Ad-vances in neural information processing systems , 33:6840–6851, 2020. [3] Jiaming Song, Chenlin Meng, and Stefano Ermon. Denoising diffusion implicit models. arXiv preprint arXiv:2010.02502 , 2020. 9[4] Zahra Kadkhodaie, Florentin Guth, Eero P Simoncelli, and St´ ephane Mallat. Generalization in diffusion models arises from geometry-adaptive harmonic representation. arXiv preprint arXiv:2310.02557 , 2023. [5] Shai Shalev-Shwartz and Shai Ben-David. Understanding Machine Learning: From Theory to Algorithms . Cambridge University Press, 2014. [6] Giulio Biroli, Tony Bonnaire, Valentin De Bortoli, and Marc M´ ezard. Dynamical regimes of diffusion models. arXiv preprint arXiv:2402.18491 , 2024. [7] Valentin De Bortoli. Convergence of denoising diffusion models under the manifold hypothe-sis. arXiv preprint arXiv:2208.05314 , 2022. [8] Beatrice Achilli, Luca Ambrogioni, Carlo Lucibello, Marc M´ ezard, and Enrico Ventura. Mem-orization and generalization in generative diffusion under the manifold hypothesis. Journal of Statistical Mechanics: Theory and Experiment , 2025(7):073401, 2025. [9] Gen Li and Yuling Yan. Adapting to unknown low-dimensional structures in score-based diffusion models. Advances in Neural Information Processing Systems , 37:126297–126331, 2024. [10] Minshuo Chen, Kaixuan Huang, Tuo Zhao, and Mengdi Wang. Score approximation, estima-tion and distribution recovery of diffusion models on low-dimensional data. In International Conference on Machine Learning , pages 4672–4712. PMLR, 2023. [11] Sixu Li, Shi Chen, and Qin Li. A good score does not lead to a good generative model. arXiv preprint arXiv:2401.04856 , 2024. [12] Christopher Scarvelis, Haitz S´ aez de Oc´ ariz Borde, and Justin Solomon. Closed-form diffusion models. arXiv preprint arXiv:2310.12395 , 2023. [13] Mason Kamb and Surya Ganguli. An analytic theory of creativity in convolutional diffusion models. In Forty-second International Conference on Machine Learning , 2025. [14] Matthew Niedoba, Berend Zwartsenberg, Kevin Patrick Murphy, and Frank Wood. Towards a mechanistic explanation of diffusion model generalization. In Forty-second International Conference on Machine Learning , 2025. [15] Mingyang Yi, Jiacheng Sun, and Zhenguo Li. On the generalization of diffusion model. arXiv preprint arXiv:2305.14712 , 2023. [16] Xiangming Gu, Chao Du, Tianyu Pang, Chongxuan Li, Min Lin, and Ye Wang. On memoriza-tion in diffusion models. arXiv preprint arXiv:2310.02664 , 2023. [17] Bao Pham, Gabriel Raya, Matteo Negri, Mohammed J Zaki, Luca Ambrogioni, and Dmitry Krotov. Memorization to generalization: Emergence of diffusion models from associative memory. arXiv preprint arXiv:2505.21777 , 2025. [18] Zhengdao Chen. On the interpolation effect of score smoothing in diffusion models. In The Fourteenth International Conference on Learning Representations , 2026. URL https:// openreview.net/forum?id=O33LAUliUF .[19] Binxu Wang and John J Vastola. The unreasonable effectiveness of gaussian score approxima-tion for diffusion models and its applications. arXiv preprint arXiv:2412.09726 , 2024. [20] Binxu Wang, Jacob Zavatone-Veth, and Cengiz Pehlevan. A random matrix theory perspective on the consistency of diffusion models. arXiv preprint arXiv:2602.02908 , 2026. [21] Ye He, Yitong Qiu, and Molei Tao. Diffusion model’s generalization can be characterized by inductive biases toward a data-dependent ridge manifold. arXiv preprint arXiv:2602.06021 ,2026. 10 [22] Peng Wang, Huijie Zhang, Zekai Zhang, Siyi Chen, Yi Ma, and Qing Qu. Diffusion models learn low-dimensional distributions via subspace clustering. In 2025 IEEE 10th International Workshop on Computational Advances in Multi-Sensor Adaptive Processing (CAMSAP) , pages 211–215. IEEE, 2025. [23] Alessandro Favero, Antonio Sclocchi, and Matthieu Wyart. Bigger isn’t always memorizing: Early stopping overparameterized diffusion models. arXiv preprint arXiv:2505.16959 , 2025. [24] Tony Bonnaire, Rapha¨ el Urfin, Giulio Biroli, and Marc Mezard. Why diffusion models don’t memorize: The role of implicit dynamical regularization in training. In The Thirty-ninth Annual Conference on Neural Information Processing Systems , 2026. URL https: //openreview.net/forum?id=BSZqpqgqM0 .[25] Alessandro Favero, Antonio Sclocchi, Francesco Cagnetta, Pascal Frossard, and Matthieu Wyart. How compositional generalization and creativity improve as diffusion models are trained. In International Conference on Machine Learning , pages 16286–16306. PMLR, 2025. [26] Lorenzo Bardone, Claudia Merger, and Sebastian Goldt. A theory of learning data statistics in diffusion models, from easy to hard. arXiv preprint arXiv:2603.12901 , 2026. [27] Nicholas M Boffi, Arthur Jacot, Stephen Tu, and Ingvar Ziemann. Shallow diffusion networks provably learn hidden low-dimensional structure. arXiv preprint arXiv:2410.11275 , 2024. [28] Hugo Cui, Cengiz Pehlevan, and Yue M Lu. A solvable model of learning generative diffusion: theory and insights. arXiv preprint arXiv:2501.03937 , 2025. [29] TaeHo Yoon, Joo Young Choi, Sehyun Kwon, and Ernest K Ryu. Diffusion probabilistic mod-els generalize when they fail to memorize. In ICML 2023 workshop on structured probabilistic inference {\ &} generative modeling , 2023. [30] Hugo Cui, Florent Krzakala, Eric Vanden-Eijnden, and Lenka Zdeborov´ a. Analysis of learning a flow-based generative model from limited sample complexity. In International Conference on Learning Representations , volume 2024, pages 51929–51955, 2024. [31] Sam Buchanan, Druv Pai, Yi Ma, and Valentin De Bortoli. On the edge of memorization in diffusion models. Advances in Neural Information Processing Systems , 38:96113–96157, 2026. [32] Artem Lukoianov, Chenyang Yuan, Justin Solomon, and Vincent Sitzmann. Locality in image diffusion models emerges from data statistics. arXiv preprint arXiv:2509.09672 , 2025. [33] Arwen Bradley. Local mechanisms of compositional generalization in conditional diffusion. 

arXiv preprint arXiv:2509.16447 , 2025. [34] Emma Finn, T Anderson Keller, Manos Theodosis, and Demba E Ba. Origins of creativity in attention-based diffusion models. arXiv preprint arXiv:2506.17324 , 2025. [35] Fangjun Hu, Guangkuo Liu, Yifan Zhang, and Xun Gao. Local diffusion models and phases of data distributions. arXiv preprint arXiv:2508.06614 , 2025. [36] Luca Ambrogioni. How out-of-equilibrium phase transitions can seed pattern formation in trained diffusion models. arXiv preprint arXiv:2603.20092 , 2026. [37] Georg A Gottwald, Shuigen Liu, Youssef Marzouk, Sebastian Reich, and Xin T Tong. Local-ized diffusion models. arXiv preprint arXiv:2505.04417 , 2025. [38] Minh Hai Nguyen, Quoc Bao Do, Edouard Pauwels, and Pierre Weiss. An analytic theory of convolutional neural network inverse problems solvers. arXiv preprint arXiv:2601.10334 ,2026. [39] Daniel Ruderman and William Bialek. Statistics of natural images: Scaling in the woods. 

Advances in neural information processing systems , 6, 1993. [40] Sander Dieleman. Diffusion is spectral autoregression, 2024. URL https://sander.ai/ 2024/09/02/spectral-autoregression.html .11 [41] Norbert Wiener. Extrapolation, interpolation, and smoothing of stationary time series: with engineering applications . The MIT press, 1949. [42] Tero Karras, Miika Aittala, Timo Aila, and Samuli Laine. Elucidating the design space of diffusion-based generative models. Advances in neural information processing systems , 35: 26565–26577, 2022. [43] Beatrice Achilli, Marco Benedetti, Giulio Biroli, and Marc M´ ezard. Theory of speciation transitions in diffusion models with general class structure, 2026. URL https://arxiv. org/abs/2602.04404 .[44] B. Derrida. Random-energy model: Limit of a family of disordered models. Phys. Rev. Lett. ,45:79–82, Jul 1980. doi: 10.1103/PhysRevLett.45.79. URL https://link.aps.org/doi/ 10.1103/PhysRevLett.45.79 .[45] Marc M´ ezard and Andrea Montanari. Information, Physics, and Computation . Ox-ford University PressOxford, January 2009. ISBN 9780191718755. doi: 10.1093/ acprof:oso/9780198570837.001.0001. URL http://dx.doi.org/10.1093/acprof:oso/ 9780198570837.001.0001 .[46] Thomas M. Cover and Joy A. Thomas. Elements of Information Theory (Wiley Series in Telecommunications and Signal Processing) . Wiley-Interscience, USA, 2006. ISBN 0471241954. [47] Enrico Ventura, Beatrice Achilli, Gianluigi Silvestri, Carlo Lucibello, and Luca Ambrogioni. Manifolds, random matrices and spectral gaps: The geometric phases of generative diffusion. 

arXiv preprint arXiv:2410.05898 , 2024. [48] Matthew Niedoba, Berend Zwartsenberg, Kevin Murphy, and Frank Wood. Towards a mecha-nistic explanation of diffusion model generalization. arXiv preprint arXiv:2411.19339 , 2024. [49] Hu Yu, Li Shen, Jie Huang, Hongsheng Li, and Feng Zhao. Unmasking bias in diffusion model training. In European Conference on Computer Vision , pages 374–390. Springer, 2024. [50] Katherine Deck and Tobias Bischoff. Easing color shifts in score-based diffusion models. 

arXiv preprint arXiv:2306.15832 , 2023. [51] Jooyoung Choi, Jungbeom Lee, Chaehun Shin, Sungwon Kim, Hyunwoo Kim, and Sungroh Yoon. Perception prioritized training of diffusion models. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition , pages 11472–11481, 2022. [52] Tim Salimans and Jonathan Ho. Progressive distillation for fast sampling of diffusion models. 

arXiv preprint arXiv:2202.00512 , 2022. 12 Appendices 

## A Notation and background on diffusion models. 

A.1 Notation conventions 

We use the following notation: • φ ∈ Rd denotes an example from the training set. For images or patches of size L pixels by L pixels by C color channels, we have d = L × L × C.• ϕ represents any arbitrary image (or other data) that can serve as input to either the score function or diffusion model. • P0(φ) denotes the true data (i.e. target) distribution, from which we sample both our train-ing and test data. • D represents a finite set of training data consisting of |D| training samples. Each training data point φ ∈ D is drawn i.i.d. from the true data distribution φ ∼ P0.• Ptrain (φ) = 1

> |D|

P 

> φ′∈D

δ(φ − φ′) denotes the empirical training distribution for any fixed realization D of the training set. It is simply a uniform distribution over |D| specific training examples φ ∈ D .• x represents a pixel location in an image. • For image data, ϕx and φx will represent the pixel values of the images ϕ and φ at pixel location x; both are elements of RC .• M [ϕ] : Rd → Rd represents a model that takes as input an image ϕ and produces a new image (e.g. an estimate of the score function, or a denoised version of ϕ). We will denote by 

Mx[ϕ] ∈ RC the value of the outputs of this model, given an input ϕ, at the pixel location 

x.• Ωx denotes a patch neighborhood indexed by the pixel x. The pixel location x will be contained within Ωx. We will denote by |Ωx| the number of pixels in the patch. We will also denote by Ωx,L a patch neighborhood of x with a characteristic length L, such that the size of the patch is |Ωx;L| = L2.• ϕΩx and φΩx denotes the restriction of images ϕ and φ to a neighborhood Ωx around a pixel x. For images with C channels, we have ϕΩx ∈ R|Ωx|× C .• Cx,t : Rd → Rn denotes a (possibly stochastic) observation map that takes as input a noised image ϕt ∈ Rd, and outputs the n dimensional observation Cx,t (ϕt) that pixel x

makes about the image ϕt. If the map is stochastic we denote its conditional distribution by 

P (Cx,t |ϕt). If the map is deterministic we denote it by the function Cx,t (ϕt). For brevity we also sometimes drop the argument ϕt, and then Cx,t ∈ Rn denotes the pixel x’s observation value, reflecting the state of knowledge of pixel x at time t in the diffusion process. • We denote the training Markov process φ → ϕt → C x,t for pixel x at time t by the joint distribution Ptrain (φ, ϕ t, Cx,t ) = Ptrain (φ)P (ϕt|φ)P (Cx,t |ϕt). Here φ ∼ Ptrain (φ) is a randomly chosen training image φ ∈ D , P (ϕt|φ) is the conditional distribution of a noisy image ϕt under the forward diffusion process, starting from the training image φ , and 

P (Cx,t |ϕt) is the (possibly stochastic) observation that pixel x makes about image ϕt. We can think about the Markov process φ → ϕt → C x,t as an information channel from the past training data D at time t = 0 to the current pixel observation Cx,t at time t. We denote by Ptrain (φ|C x,t ) the Bayesian posterior distribution over the training data given a pixel 

x’s channel observation Cx,t . This posterior assigns a probability to each of the |D| training images φ ∈ D . If a pixel x had to guess, based on its (restricted) observation Cx,t of the noisy image ϕt, which clean training image φ ∈ D at time 0 in the past led to ϕt under the forward process, then any optimal guess would be based on the posterior Ptrain (φ|C x,t ),which encapsulates the pixel’s belief state about the past training data origin of its current observation. 13 • We denote the testing Markov process φ → ϕt → C x,t for pixel x at time t by the joint distribution Ptest (φ, ϕ t, Cx,t ) = P0(φ)P (ϕt|φ)P (Cx,t |ϕt). This is identical to the training Markov process except the prior is now the true data distribution P0(φ) from which a test sample is drawn, rather than the empirical training distribution Ptrain (φ) for a specific dataset D. We denote the posterior distribution of the input given the output under this Markov process by Ptest (φ|C x,t ). Unlike Ptrain (φ|C x,t ), the support of Ptest (φ|C x,t ) need not be restricted to the training data φ ∈ D .• ¯αt denotes the signal variance in the forward diffusion process. • σ2 

> t

= 1− ¯αt 

> ¯αt

denotes the noise-to-signal ratio at a time t.• I(A; B) will indicate the mutual information between two random variables A and B.

I(A; B ∼ P ) will indicate the mutual information between these variables with a particular emphasis that the random variable B is distributed according to P .• S[P ] will denote the entropy of a distribution P . SP [A] will indicate the entropy of the random variable A under the distribution P .• N (x|μ, Σ) represents the PDF of the normal distribution with mean μ and covariance Σ.We also use the short-hand N (μ, Σ) when we do not need to refer to the name of a specific random variable. 

A.2 Background on diffusion models 

Diffusion models are designed to solve the problem of generating samples from a complex data distribution φ ∼ P0(φ) in high dimensions (e.g. for φ ∈ Rd for large d). To do so, they first develop, through a forward diffusion process, an intermediate bridge between the complex data distribution 

P0(φ) and simple isotropic Gaussian noise distribution η ∼ N (0 , I ). This bridge corresponds to a time dependent family of distributions of noised data ϕt ∼ Pt(ϕt) that interpolate between data points φ at time t = 0 and pure isotropic noise η at time t = 1 . A single sample from this bridge at any fixed time t can be obtained via the interpolation 

ϕt(φ, η ) = √¯αtφ + √1 − ¯αtη. (9) The parameter ¯αt can be thought of as the signal power, while 1 − ¯αt can be thought of as the noise power ¯αt. The temporal schedule of the signal power is chosen to monotonically decrease from 

¯α0 = 1 at time t = 0 to ¯α1 = 0 at time t = 1 . Thus over the time interval t ∈ [0 , 1] , the forward diffusion process gradually turns data φ into noise η through the family of noised samples ϕt. We define a useful quantity σ2 

> t

, which characterizes the noise to signal ratio at a time t:

σ2 

> t

= 1 − ¯αt

¯αt

. (10) (This parameter is the direct analog of the σ2 

> t

parameter in variance-exploding forward processes [42]). The interpolation in (9) induces a flow on distributions Pt(ϕt) defined by the forward diffusion partial differential equation 

dP t(ϕt)

dt = γt∇ · (ϕtPt(ϕt)) + γt∇2Pt(ϕt), (11) with γt = − 12 ∂t ln ¯ αt. Diffusion models in deterministic mode (DDIM parameterization, [3]) aim to reverse this flow by evolving pure noise samples ϕ1 ∼ N (0 , I ) backwards in time under the ODE 

dϕ t

dt = −γt(ϕt + ∇ log Pt(ϕt)) , (12) where ∇ log Pt is the score function for the distribution Pt(ϕt) over noised samples induced by the interpolation in (9) or equivalently the forward diffusion equation in (11). This ODE provably transports the distribution N (0 , I ) back to the data distribution P0(φ) through the intermediate family of distributions Pt(ϕt) as time flows backwards from t = 1 to t = 0 . Thus the reverse process in (12) exactly reverses the forward diffusion in (11) in terms of the time-dependent family of marginal distributions Pt(ϕt).14 However, implementing the reverse process in (12) requires access to the score function ∇ log Pt.When direct calculation of the score is not available or not desirable, there is a convenient iden-tity that reframes the score computation as a problem of optimal denoising, known as Tweedie’s theorem: 

∇ log Pt(ϕt) = − E[η|ϕt]

√1 − ¯αt

=

√¯αt

1 − ¯αt

E[φ|ϕt] − ϕt

1 − ¯αt

(13) Thus the calculation of the posterior mean E[φ|ϕt] over the data φ at time t = 0 , conditioned on the noised sample ϕt at any point t > 0 in the reverse flow, becomes a central quantity required to successfully implement the reverse flow to transport Pt(ϕt) back to P0(ϕ0 = φ). In turn, one can approximate this posterior mean by minimizing the following squared loss over the parameters θ of a neural network Mt,θ [ϕt]:

Lt = 12 Eφ∼P0,η ∼N (0 ,I )[∥φ − Mt,θ [ϕt(φ, η )] ∥2]. (14) This loss is a denoising loss that trains the neural network function Mt,θ to take any noisy sample 

ϕt at time t as input, and convert it back to the clean sample φ that led to ϕt under the forward diffusion process. It is well known that the minimum mean squared error (MMSE) solution to any optimization of the form in (14) will yield the posterior mean E[φ|ϕt] as the Bayes optimal estimator or denoiser. More precisely, if the family of neural network functions Mt,θ is expressive enough to compute this posterior mean, then for the optimal θ∗ that achieves zero loss in (14) we have the MMSE result 

Mt,θ ∗ [ϕt] = E[φ|ϕt]. (15) Learning the reverse process from a finite training set and then implementing it through a parametric neural network Mt,θ then corresponds at a high level to four main steps: 1. Replace the average over the true data distribution P0(φ) with an empirical average over a finite training set. 2. Minimize the loss in (14) to obtain the learned neural network M [t, θ ]( ϕt).3. Replace the posterior mean E[φ|ϕt] in Tweedie’s formula in (13) with its approximation 

Mt,θ [ϕt], thereby obtaining an approximate score function. 4. Insert this approximate score into the deterministic ODE representing the reverse process in (12). In practice, for purposes of variance reduction, rather than predicting the clean data φ from the noisy sample ϕt, one can also predict the noise η or the velocity vt through the following two losses respectively: 

Lηt = 12 Eφ∼P0,η ∼N (0 ,I )[∥η − Mt,θ [ϕt(φ, η )] ∥2] (16) 

Lvt = 12 Eφ∼P0,η ∼N (0 ,I )[∥vt(φ, η ) − Mt,θ [ϕt(φ, η )] ∥2]. (17) Here the velocity vt(φ, η ) roughly points from the clean data φ to the noisy sample ϕt with weighting coefficients: 

vt(φ, η ) = 

r ¯αt

1 − ¯αt

ϕt − φ

√1 − ¯αt

.

In analogy to (15), the MMSE neural network (assuming no expressivity constraints) computes the following posterior means for the losses Lηt and Lvt respectively: 

E[η|ϕt] = ϕt

√1 − ¯¯tα −

r ¯αt

1 − ¯αt

E[φ|ϕt]

E[vt(ϕt, η )|ϕt] = 

r ¯αt

1 − ¯αt

ϕt − 1

√1 − ¯αt

E[φ|ϕt].

The MMSE solutions to all three losses above produce a linear combination of ϕt and the optimal MMSE denoiser E[φ|ϕt]. Thus we can easily interchange theoretical results derived for one ap-proach to any other approach. For the remainder of the theory section, we will focus primarily on the loss (14) for which the optimal model simply computes E[φ|ϕt]. However, for numerical experi-ments in our paper, we use velocity prediction, which shows significant benefits from the perspective of training stability and the stability of generated outputs, especially early in training. 15 B Bayes-optimal diffusion models always memorize finite training sets 

In the denoising loss in (14) we never have direct access to the true data distribution P0(φ). Instead, we only have a finite dataset D consisting of |D| training samples φ ∈ D each drawn i.i.d from the true data distribution P0(φ). This leads to an empirical training distribution 

Ptrain (φ) = 1

|D| 

X

> φ′∈D

δ(φ − φ′), (18) which is simply a uniform sum of delta-functions over |D| specific training examples φ ∈ D . It follows that the Bayes optimal MMSE denoiser that achieves minimal loss in (14), when P0(φ)

replaced with Ptrain (φ), computes the posterior mean over the finite training set: 

Mt[ϕt] = E[φ|ϕt] = X

> φ∈D

φ P train (φ|ϕt). (19) Here Ptrain (φ|ϕt) is the posterior probability that a clean training data image φ ∈ D at time t = 0 

leads to the noisy observed image ϕt at a given time t under the forward diffusion process. Because the forward diffusion process shrinks the training data and adds isotropic Gaussian noise to it (see (9)), the forward conditional probability of obtaining ϕt starting from φ ∈ D is simply an isotropic Gaussian distribution whose mean shrinks with time and whose variance grows with time: 

P (ϕt|φ) = N (ϕt|√¯αtφ, (1 − ¯αt)I). (20) And since the prior over the training data in (18) is uniform, Bayes rule simply yields 

Ptrain (φ|ϕt) = P (ϕt|φ)

P 

> φ′

P (ϕt|φ′) = N (ϕt|√¯αtφ, (1 − ¯αt)I)

P 

> φ′∈D

N (ϕt|√¯αtφ′, (1 − ¯αt)I) . (21) In summary, the Bayesian posterior distribution Ptrain (φ|ϕt) in (21) over the past training data 

φ ∈ D given the current image ϕt provides a full analytic solution to both the Bayes optimal denoiser in (19) and the reverse process in (12) through Tweedie’s theorem in (13). As explained in [13], the structure of this analytic solution has an appealing interpretation in terms of a Bayesian guessing game about the past of the forward diffusion process given a noised image 

ϕt. This Bayesian guessing game is played by both the denoiser and the reverse process. In essence, both the denoiser and the reverse process are trying to guess which training image φ ∈ D at time 

t = 0 led to ϕt under the forward diffusion. The optimal Bayesian observer would summarize this guess in the Bayesian posterior distribution Ptrain (φ|ϕt) in (21). The optimal MMSE Bayesian denoiser then uses this posterior to compute the posterior mean training set image through (19). Similarly, the task of the reverse process is to flow ϕt backwards in time from t to 0 and reverse the forward diffusion. The Bayes-optimal reverse process (obtained by inserting (21) into (19), then inserting (19) into (13), and then finally inserting (13) into (12)) uses the posterior to flow ϕt back to each data point φ ∈ D with a Bayesian posterior weight controlled by Ptrain (φ|ϕt). In essence, the optimal Bayesian reverse process guesses the past training data origin of ϕt under the forward diffusion and flows back towards this origin. While the Bayes optimal diffusion model is appealing in terms of its both its analytic and conceptual simplicity, its sampling behavior is unfortunately quite unrealistic and does not explain what trained neural network based diffusion models actually do. The key issue is that for any finite training set, the Bayes optimal reverse process memorizes the training data, and can only flow to one of the training set points φ ∈ D (see also [13]). Intuitively, as time decreases from t = 1 to t = 0 in the reverse process, the posterior distribution in (21) increases its probability on training points φ close to ϕt, and in turn, ϕt then increasingly flows towards such training points. The positive feedback between posterior belief Ptrain (φ|ϕt) and reverse flow of ϕt to images φ of high belief, eventually causes ϕt to converge to a single training set point φ ∈ D .In terms of the Bayesian guessing game interpretation, with small amounts of data and low enough noise levels, the guessing game is too easy . With only a small number of training data points φ ∈ D ,which are likely to be well separated (relative to the noise scale σt) in an exponentially large high dimensional space, ϕt will generically be much closer to the data point φ∗ that generated it than any other data point φ̸ = φ∗. In this situation, the posterior belief Ptrain (φ|ϕt) will have a very high 16 probability when φ = φ∗ and a low probability otherwise. Thus the posterior belief Ptrain (φ|ϕt)

correctly guesses the origin of ϕt in the forward process. Even more intuitively, in terms of images, if one observes the entirety of a noised image ϕt at moderately low levels of noise, and one knows that the noised image came from a small set of widely separated clean training images, it is easy to guess which training image it came from. Thus the ease of the Bayesian guessing game in high dimensions is intimately tied to the deleterious memorization behavior of the Bayes optimal diffusion model. This memorization behavior hurts denoising on a test image; this denoiser will be overly influenced by a particular training set image. Also, from the diffusion model perspective, this memorization behavior also impedes creative generation of new images different from any image in the training set. 

## C Bayesian Information Restricted Diffusion (BIRD) Models 

We have seen that optimal Bayesian diffusion models memorize finite training sets precisely because the Bayesian guessing game of which training set image φ ∈ D leads to a given noise image ϕt in the forward diffusion is too easy, especially for small numbers of training images in a high dimensional space at low levels of noise (or equivalently last in the reverse process). This raises the question of whether one can obtain alternate Bayesian diffusion models that retain analytic tractability and conceptual simplicity, but also generalize appropriately, thereby behaving more like trained neural network diffusion models. We show that we can simultaneously achieve both theoretical tractability and more realistic gener-alization. The key idea is to prevent memorization by making the Bayesian guessing game harder 

without increasing the amount of training data |D| or increasing the noise level. In particular we combine two key approaches: 1) we instead make the Bayesian guessing game harder by restricting the image information the Bayesian guesser uses to guess the past of the forward diffusion process; and 2) we embrace diversity by allowing different parts of the image, down to the level of individual pixels , to use different pieces of restricted information to guess the past. The former prevents memo-rization while the latter enhances creative generalization. We call such models Bayesian Information Restricted Diffusion (BIRD) models. To introduce the general class of BIRD models, we first note that the denoising loss (14), as a simple squared loss, decouples across pixels x. In particular, we can write it as a sum over pixels x (or more generally, as a sum over any orthonormal basis over pixels): 

Lt = X

> x

Lx,t where Lx,t = 12 Eφ∼Ptrain ,ϕ t ∥φx − Mx,t [ϕt]∥2. (22) Here Mx,t [ϕt] ∈ RC is the evaluation of the model denoiser at time t at single pixel location x,and it is trained to recover the single clean training image pixel value φx ∈ RC . We can thus posit a separate denoiser model for each pixel and analyze them individually. Without this decoupling, analyzing the behavior of diffusion models with inductive biases becomes significantly less tractable; see e.g. [34] for an example of the complexities that arise when couplings between pixels are introduced. To achieve the restricted information, we ensure that the denoiser function Mx,t is not allowed to observe the entire noised image ϕt. Instead it is only allowed to make a restricted, possibly stochastic, observation Cx,t ∈ Rnx,t of ϕt ∈ Rd. We usually assume the observation dimensionality 

nx,t is less than the full image dimensionality d. If the observation is stochastic we denote its conditional distribution by P (Cx,t |ϕt). If it is deterministic we denote it by the function Cx,t (ϕt).For brevity we also sometimes drop the argument ϕt, and then Cx,t ∈ Rnx,t denotes the pixel x’s observation value. Furthermore we assume the that different pixels x can make different observations 

Cx,t about the noised image ϕt. This diversifies the state of knowledge of each pixel. The BIRD model is then simply the collection of individual pixel denoisers Mx,t [Cx,t ] that each achieve the MMSE loss for Lx,t in (22). Just like the full optimal Bayesian model, the MMSE BIRD model also has an analytic solution for pixel x given by 

Mx,t [Cx,t ] = X

> φ∈D

φx Ptrain (φ|C x,t ), (23) 17 where the Bayesian posterior is given by 

Ptrain (φ|C x,t ) = P (Cx,t |φ)

P 

> φ′

P (Cx,t |φ′) . (24) We can then combine these pixel-wise estimates into the BIRD model for the full image 

Mt[φ] = X

> x

Mx,t [Cx,t ]ˆ x (25) To understand this solution, consider the training Markov process φ → ϕt → C x,t for pixel x at time t, denoted by the joint distribution Ptrain (φ, ϕ t, Cx,t ) = Ptrain (φ)P (ϕt|φ)P (Cx,t |ϕt). Here 

φ ∼ Ptrain (φ) is a randomly chosen training image φ ∈ D , P (ϕt|φ) is the conditional distribution of a noisy image ϕt under the forward diffusion process, starting from the training image φ, and 

P (Cx,t |ϕt) is the (possibly stochastic) observation that pixel x makes about image ϕt. We can think about the training Markov process φ → ϕt → C x,t as an information channel from the past training data D at time t = 0 to the current pixel observation Cx,t at time t.

Ptrain (φ|C x,t ) in (24) is then the Bayesian posterior distribution over the training data φ ∈ D given a pixel x’s channel observation Cx,t . This posterior assigns a probability to each of the |D| training images φ ∈ D . If a pixel x had to guess, based on its (restricted) observation Cx,t of the noisy image 

ϕt, which clean training image φ ∈ D at time 0 in the past led to ϕt under the forward diffusion, then any optimal guess would be based on the posterior Ptrain (φ|C x,t ), which encapsulates the pixel’s belief state about the past training data origin of its current observation. The individual Bayes optimal pixel denoiser at pixel x, based on its restricted observation Cx,t , then simply returns in (23) the weighted average of pixel values φx of pixel x for all images φ in the training dataset D, where the weights are determined by the posterior Ptrain (φ|C x,t ).The channel restriction Cx,t at the end of the forward information channel φ → ϕt → C x,t makes the backwards Bayesian guessing game harder, and ideally prevents the posterior P (φ|C x,t ) from memorizing by concentrating on a single correct training example φ ∈ D . This behavior would be in contrast to the more informative posterior Ptrain (φ|ϕt) that always forces the unrestricted Bayes optimal diffusion model to memorize at low enough levels of noise (or equivalently late in the reverse process), as discussed in App. B. 

C.1 Examples of BIRD models: LS, ES, and ELS 

A natural and simple channel restriction Cx,t is a deterministic low dimensional projection applied to ϕt. In the case of a linear projection, Cx,t (ϕt) = Px,t ϕt where Px,t is an nx,t by d matrix with orthonormal rows. Under this restriction, the Bayesian posterior is given by 

P (φ|P x,t ϕt) = exp 



− 12(1 − ¯αt) ∥P x,t (ϕt − √¯αtφ∥2

> S

P 

> φ′

exp 



− 12(1 − ¯αt) ∥P x,t (ϕt − √¯αtφ′∥2

> S

 . (26) The Local Score (LS) machine of [13] is an instance of this framework when the projection operator 

Px,t projects onto a coordinate subspace of pixel components within a local patch Ωx surrounding pixel x:

P (φ|ϕt;Ω x ) = exp 



− 12(1 − ¯αt) ∥ϕt, Ωx − √¯αtφΩx ∥2P 

> φ′

exp 



− 12(1 − ¯αt) ∥ϕt, Ωx − √¯αtφΩx ∥2 . (27) Here we follow the notation of [13], where ϕΩx refers to a vector of concatenated pixel values of the image ϕ, but restricted only to all pixels inside the local patch of pixels Ωx. Projections onto subspaces in more general bases may be admitted, as hypothesized by [32], although the latter paper did not propose any specific basis other than the pixel basis used by [13]. While this framework is already fairly general, there is a further generalization of it that can be considered, which is to regress onto a correlated target Tx rather than onto φ itself: 

L = X

> x

12 ∥Tx − Mx[Cx,t ]∥2 

> φ,T x,Cx,t

, (28) 18 for which the minimizer is given by 

Mx[Cx,t ] = X

> φ

Z

dT x TxP (Tx|φ, Cx,t )P (φ|C x,t ) (29) This generalization may be useful for various stochastic flow-matching setups. It also allows us to reproduce the Equivariant Score (ES) machine and Equivariant Local Score (ELS) machine of [13] under a particular correlated choice of target Tx and channel Cx,t . [13] consider the setting of a model that is equivariant under a group G, which means that for any g ∈ G the model M [ϕ]

satisfies M [Ug ϕ] = Ug M [ϕ], where Ug is a unitary representation of the group G. Under the following choice of correlated channel and target: 

g ∼ Haar (G) (30) 

Cx,t = U † 

> g

ϕt (31) 

Tx = Ug φ (32) we reproduce the ES machine with the equation (29): 

Mx[ϕt] = X

> φ∈D

X

> g∈G

Ug φ P (φ, g |U † 

> g

ϕt) (33) 

P (φ, g |Ug ϕt) = exp 



− 12(1 − ¯αt) φ − U † 

> g

ϕt

> 2

P

> φ′∈D

P 

> g′∈G

exp 



− 12(1 − ¯αt) φ′ − U † 

> g′

ϕt

> 2

 (34) The ELS machine can be reproduced by specializing g to the group of translations T on images with 

Ug the fundamental representations, and then subsequently projecting the output U † 

> g

ϕt into a patch 

Ωx:

Mx[ϕt] = X

> φ∈D

X

> g∈T

(Ug φ)x P (φ, g |(U † 

> g

ϕt)Ωx ) (35) 

P (φ, g |Ug ϕt) = exp 



− 12(1 − ¯αt) φΩx − (U † 

> g

ϕt)Ωx

> 2

P

> φ′∈D

P 

> g′∈G

exp 



− 12(1 − ¯αt) φ′ 

> Ωx

− (U † 

> g′

ϕt)Ωx

> 2

 (36) 

## D A theory of BIRD memorization-generalization phase transitions 

In App. B we noted that unrestricted Bayes optimal diffusion models always memorize at low enough levels of noise (corresponding to late enough times in the reverse process). Motivated by this issue we introduced a very general class of BIRD models in App. C. In this section we now wish to develop a general theory of when such BIRD models memorize versus generalize. 

D.1 A high level overview of memorization versus generalization 

Memorization versus generalization can be assessed by the behavior of the posterior distribution 

Ptrain (φ|C x,t ) in (24) which encapsulates each pixel x’s belief state about which past training image 

φ ∈ D at time t = 0 led to the pixel’s current observation Cx,t at the current time t under the training Markov process for forward diffusion and observation φ → ϕt → Cx,t , specified by the joint distribution Ptrain (φ, ϕ t, Cx,t ) = Ptrain (φ)P (ϕt|φ)P (Cx,t |ϕt). In the memorization phase, the posterior Ptrain (φ|C x,t ) would concentrate on a single, or very small number of training images 

φ ∈ D .An important consequence of the concentration of the posterior Ptrain (φ|C x,t ) is that the accom-panying BIRD denoiser and diffusion model then both become extremely sensitive to the particular realization of the training data D. For example, consider two distinct data sets D and D′ each drawn i.i.d. from the same true data distribution P0(φ). Because the posterior Ptrain (φ|C x,t ) concentrates on individual (or a small number of) data points in the memorizing phase, the BIRD model denoiser and reverse process would yield different outputs even for the same observation inputs Cx,t when 19 using the two different training datasets D and D′. Thus the BIRD model would not be robust to the choice of the training data D.In contrast, the hallmark of the generalization phase is that a BIRD model should be robust to the choice of training data D. In particular, averages of quantities with respect to the posterior 

Ptrain (φ|C x,t ), should not depend on the detailed realization of the training data D. An important such average is the posterior mean computed by the denoiser output Mx,t [Cx,t ] in (23). In this robust regime, two different denoisers, and two different BIRD model reverse samplers, obtained from two different datasets D and D′, consistently generalize to the same denoiser and sampler. How do we describe this common denoiser in the robust, consistent generalization phase? Well, a simple way to achieve this phase is to take the limit of a large number of data points |D| . Then averages of quantities with respect to Ptrain (φ) in (18) should converge to averages with respect to the true distribution P0(φ) from which a new test example φ would be drawn. Thus we can consider the testing Markov process φ → ϕt → C x,t for pixel x at time t, defined by the joint distribution Ptest (φ, ϕ t, Cx,t ) = P0(φ)P (ϕt|φ)P (Cx,t |ϕt). This is identical to the training Markov process above, except the prior is now the true data distribution P0(φ) from which a test sample is drawn, rather than the empirical training distribution Ptrain (φ) for a specific dataset D. We denote the posterior distribution of the input φ given the output Cx,t under this Markov process by 

Ptest (φ|C x,t ). This posterior distribution is the outcome of the Bayesian guessing game in the limit of a large amount of data. With the testing Markov process in place, we can see that in the robust, consistent generaliza-tion phase, the denoiser output in (23) converges to a common denoiser for any dataset D, where the dataset dependent posterior Ptrain (φ|C x,t ) is replaced with the dataset indpendent posterior 

Ptest (φ|C x,t ). This common denoiser then determines a common reverse sampler as described in App. B. In this fashion, in the generalization phase, when two different BIRD models are trained on two different datasets, they both consistently converge to this same sampler. 

D.2 The entropy of Bayesian guessing determines memorization and generalization 

We would like to derive sharper conditions on the minimum amount of training data |D| required to achieve generalization instead of memorization, and how such a data amount threshold depends on time t in the reverse process (or equivalently the amount of noise), as well as the amount of information the restricted observation Cx,t provides about the training data φ ∈ D .To do so, it is useful to focus on the entropy of the outcome of the Bayesian guessing game, encap-sulated by posterior distribution Ptrain (φ|C x,t ). This entropy is given by 

S[Ptrain (φ|C x,t )] = − X

> φ∈D

Ptrain (φ|C x,t ) ln Ptrain (φ|C x,t ). (37) This entropy can be written as the prior entropy S[Ptrain (φ)] = ln |D| , minus the amount of infor-mation learned about the past training data φ ∈ D relative to the prior by the current measurement 

Cx,t , also known as the Bayesian surprise :

S[Ptrain (φ|C x,t )] = ln |D| − DKL (Ptrain (φ|C x,t ) || Ptrain (φ)) . (38) In the memorization phase, Ptrain (φ|C x,t ) concentrates on a single point and so the entropy van-ishes. In a generalizing phase, the posterior will have support on many samples, and so we may expect the Bayesian surprise DKL (Ptrain (φ|C x,t ) || Ptrain (φ)) to converge to its value under the true data distribution, i.e. DKL (Ptest (φ|C x,t ) || P0(φ)) .As we will see below, when both the logarithm of the amount of data ln |D| and the data dimension 

d are large, there is a sharp phase transition between the two phases. We find that whenever ln |D| − 

DKL (Ptest (φ|C x,t ) || P0(φ)) ≥ 0, the training surprise matches the test surprise. This corresponds to the generalization phase. Conversely, when this inequality does not hold, the posterior entropy is zero. Thus we obtain the following formula in the large ln |D| limit (see Theorem D.1 in App. D.5 for a formal theorem statement and proof): 

S[Ptrain (φ|C x,t )] = 

ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) ln |D| > D KL (Ptest (φ|C x,t )|| P0(φ)) 0 ln |D| ≤ DKL (Ptest (φ|C x,t )|| P0(φ)) 

(39) 20 This result yields a pointwise lower threshold for the minimum amount of data |D| required to avoid memorization and achieve generalization for any fixed observation Cx,t . If ln |D| is less than the Bayesian test surprise DKL (Ptest (φ|C x,t )|| P0(φ)) , then the BIRD model is in the memorization regime given that observation. To understand this result intuitively, note that the Bayesian surprise quantifies how much a pixel x

needs to update its prior belief distribution about the training data P0(ϕ) to obtain the posterior belief distribution Ptest (φ|C x,t ) after making the observation Cx,t . If this surprise is high, then intuitively 

Cx,t is highly informative about φ. Thus the higher the surprise, the more data |D| is required to avoid memorization for any fixed observation Cx,t . Indeed we obtain the extremely simple condition that the amount of data |D| needs to be at least exponential in the surprise to achieve generalization. Conversely, when ln |D| is greater than the surprise, the posterior entropy is nonnegative, indicating the generalization phase. And quantitatively it is equal to the prior entropy minus the Bayesian surprise, yielding the remaining entropy after the observation. While the above result holds pointwise for a single observation Cx,t , we can also find a simple averaged condition when Cx,t is drawn randomly from the test distribution via the testing Markov process φ → ϕt → C x,t where φ ∼ P0(ϕ). As is well known, the average of the Bayesian surprise 

DKL (Ptest (φ|C x,t )|| P0(φ)) over the marginal distribution Ptest (Cx,t ) yields the mutual information 

I(φ; Cx,t ). Thus if the average surprise is high, so is the mutual information (as they are equal). This yields an extremely simple and highly general information theoretic criterion for the memorization-generalization phase transition boundary in any BIRD model and a wide variety of data distributions: 

ln |D| > I (φ; Cx,t ) Generalization phase. 

ln |D| < I (φ; Cx,t ) Memorization phase. (40) This result then implies that restricting information contained in observations can reduce the mini-mum data threshold required for generalization in BIRD models. In summary, (40) reveals that the amount of data |D| needs to be at least exponential in the mutual information between current observations Cx,t and the true test data φ ∼ P0(φ) in the forward testing Markov process φ → ϕt → C x,t , to avoid memorization and achieve generalization. More formal statements of these results and their proofs are given below. 

D.3 Three types of phase transitions between memorization and generalization. 

One can think of the phase transition boundary between the memorization phase and generalization phase, determined by the equality condition I(Ct,x ; φ) = ln |D| in several ways: 

A data phase transition. First, in terms of the amount of data |D| for a fixed observation method 

Cx,t and fixed time t, as ln |D| increases from below I(Ct,x ; φ) to above I(Ct,x ; φ), we transition from the memorization to generalization phase. The critical amount of data Dc at which this tran-sition occurs obeys the equation I(Ct,x ; φ) = ln Dc. For |D| < D c any BIRD model memorizes while for |D| > D c it generalizes. 

A temporal phase transition. Second, we can consider time t in the reverse process at a fixed amount of data |D| (and an observation method that does not depend explicitly on time). At times t

near 1 near the beginning of the reverse process, the noise to signal ratio σ2 

> t

is large and therefore the mutual information I(Ct,x ; φ) is small. However at times t near 0 near the end of the reverse process, the noise to signal ratio σ2 

> t

is small and therefore the mutual information I(Ct,x ; φ) is large. There-fore, as the reverse process proceeds from t = 1 down to t = 0 , the mutual information I(Ct,x ; φ)

could in principle transition from below ln |D| to above ln |D| , implying the reverse process could transition from a generalizing phase to a memorizing phase. The temporal boundary tb at which this transition occurs obeys the equation I(Ctb,x ; φ) = ln |D| . For t > t b any BIRD model (with a fixed, time-independent observation method) generalizes early in the reverse process, while for t < t b it memorizes late in the reverse process. 

An observation phase transition. Third, we can consider a one parameter family of observations 

Cx,t of varying information content, at a fixed time t and a fixed amount of data |D| . For example, let nx,t be the dimensionality of the observation Cx,t such that reducing nx,t monotonically drops 21 measurements of ϕt. As a concrete example, let Cx,t (ϕ) = ϕΩx be the restriction of the image ϕ

to a local L by L image patch Ωx centered at x, yielding the restricted image ϕΩx of dimension 

nx,t = L × L × C. In this example, reducing nx,t by reducing the patch size L strictly reduces the information content of the observation Cx,t . Now when nx,t varies from large to small, the mutual information I(Ct,x ; φ) can also vary from large to small. Thus as nx,t varies from large to small, the mutual information I(Ct,x ; φ) can in principle transition from above ln |D| to below 

ln |D| , implying a transition from memorizing to generalizing as the dimensionality or information content of the observation Cx,t is reduced. The critical dimension nc at which this transition occurs obeys the equation I(Cnc

> t,x

; φ) = ln |D| . For nx,t > n c any BIRD model memorizes for highly informative observations, while for nx,t < n c it generalizes for information restricted observations. 

A general co-dimension one phase boundary. Of course in full generality, in the joint space of the amount of data |D| , the time t in the reverse process, and any family of observations Cx,t of varying information content, the equation I(Ct,x ; φ) = ln |D| determines a co-dimension one phase boundary between the memorization and generalization phase. The three factors of amount of data, time in reverse process, and information content of observations will compete to determine which phase the BIRD model is in. Small amounts of data, late or small times t, and large observational information content, each favor the memorization phase, while opposite directions favor generaliza-tion. As we will see, BIRD models can combat memorization at late times in the reverse process, by reducing the information content of observations Cx,t as t decreases. The observational restriction impedes the memorization at small times t, and if strong enough, favors generalization instead. 

D.4 A simple calculation of the posterior entropy in the generalizing phase 

Consider a channel Cx,t and a set of codewords or images |D| drawn i.i.d. from the distribution P0.Given a input φ the channel produces the output Cx,t with probability density P (Cx,t |φ). We can define the training set probability distribution conditioned on the channel outputs Ptrain : Rd → R:

Ptrain (Cx,t ) = 1

|D| 

X

> φ∈D

P (Cx,t |φ) (41) We can then use Bayes rule to find the Bayesian probability over the training set given Cx,t Ptrain :

D × Rd → R.

Ptrain (φ|C x,t ) = P (Cx,t |φ)

Ptrain (Cx,t ) = P (Cx,t |φ)

P 

> φ′∈D

P (Cx,t |φ′) (42) Note that both Ptrain distributions are random variables dependent on the codewords D. We also define the test set probabilities, which are not random. 

Ptest (Cx,t ) = 

Z

dφ P 0(φ)P (Cx,t |φ) (43) 

Ptest (φ|C x,t ) = P (Cx,t |φ)

Ptest (Cx,t ) (44) We would like to understand the entropy of the Bayesian posterior on the training set: 

S[Ptrain (φ|C x,t )] = X

> φ∈D

−Ptrain (φ|C x,t ) ln Ptrain (φ|C x,t ) (45) 

= X

> φ∈D

− P (Cx,t |φ)

P 

> φ′∈D

P (Cx,t |φ′) ln P (Cx,t |φ)

P 

> φ′∈D

P (Cx,t |φ′) (46) 

= ln 

 X 

> φ∈D

P (Cx,t |φ)



− 1

P 

> φ′∈D

P (Cx,t |φ)

X

> φ∈D

P (Cx,t |φ) ln P (Cx,t |φ) (47) 

= ln |D| + ln 

 P 

> φ∈D

P (Cx,t |φ)

|D| 



(48) 

− 1

P 

> φ∈D

P (Cx,t |φ)/|D| 

1

|D| 

X

> φ∈D

P (Cx,t |φ) ln P (Cx,t |φ) (49) 22 All the terms in this sum are independent random variables. As the dataset becomes large, the central limit theorem lets us replace the empirical means with their actual means up to a O( 1√|D| )

corrections, so long as their means are non-zero. This shows that 

1

|D| 

X

> φ∈D

P (Cx,t |φ) → Eφ∼P0 [P (Cx,t |φ)] (50) 

=

Z

dφP 0(φ)P (Cx,t |φ) = Ptest (Cx,t ) (51) 

− 1

|D| 

X

> φ∈D

P (Cx,t |φ) ln P (Cx,t |φ) → Eφ∼P0 [−P (Cx,t |φ) ln P (Cx,t |φ)] (52) 

= −

Z

dφP 0(φ)P (Cx,t |φ) ln P (Cx,t |φ) (53) 

= −

Z

dφP test (Cx,t , φ )(ln Ptest (Cx,t ) + ln P (Cx,t , φ )

P0(φ)Ptest (Cx,t ) ) (54) 

= Ptest (Cx,t )



− ln Ptest (Cx,t ) −

Z

dφP test (φ|C x,t )(ln Ptest (φ|C x,t )

P0(φ) )



(55) 

= Ptest (Cx,t )



− ln Ptest (Cx,t ) − DKL (Ptest (φ|C x,t )|| P0(φ)) 



(56) Plugging equations 51 and 56 into equation 48-49 we see, 

S[Ptrain (φ|C x,t )] = ln |D| + ln Ptest (Cx,t ) (57) 

+ 1

Ptest (Cx,t ) Ptest (Cx,t )



− ln Ptest (Cx,t ) − DKL (Ptest (φ|C x,t )|| P0(φ)) 



(58) 

+ O( 1

p|D| ) (59) 

= ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) + O( 1

p|D| ) (60) When either average above becomes smaller than O( 1√D ) the fluctuations might dominate over the mean. At the same time, we know that entropy must be strictly non-negative and should decrease as the information increases, so we can guess: 

S[Ptrain (φ|C x,t )] = max(ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) , 0) (61) Because the central limit theorem no longer applies, it is difficult to estimate the error in this formula. As it turns out, O(1 /√D) is too optimistic in the memorizing phase. In the section below, we use the random energy model to prove that this formula becomes exact in the large |D| limit with O( ln ln |D|   

> ln |D|

)

finite size corrections. If we average this result over Cx,t drawn from Ptest .

ECx,t ∼Ptest [S[Ptrain (φ|C x,t )]] = max(ln |D| − I(φ; Cx,t ), 0) (62) 

D.5 A proof from the Random Energy Model 

In this section, we give several proofs based on methods from statistical mechanics, inspired by recent approaches in the literature of diffusion models as in [6, 43]. We start by defining an energy function E and inverse temperature β = 1 /T so that Ptrain corresponds to a Boltzmann distribution: 

−βE (φ|C x,t ) = ln P (Cx,t |φ) (63) 

Ptrain (φ|C x,t ) = e−βE (φ|C x,t )

P 

> φ′∈D

e−βE (φ′|C x,t ) (64) Therefore, the energy E(φ|C x,t ) is simply the temperature T times the minus log likelihood of making the channel observation Cx,t at time t under the forward diffusion process starting from a 23 data point φ at time t = 0 . If we fix the channel observation Cx,t , we can think of E(φ|C x,t ) in (63) as an energy function on the training data φ ∈ D , with a high (low) energy assigned to φ indicating a low (high) likelihood that φ was the origin of the channel observation Cx,t in the forward diffusion process. After normalization, the Boltzmann distribution in (64) is simply the posterior probability that a particular training data point φ ∈ D at time t = 0 was the origin of the conditioned channel observation Cx,t in the forward diffusion process. 

Theorem D.1 (Independent Pointwise Posterior Entropy) If D is a set of training images drawn i.i.d. from P0 and Cx,t is a channel such that P (E|C x,t ) has a rate function for large E, with a continuous first derivative. We also assume that both the rate function and its derivative have a finite number of maxima and minima at E = O(1) .

S[Ptrain (φ|C x,t )] = 

ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) ln |D| > D KL (Ptest (φ|C x,t )|| P0(φ)) 0 ln |D| ≤ DKL (Ptest (φ|C x,t )|| P0(φ)) 

(65) 

up to O( ln ln |D|   

> ln |D|

) corrections. 

Proof We will begin by considering the case where Cx,t is drawn from the test set. This means 

Cx,t , as a random channel observation, is statistically independent of the random training data D.If Cx,t were drawn from the training set, then the dataset sample that generates Cx,t would not be identically distributed and would break the i.i.d assumption in hypothesis of this theorem. We will first fix Cx,t to be an arbitrary value, then we will average over the distribution of Cx,t . Now since the data points φ ∈ D are drawn i.i.d. from P0(φ), the energies E(φ|C x,t ) of each data point φ

can be thought of as independent random variables. Therefore, the Boltzmann distribution in (64) is equivalent to an independent random energy model (REM) as first described by [44] (see also [45] for a nice pedagogical treatment). The REM is solvable by a saddle point approximation which becomes exact in a “thermodynamic” limit, which in our case, corresponds to a large amount of training data, or large |D| . We define 

P (E′|C x,t ) = 

Z

dφ δ (E′ − E(φ|C x,t )) P0(φ) (66) This is simply the distribution of energies E′ we expect to see conditioned on a fixed channel obser-vation Cx,t where the randomness comes from the distribution of a single training point P0(φ). In the REM, the log number of states, ln |D| , is the extensive parameter, originally corresponding to the number of spins. For ML readers not familiar with the statistical mechanics language, the extensive parameter, ln |D| , is used to group together different values based on their relative size to it. intensive ⇌ O(1) (67) extensive ⇌ O(ln |D| ) (68) exponentially large ⇌ O(eα ln |D| ) = O(|D| α) (69) exponentially small ⇌ O(e−α ln |D| ) = O( 1

|D| α ) (70) In some strict sense, this means that we are imagining that all of the variables in the problem have some dependence on ln |D| which allows us to take the limit ln |D| → ∞ , called the thermodynamic limit. To make this analysis quite general, we will avoid explicit expressions for this dependence on ln |D| and only express asymptotic scaling. We would like to define this limit so there are sensible answers for the entropy of the posterior and relevant quantities at polynomial orders in 

ln |D| . By that we mean that we are interested in finding asymptotic series as ln |D| → ∞ like 

E[O] = PNn=−∞ On(ln |D| )n. In this context, quantities which are exponentially small do not show up in any order of these asymptotic series since they decay faster than any rational function. Therefore, exponentially small quantities can simply be set to 0. For the REM to have a sensible thermodynamic limit, we require that the energy is extensive. This is equivalent to the existence of 24 a function I called the rate function defined in the following way: 

ϵ(φ|C x,t ) = E(φ|C x,t )ln |D| (71) 

I(ϵ|C x,t ) := 

− ln P (ϵ ln |D| Cx,t )ln |D| = Ω(1) (72) 

P (ϵ|C x,t ) = e− ln |D| I(ϵ|C x,t ) (73) 

ϵ is called the intensive energy because we removed the ln |D| scaling of the energy by dividing. We also assume that the inverse temperature is intensive. The existence of the rate function implies that there is sensible probability distribution for the intensive energy. If we unwrap the asymptotic expression in 72, we find 

− ln P (ϵ ln |D| Cx,t )ln |D| ≥ C1 (74) 

ln P (ϵ ln |D| Cx,t ) ≤ − C1 ln |D| (75) 

P (ϵ ln |D| Cx,t ) ≤ C2e−C1 ln |D| (76) 

⇐ P (E|C x,t ) ≤ C2e−C1E−o(1) . (77) We’ll now look at the ”density of states” which measures how many energies sit inside of a window around ϵ.

ρ(ϵ|C x,t ) = 1

W

X

> φ∈D

1ϵ(φ|C x,t )∈[ϵ,ϵ +W ] (78) where 1E(φ|C x,t )∈[ϵ,ϵ +W ] is the indicator function 

1E(φ|C x,t )∈[ϵ,ϵ +W ] =

1 ϵ(φ|C x,t ) ∈ [ϵ, ϵ + W ]0 ϵ(φ|C x,t ) /∈ [ϵ, ϵ + W ] (79) Saddle-point approximation leads to an expected density of states 

E[ρ(ϵ|C x,t )] = E[ X

> φ∈D

1E(φ|C x,t )∈[ϵ,ϵ +W ]]/W = |D| Eφ∼P0 [1E(φ|C x,t )∈[ϵ,ϵ +W ]]/W (80) 

=

Z ϵ+Wϵ

dϵ W eln |D| 



> 1−I(ϵ|C x,t )



(81) 

=

eln |D| (1 −I(ϵ|C x,t ))+ o(1) 1 > I (ϵ|C x,t )0 + O(e− ln |D| ) 1 ≤ I(ϵ|C x,t ) (82) Since 1E(φ|C x,t )∈[ϵ,ϵ +W ] is its own square, the variance of ρ is equal to its expectation which implies the standard deviation is O(p|D| ) where the leading order term is O(|D| ). Therefore, the leading order term of the density of states is deterministic 

ρ(ϵ|C x,t ) = 

eln |D| (1 −I(ϵ|C x,t )) + O(eln |D|/2) 1 > I (ϵ|C x,t )0 + O(e− ln |D| ) 1 ≤ I(ϵ|C x,t ) (83) The micro-canonical entropy S(ϵ) is just the log density of states. It is deterministic to all orders in 

ln |D| .

S(ϵ|C x,t ) = ln ρ(ϵ|C x,t ) (84) 

=

(

ln 



eln |D| (1 −I(ϵ|C x,t )) + O(eln |D|/2)



1 > I (ϵ|C x,t )ln  0 + O(e− ln |D| ) 1 ≤ I(ϵ|C x,t ) (85) 

=

ln |D| (1 − I(ϵ|C x,t )) + ln  1 + O(e− ln |D|/2) 1 > I (ϵ|C x,t )

−∞ + O(e− ln |D| ) 1 ≤ I(ϵ|C x,t ) (86) 

=

ln |D| (1 − I(ϵ|C x,t )) + O(e− ln |D| /2) 1 > I (ϵ|C x,t )

−∞ + O(e− ln |D| ) 1 ≤ I(ϵ|C x,t ) (87) 25 Between equation 85 and equation 86, we multiplied and divided by the deterministic contribution to the density of states and then used the logarithm to separate the deterministic contribution into its own term. From equation (87), we see that the entropy is deterministic up to exponentially small corrections. We now define the annealed micro-canonical entropy density sann,M C (ϵ|C x,t ) and the micro-canonical entropy density s(ϵ|C x,t ) as follows: 

sann,M C (ϵ|C x,t ) := ln E[ρ(ϵ|C x,t )] ln |D| = 1 − I(ϵ|C x,t ) (88) 

s(ϵ|C x,t ) := S(ϵ|C x,t )ln |D| =

sann,M C (ϵ|C x,t ) sann,M C (ϵ|C x,t ) > 0

−∞ sann,M C (ϵ|C x,t ) ≤ 0 (89) Our goal is to calculate the entropy of the Gibbs distribution which is the posterior over training images. This requires we switch from the micro-canonical ensemble perspective to the canonical ensemble perspective. We can define the standard thermodynamic functions for the canonical en-semble: The Partition Function ⇌ Z(β|C x,t ) := X

> φ∈D

e−βE (φ|C x,t )

Gibbs Distribution ⇌ Pβ (φ|C x,t ) := e−βE (φ|C x,t )

Z

The Free Energy ⇌ F (β|C x,t ) := ln Z(β|C x,t )

−β

The Expected Energy ⇌ E(β|C x,t ) := X

> φ∈D

E(φ|C x,t )Pβ (φ|C x,t )

The Canonical Entropy ⇌ S(β|C x,t ) = X

> φ

−Pβ (φ|C x,t ) ln Pβ (φ|C x,t )= X

> φ

− e−βE (φ|C x,t )

Z (− ln Z − βE (φ|C x,t )) = ln Z + β X

> φ

E(φ|C x,t )Pβ (φ|C x,t )= −βF (β|C x,t ) + βE (β|C x,t )

In models with disorder, like the random energy model, you often look at the disorder averaged or ”annealed” partition function. We can define the annealed versions of the functions in the canonical ensemble. Since I’ve proven that some of the above functions are deterministic, I can replace them with their annealed averages where appropriate. Annealed Partition Function ⇌ Zann (β|C x,t ) := ED [ X

> φ∈D

e−βE (φ|C x,t )]

(Canonical) Annealed Free Energy ⇌ Fann (β|C x,t ) := ln Zann (β|C x,t )

−β

(Canonical) Annealed Expected Energy ⇌ Eann (β|C x,t ) := 1

Zann 

ED [ X

> φ∈D

E(φ|C x,t )e−βE (φ|C x,t )]

(Canonical) Annealed Canonical Entropy ⇌ sann,M C (β|C x,t ) = −βF ann (β|C x,t ) + βE ann (β|C x,t )

The free energy, expected energy, and canonical entropy are extensive so its natural to define the intensive densities for each. The Free Energy Density ⇌ f (β|C x,t ) := F (β|C x,t )ln |D| (90) Expected Energy Density ⇌ ϵ(β|C x,t ) := E(β|C x,t )ln |D| (91) The Canonical Entropy Density ⇌ s(β|C x,t ) := S(β|C x,t )ln |D| (92) 26 Because this model has an exact Saddle point in the thermodynamic limit, the canonical ensemble functions will just be Legendre transforms of their micro-canonical counter parts, but the regions where density of states drop to zero constrain the energy. Similar to the micro-canonical entropy, they will be deterministic to all orders in ln |D| . We can see this by using saddle point approximation to find the partition function. 

Z(β|C x,t ) = X

> φ∈D

e−βE (φ|C x,t ) =

Z

> R

dϵ e − ln |D| βϵ ρ(ϵ|C x,t )=

Z 

> sann,M C (ϵ|C x,t )>0

dϵ e ln |D| (sann,M C (ϵ|C x,t )−βϵ ) + O(eln |D| /2)= exp 



ln |D| 



max ϵ s.t. s ann,M C (ϵ)>0 sann,M C (ϵ|C x,t ) − βϵ 



+ O(ln ln |D| )



The O(ln ln |D|) error term above accounts for the normalization of the Gaussian fluctuations around the saddle point. Expanding up to second order in ϵ′ − ϵ(β) leads to a Gaussian with variance σ2 = O((ln |D|)−1). The normalization of a Gaussian integral is (2 πσ 2)−1/2 =exp( − ln σ + O(1)) = exp( O(ln ln |D| )) , which produces this term. If ϵ(β) lies at a bound-ary of integration, the lowest order fluctuation is exponential and the normalization still scales as 

O((ln |D| )−1) = exp( O(ln ln |D| )) , so the same logic applies. Now, using this result to find the free energy density: 

−βf (β|C x,t ) = max ϵ s.t. s ann,M C (ϵ)>0 sann,M C (ϵ|C x,t ) − βϵ + O( ln ln |D| 

ln |D| ). (93) We can similarly solve for the energy density 

ϵ(β|C x,t ) = 1

Z

Z

> R

dϵ ρ (ϵ|C x,t )ϵe −β ln |D| ϵ

= 1

Z

Z 

> sann,M C (ϵ)>0

ϵe ln |D| (sann,M C (ϵ|C x,t )−βϵ )

= argmax sann,M C (ϵ)>0sann,M C (ϵ|C x,t ) − βϵ + O( ln ln |D| 

ln |D| )

For the rest of the proof, I will omit the O(ln ln |D|/ ln |D|) term. It should be assumed that all canonical ensemble functions have corrections of that order. We make one last transformation from micro-canonical to canonical transformation for annealed thermodynamic functions. As always in thermodynamics, saddle point approximation shows that the annealed canonical functions concen-trate to the annealed micro-canonical functions in the large ln |D| limit. I will elide some details since this is the third similar calculation. Note that ϵ is now allowed to range over the entire real line instead of only where sann,M C (ϵ) > 0.

Zann (β|C x,t ) := ED [ X

> φ∈D

e−βE (φ|C x,t )] = |D| Eφ[e−βE (φ|C x,t )]= |D| 

Z

dϵe ln |D| (sann,M C (ϵ|C x,t )−βϵ ) = exp(ln |D| max ϵ sann,M C (ϵ|C x,t ) − βϵ )=⇒ fann (β|C x,t ) = ln Zann (β|C x,t )

−β ln |D| → −1

β max ϵ sann,M C (ϵ|C x,t ) − βϵ 

=⇒ ϵann (β|C x,t ) = argmax ϵsann,M C (ϵ|C x,t ) − βϵ 

=⇒ sann (β|C x,t ) = −βf ann (β|C x,t ) + βϵ ann (β|C x,t ) = sann,M C (ϵann (β)|C x,t )

Note the difference between the annealed and actual (quenched) energy in the thermodynamic limit of the canonical ensemble 

ϵann (β) = argmax ϵ sann,M C (ϵ|C x,t ) − βϵ ϵ(β) = argmax sann,M C (ϵ)>0 sann,M C (ϵ|C x,t ) − βϵ 

27 If ϵann (β) = ϵ(β), then sann (ϵann (β)) = sann (ϵ(β)) ≥ 0. On the other hand, if ϵann (β)̸ = ϵ(β)

then we can conclude sann (ϵann (β)) < 0 since no ϵ with sann (ϵ) ≥ 0 was able to achieve this min-imum. We now must apply more of our hypothesizes to determine what happens to sann,M C (ϵ(β)) 

when actual (quenched) and annealed energies disagree. By assumption, I(ϵ) and I′(ϵ) have a finite number of local maxima and minima that must lie be-tween some Ea and Eb. This means all local minima and maxima of sann,M C (ϵ) and s′ 

> ann,M C

(ϵ)

must lie between Ea  

> ln |D|

and Eb  

> ln |D|

. Outside this sub-extensive ( O( 1ln |D| )) region, the deriva-tive of entropy has a constant sign. Since P (ln |D| ϵ) = e− ln |D| I(ϵ) and normalization implies 

lim E→−∞ P (E) = 0 , lim ϵ→−∞ I(ϵ) = ∞. Therefore, I′(ϵ) must be negative and s′ 

> ann,M C

(ϵ)

must be positive below Ea  

> ln |D|

. By similar logic, we conclude that s′ 

> ann,M C

(ϵ) is negative above   

> Eb
> ln |D|

. Since β > 0 and ϵ = O(1) , we conclude that ϵann (β) < Ea  

> ln |D|

and ϵ(β) < Ea  

> ln |D|

for all beta. Since the derivative is monotonic in this region, there can only be one solution to the equation  

> ∂s ann,M C
> ∂ϵ

= β which ϵann must satify. Therefore, if ϵ(β)̸ = ϵann (β), then ∂s ann,M C  

> ∂ϵ

(ϵ(β)) ̸ = β.This implies ϵ(β) cannot lie at a local maximum of the optimization problem and therefore must lie on the boundary where sann,M C (ϵ(β)) = 0 . So, we see 

ϵ(β)̸ = ϵann (β) = ⇒ sann,M C (ϵ(β)) = 0 

We can combine these results with equation 89 to get the entropy. 

s(β|C x,t ) = 

sann,M C (ϵann (β|C x,t )|C x,t ) sann,M C (ϵann (β|C x,t )|C x,t ) > 00 sann,M C (ϵann (β|C x,t )|C x,t ) ≤ 0 (94) So, we can see this thermodynamic model has two different phases. One where the Gibbs distribu-tion is spread over many states and sann,M C (ϵann (β)|C x,t ) > 0 and one where all the probability concentrates on a single state and sann,M C (ϵann (β)|C x,t ) ≤ 0. These correspond exactly to the generalizing and the memorizing phase described in the main text. Note that simply calculating 

sann,M C (ϵ(β)|C x,t ) for a given temperature and Cx,t is enough to determine which phase the model is in. Work previously done by [6] explicitly solved for s(ϵ) and ϵ(β) in the case of isotropic Gaus-sian data. However, stepping back from these saddle-point equations and examining the context of the problem allows us to work in more generality. One can generically write the canonical annealed canonical entropy in terms of the temperature using the annealed free energy and annealed energy 

Sann (β|C x,t ) = −βF ann (β) + βE ann (β) = ln E[Z|C x,t ] + β E[EZ |C x,t ]

E[Z|C x,t ] (95) 

= ln |D| + ln EE [e−βE |C x,t ] + β EE [Ee −βE |C x,t ]

EE [e−βE |C x,t ] (96) Usually, evaluating this expression would require knowing the full moment generating function for the E distribution, which would be as difficult as solving for s(ϵ). However, we are not interested in the behavior of our model at arbitrary temperatures. Recall that equation 63 defines a specific temperature for the model. 

EE [e−βE |C x,t ] = 

Z

dφ P 0(φ)e−βE (φ|C x,t ) (97) 

=

Z

dφ P 0(φ)P (Cx,t |φ) = Ptest (Cx,t ) (98) So, we see that at the temperature given by 63, the moment generating function of the energy distri-bution is just the test set probability of Cx,t . Similarly, for the energy expectation, we find 

βEE [Ee −βE |C x,t ]

EE [e−βE |C x,t ] =

Z

dφ P0(φ)

Ptest (Cx,t ) βE (φ|C x,t )e−βE (φ|C x,t ) (99) 

=

Z

dφ P0(φ)

Ptest (Cx,t ) P (Cx,t |φ) ( − ln P (Cx,t |φ)) (100) 

=

Z

dφ P test (φ|C x,t ) ( − ln P (Cx,t |φ)) (101) 28 Combining everything, we find 

ln |D| sann,M C (ϵann (β)|C x,t ) = Sann (β|C x,t )= ln |D| + ln Ptest (Cx,t ) + 

Z

dφ P test (φ|C x,t ) ( − ln P (Cx,t |φ)) = ln |D| +

Z

dφ P test (φ|C x,t ) ( − ln Ptest (φ|C x,t )

P0(φ) )= ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) 

Plugging this into the formula for the actual (quenched) entropy, 

S(β|C x,t ) = max 



0, ln |D| − DKL (Ptest (φ|C x,t )|| P0(φ)) 



(102) From this, we can conclude the pointwise collapse condition. □

This pointwise condition is useful for situations where Cx,t is not drawn from a known distribution. So, long as it is drawn independently from D, we can calculate the posterior entropy. If Cx,t is drawn from the test set, the expression further simplifies: 

Theorem D.2 (Test Set Averaged Posterior Entropy) If D is a set of training images drawn i.i.d. from P0 and C is a channel such that P (E|C x,t ) has sub-exponential tails and pV ar (DKL (Ptest (φ|C x,t )|| P0(φ))) = o(1) , then when a noisy image is drawn from the test set 

ECx,t ∼Ptest [S[Ptrain (φ|C x,t )]] = 

ln |D| − I(φ; Cx,t ) ln |D| > I (φ; Cx,t )0 ln |D| ≤ I(φ; Cx,t ) (103) 

up to o(1) corrections. 

Proof Since the annealed entropy is assumed to have sub extensive standard deviation, we can replace it with its mean: 

ECx,t ∼Ptest [Sann (φ|C x,t )] = ln |D| − ECx,t ∼Ptest [DKL (Ptest (φ|C x,t )|| P0(φ))] (104) 

= ln |D| +

Z

dφd Cx,t Ptest (Cx,t )P (φ|C x,t ) ( − ln Ptest (φ|C x,t )

P0(φ) ) (105) 

= ln |D| +

Z

dφd Cx,t Ptest (φ, Cx,t ) ( − ln Ptest (φ, Cx,t )

Ptest (Cx,t )P0(φ) ) (106) 

= ln |D| − I(φ; Cx,t ) (107) 

□

Theorem D.3 (Training Set Posterior Entropy) If D/φ 0 is a set of training images drawn i.i.d. from P0 and C is a channel such that P (E|C x,t ) satisfies the conditions in theorem D.1, 

pV ar (DKL (Ptest (φ|C x,t )|| P0(φ))) = o(1) , and V ar (ln Ptest (Cx,t ,φ ) 

> Ptest (Cx,t )P0(φ)

) = o(1) , then when a noisy image is drawn from the train set 

ECx,t ∼Ptrain [S[Ptrain (φ|C x,t )]] = 

ln |D| − I(φ; Cx,t ) ln |D| > I (φ; Cx,t )0 ln |D| ≤ I(φ; Cx,t ) (108) 

up to o(1) corrections. 

Proof If Cx,t is not drawn independently from D, for example if Cx,t is generated from the training set, there is less that we can say. This violates the i.i.d. assumption imposed on the D training set in theorem D.1. A training image φ0 ∈ D is first chosen uniformly and then Cx,t is drawn from 

P (Cx,t |φ). This leads to additional contribution to the annealed partition function 

Ztrain = P (Cx,t |φ0) + X

> φ∈D /{φ0}

P (Cx,t |φ) (109) 29 Each term in the second sum is identically distributed and their contribution from the second term is identical to the test partition function with |D| − 1 samples. However, this P (Cx,t |φ0) term must be specifically accounted for. In the error correction context, this quantity is usually taken to be much smaller than ln |D| and therefore ignored. If we don’t ignore it, it can lead to the collapse transition happening faster. 

Ztrain = P (Cx,t |φ0) + max(( |D| − 1) Ptest (Cx,t ), e − ln |D| βϵ ∗

) (110) (111) where ϵ∗ is the intensive energy such that I(ϵ∗|C x,t ) = 1 . We must assume that ln P (Cx,t |φ0)/ ln |D| 

concentrates to some deterministic O(1) value when φ0 is drawn from P0 and Cx,t from P (·| ϕ0).Because the two terms are exponentially small in ln |D| , Ztrain is always only dominated by one of the two terms. If ln P (Cx,t |φ0)   

> βln |D|

> ϵ ∗, then there are exponentially many other states around the energy ln P (Cx,t |φ0)   

> βln |D|

and Ztrain = Ztest + O(1) we conclude the theorem by applying theorem D.2. If ln P (Cx,t |φ0)   

> βln |D|

< ϵ ∗ then, 

−βF (β) = ln Z =

ln( |D| − 1) + ln Ptest (Cx,t ) ln( |D| − 1) + ln Ptest (Cx,t ) > ln P (Cx,t |φ0)ln P (Cx,t |φ0) ln( |D| − 1) + ln Ptest (Cx,t ) ≤ ln P (Cx,t |φ0)

In the collapsed phase, we know the energy associated with the training sample that generated Cx,t 

is ln P (Cx,t |φ0)   

> βln |D|

. In the generalizing phase, we can use the conclusion of theorem D.1 since Ztrain =

Ztest + O(e− ln |D|).

S(β) = 

ln( |D| − 1) − DKL (Ptest (φ|C x,t )|| P0(φ)) ln( |D| − 1) + ln Ptest (Cx,t ) > ln P (Cx,t |φ0)0 ln( |D| − 1) + ln Ptest (Cx,t ) ≤ ln P (Cx,t |φ0)

Now using Bayes rule P (Cx,t |φ0) = Ptest (Cx,t ,φ 0)

> P0(φ0)

S[Ptrain (φ|C x,t )] = 

(ln( |D| − 1) − DKL (Ptest (φ|C x,t )|| P0(φ)) ln( |D| − 1) > ln Ptest (Cx,t ,φ 0)

> Ptest (Cx,t )P0(φ0)

0 ln( |D| − 1) ≤ ln Ptest (Cx,t ,φ 0)

> Ptest (Cx,t )P0(φ0)

Now we note that ln Ptest (Cx,t ,φ 0) 

> Ptest (Cx,t )P0(φ)

is the observable averaged in the expression for the mutual information. 

I(Cx,t , φ 0) = 

Z

dφd Cx,t Ptest (Cx,t , φ 0) ln Ptest (Cx,t , φ 0)

Ptest (Cx,t )P0(φ) (112) Therefore, if we assume that ln Ptest (Cx,t ,φ 0) 

> Ptest (Cx,t )P0(φ0)

also concentrates, then 

S[Ptrain (φ|C x,t )] = 

ln( |D| − 1) − DKL (Ptest (φ|C x,t )|| P0(φ)) ln( |D| − 1) > I (Cx,t ; φ)0 ln( |D| − 1) ≤ I(Cx,t ; φ)

(113) So, we see that the posterior entropy for a noisy training image is essentially the same when appro-priate strong assumptions are made about P (Cx,t |φ). □

D.6 Relationship to Biroli et al.’s Collapse Condition 

In [6] the memorization to generalization phase transition for the empirical score function was stud-ied using the random energy model. Our work with the random energy model in section D.5 was inspired by theirs. The empirical score function is, in some sense, the simplest BIRD model in which all pixels see the entire noisy image. The Markov chain for the observation is just a white noise channel 

φ → ϕ := √¯αtφ + √1 − ¯αtη (114) They were able to solve for the entropy of the generated distribution and memorization time in the large dimension limit exactly in the case of isotropic Gaussian data by directly calculating the micro-canonical entropy. They conjectured that for the memorization time t∗, the identity 

S[P (ϕt∗ )] = S|D|  

> sep

(115) 30 would hold for data φ from arbitrary distributions, where S|D|  

> sep

is the entropy of |D| well-separated isotropic Gaussians with variance (1 − ¯αt)I:

S|D|  

> sep

= ln |D| + S[N (η|0, (1 − ¯αt)I)] = ln |D| + d

2 (1 + ln(2 π(1 − ¯αt))) (116) Using the mutual information formula for the white noise channel with this parametrization (theorem E.1) 

ln |D| = S[P (ϕt∗ )] − S[N (η|0, (1 − ¯αt)I)] = I(ϕt∗ ; φ) (117) which corresponds to the collapse condition (5) for the trivial channel Cx,t (ϕt) = ϕt. Thus, our analysis proves Biroli’s conjecture using random energy model methods, but also significantly gen-eralizes their notions to cases where the channel Cx,t is potentially nonlinear or stochastic, for which their conjectured identity (115) no longer serves as the correct collapse condition. 

## E Useful Mutual Information Bounds and Calculations 

E.1 Memorization implies a lower bound on mutual information 

Here we will use Fano’s inequality to prove a simple lower bound on the mutual information 

I(Cx,t ; φ) whenever a BIRD model is in the memorization phase. Consider again the mutual information I(Cx,t ; φ) between a pixel x’s current observation Cx,t at time t and the past training image φ ∈ D at time t = 0 that led to the observation under the forward diffusion. In BIRD models, the pixel x attempts to play a reverse time Bayesian guessing game to guess the correct past training image φc ∈ D that led to Cx,t . Its knowledge about the past training data is encapsulated in the posterior distribution P (φ|C x,t ) for φ ∈ D . The probability the pixel guesses the correct training point φc is P (φc|C x,t ). Thus the probability a pixel x loses the Bayesian guessing game by making an incorrect guess or error is pe = 1 − P (φc|C x,t ). The pixel 

x has then memorized the training data, by definition, if pe is close to 0 and the posterior P (φ|C x,t )

concentrates on the correct data point φc.We can think of this process as an error correction problem where the |D| training samples φ ∈ D 

can be thought of as |D| codewords. One of these codewords φc is sent through the forward diffusion plus observation communication channel φ → ϕt → C x,t , yielding the channel output Cx,t . Then the pixel x must guess the correct input codeword φc based on this output. Memorization then corresponds to successful error correction in which pe → 0 (which is deleterious though for BIRD models as it leads to the failure to generalize). With this error correction analogy in place, we can now apply Fano’s inequality (see [46] for a standard proof) which provides a lower bound on the error probability pe in terms of the conditional entropy S(φ|C x,t ). Applying Fano’s inequality yields 

Hb(pe) + pe ln( |D| − 1) ≥ S(φ|C x,t )= S(φ) − I(Ct,x ; φ)= ln |D| − I(Ct,x ; φ).

Here Hb(pe) is the entropy of a binary random variable with probability parameter pe. We next take the memorization limit pe → 0. This makes the left hand side 0, yielding a lower bound on mutual information in the memorization phase: 

I(Ct,x ; φ) ≥ ln |D| . (118) Importantly, this analysis only proves that in the memorization phase, corresponding to pe → 0,we have I(Ct,x ; φ) ≥ ln |D| . It does not prove that in the generalizing phase, when pe is bounded away from 0, that the opposite holds: I(Ct,x ; φ) < ln |D| . One would ideally like to have this latter inequality also in the generalizing phase to prove that the boundary between the generalizing phase and memorizing phase occurs exactly when I(Ct,x ; φ) = ln |D| . Indeed, we are able to prove this result in App. D.5, when Cx,t is drawn from the testing Markov process. 

E.2 The Gaussian Bound on Mutual Information for the Additive White Noise Channel 

In the prior sections, we keep the description of Bayes optimal denoisers as generic as possible, but calculating bounds on the channel mutual information requires specificity on the type of channel that 31 we are considering. In the case of the ideal score function, the local score function, the equivariant local score function, the channels for each agent or pixel can be viewed as the composition of two channels. First, a non-invertible, possibly random linear map applied to a noiseless image, and second, rescaling and adding white noise. For example, in the case of the local score machine, 

Cx,t : φ → φΩx → √¯αtφΩx + √1 − ¯αtηΩx (119) where η is an isotropic Gaussian. Note that this is the opposite of the order which these two channels are applied in the actual computation. We actually apply a projection to the noisy image. However, because the white noise at each pixel is independent and the projection map is linear we can view the two as happening in either order. PΩx : RH×W ×C → Rk×k×C

Cx,t = PΩx (√¯αtφ + √1 − ¯αtη1) = √¯αtPΩx φ + √1 − ¯αtη2 (120) where η1 ∼ N(0 , I H×W ×C ) and η1 ∼ N(0 , I k×k×C ). By the data processing inequality, 

I(Cx,t ; φ) ≤ I(Cx,t ; PΩx φ) (121) In other words, because Cx,t only depends on φ through its dependence on PΩx φ, Cx,t cannot have more information about φ than was contained in PΩx φ. For ease of notation in the following sec-tions, I’ll define Φx as the noise free version of the observation Cx,t .

Φ := PΩx φ (122) Therefore, we can understand most Bayes optimal diffusion models just by understanding the mutual information of the additive white noise channel. Let Φ be some random variable inserted into an additive white noise channel. 

I(Cx,t := √¯αtΦ + √1 − ¯αtη; Φ) = S(Cx,t ) + S(Φ) − S(Cx,t , Φ) (123) 

= S(Cx,t ) − S(Cx,t |Φ) (124) 

= S(Cx,t ) − S(√¯αtΦ + √1 − ¯αtη|Φ) (125) After conditioning on Φ, √¯αtΦ is just a deterministic shift and η is independent. So, this simplifies to: 

I(Cx,t ; Φ) = S(Cx,t ) − S(√1 − ¯αtη) (126) 

= S(Cx,t ) + 

Z

dη N (η|0, (1 − ¯αt)I) ln N (η|0, (1 − ¯αt)I) (127) 

= S(Cx,t ) −

Z

dη 

 1 − ¯αt

2 || η|| 2 + d

2 ln(2 π(1 − ¯αt)) 

 exp  − 1− ¯αt 

> 2

|| η|| 2p(2 π(1 − ¯αt)) d (128) 

= S(Cx,t ) − d

2 (1 + ln(2 π(1 − ¯αt))) (129) This recovers the well known theorem describing the Mutual information between the input and output of an additive Gaussian white noise channel: 

Theorem E.1 For the additive white noise channel with input Φ and output Cx,t := √¯αtΦ + √1 − ¯αtη with η ∼ N (0 , I ),

I(Cx,t ; Φ) = S(Cx,t ) − d

2 (1 + ln(2 π(1 − ¯αt))) (130) 

where d is the data dimension or the number of pixels. 

As we can see, the mutual information of the additive white noise channel only depends on the input, or data distribution through the entropy of the noisy distribution ϕ. Therefore, we can bound the mutual information of the channel by choosing a maximal entropy distribution for ϕ. Under the constraint that the data covariance matrix is Σ0 and its distribution is centered, the maximal entropy 32 distribution of ϕ is Gaussian with covariance Σt = ¯ αtΣ + (1 − ¯αt)I. Let ˜ϕ ∼ N (0 , Σt) be the Gaussianized version of the data. 

S( ˜ϕ) = −

Z

d ˜ϕN ( ˜ϕ|0, Σt) ln N ( ˜ϕ|0, Σt)= 12 ln det(2 πΣt) + 

Z

d ˜ϕ ˜ϕT Σ−1 

> t

ϕ

2exp 



− ˜ϕT Σ−1 

> t

ϕ/ 2

pdet(2 πΣt)= 12 Tr ln(2 πΣt) + 12 Tr [Σ −1 

> t

Σt]= d

2 + 12 Tr ln(2 πΣt)

Now using this to find the mutual information 

I( ˜ϕ; ˜Φ) = S( ˜ϕ) − d

2 (1 + ln(2 π(1 − ¯αt))) (131) 

= 12 Tr ln 

 Σt

1 − ¯αt



(132) 

= 12 Tr ln 

 ¯αtΣ + (1 − ¯αt)I

1 − ¯αt



(133) 

= 12 Tr ln 



I + ¯αt

1 − ¯αt

Σ



(134) Therefore, we can conclude theorem 

Theorem E.2 Given a patch Φ with covariance Σ the mutual information of the additive white noise channel, ϕ := √¯αtΦ + √1 − ¯αtη, is bounded above by 

I(ϕ; Φ) ≤ 12 Tr ln[ I + ¯αt

1 − ¯αt

Σ] . (135) 

E.3 Asymptotic Expansion for the Mutual Information of the AWNC at High Noise 

In this section we will again consider a simple white noise channel. In section E.2, we argued that the Gaussianized distribution provides an upper bound on the mutual information. Here, we provide a universal expansion for the mutual information of the white noise channel regardless of the underlying distribution. This helps give intuition for the surprisingly good agreement between the Gaussian prediction and the posterior entropies measured on real data sets. Recall for the additive white noise channel: 

I(φ; ϕ) = S[Ptest (ϕ)] − S[N (0 , (1 − ¯αt)I)] . (136) We can define the cumulant generating function for the test distribution as follows: 

Kϕ( ˆϕ) = ln Eϕ∼Ptest [exp 

 ˆϕ · ϕ



] (137) The formal Taylor expansion for this function provides the cumulants of the distribution of ϕ. In addition, the cumulant generating function with an imaginary argument can be used to entirely re-construct the distribution. Because the cumulant generating function of the sum of two independent random variables is just the sum of each variable’s cumulant generating function, we have 

Kϕ( ˆϕ) = ln Eφ∼P0,η ∼N (0 ,I )[exp 

 ˆϕ · (√¯αtφ + √1 − ¯αtη)



] (138) 

= ln Eφ∼P0 [exp 

 ˆϕ · (√¯αtφ)



] + ln Eη∼N (0 ,I )[exp 

√1 − αt ˆϕ · η



)] (139) 

= Kφ(√¯αt ˆϕ) + Kη (√1 − ¯αt ˆϕ) (140) 

= Kφ(√¯αt ˆϕ) + 1 − ¯αt

2 || ˆϕ|| 2. (141) The cumulant generating function of the isotropic Gaussian is just a quadratic. In addition, √¯αt

ranges from 0 to 1, so one would expect that the low order terms in √¯αt describe the behavior at 33 most time steps. This means that only the lowest order cumulants of the data distribution have a significant effect on the cumulants of the generated distribution. By simply representing the entropy in terms of the cumulant generating function, we should be able to expand the expression to lowest order terms and find a cumulant expansion for the mutual information. We start with the expression for the distributio in terms of its CGF: 

Ptest (ϕ) = 

Z d ˆϕ

(2 π)d exp 



−i ˆϕ · ϕ + Kϕ(i ˆϕ)



. (142) Taking derivatives with respect to ϕ has the effect of adding factors of −i ˆϕ to the integrand in equation (142). 

Ptest (ϕ) = 

Z d ˆϕ

(2 π)d exp 



−i ˆϕ · ϕ + Kϕ(i ˆϕ)



(143) 

=

Z d ˆϕ

(2 π)d exp 



Kφ(i√¯αt ˆϕ)



exp 



−i ˆϕ · ϕ − 1 − ¯αt

2 || ˆϕ|| 2



(144) 

= exp  Kφ(−√¯αt∂ϕ) Z d ˆϕ

(2 π)d exp 



−i ˆϕ · ϕ − 1 − ¯αt

2 || ˆϕ|| 2



(145) 

= exp  Kφ(−√¯αt∂ϕ) exp 



− || ϕ|| 2 

> 2(1 −¯αt)

pdet(2 πΣt) (146) In the last line, 

S[Ptest (ϕ)] = −

Z

dϕ P test (ϕ) ln Ptest (ϕ) (147) 

= −

Z

dϕ exp 

 ˜Kφ(−√¯αt∂ϕ)



N (ϕ|0, (1 − ¯αt)I) (148) 

× ln exp 

 ˜Kφ(−√¯αt∂ϕ)



N (ϕ|0, (1 − ¯αt)I) (149) Expanding equation (149) in powers of ¯αt will provide an expansion of the entropy in terms of higher cumulants of the distribution and expectations of Gaussian random variables. For example, the first order term in ¯αt is 

S[Ptest (ϕ)] = d

2 (1 + ln(2 π(1 − ¯αt))) + 12

 ¯αt

1 − ¯αt



Tr [Σ] (150) 

− 14

 ¯αt

1 − ¯αt

2

(Tr [Σ 2] + κabcd 

12 (δa,b δc,d + δa,c δb,d + δa,d δb,c )) (151) 

+ O(¯ α5/2 

> t

) (152) where Σ is the covariance matrix of the dataset and κabcd is the four-cumulant of the data. We can convert this into a prediction for the mutual information of the white noise channel 

I(ϕ; φ) = S[Ptest (ϕ)] − S[N (0 , (1 − ¯αt)I)] (153) 

= 12

 ¯αt

1 − ¯αt



Tr [Σ] (154) 

− 14

 ¯αt

1 − ¯αt

2

(Tr [Σ 2] + κabcd 

12 (δa,b δc,d + δa,c δb,d + δa,d δb,c )) (155) 

+ O(¯ α5/2 

> t

) (156) Higher order terms of this expansion are quite tedious to calculate. 

E.4 A Data Model for Speciation with Exactly Solvable Mutual Information 

Imagine that the true image distribution is a mixture of Gaussians where the parameters are them-selves random. 

P0(φ) = 1

|M |

X

> μ∈M

N (φ|μ, Σ0) (157) 34 where the means M are drawn from some other distribution N (0 , ΣM ). If we assume that |M |

is large, we can exactly solve for the mutual information of this data passed through the white noise channel. This can be achieved by noting that P0(φ) itself is exactly the same as the training distribution created by M samples from N (0 , ΣM ) then passed through an additive noise channel 

μ + η with η ∼ N (φ|μ, Σ0). However, the theory that we’ve built to this point would only allow you to predict the entropy of the posterior distribution, that is given φ what is the probability that it came from the Gaussian centered at μ. However, we can instead make use of our prediction for the partition function 

Z(φ) = X

> m∈M

N (φ|μ, Σ0) = 

|M |N (φ|0, Σ0 + Σ M ) ln |M | > D KL (P (μ|φ)|P (μ)) 

e−βE ∗

ln |M | ≤ DKL (P (μ|φ)|P (μ)) (158) where e−βE ∗

does not concentrate, but behaves like M well separated Gaussian distributions. This predicts the probability density at φ in the large |M | limit. Leading to the counterintuitive result that 

P0(φ) concentrates to a value independent of M . This leads to 

S(φ) = 

S[N (μ, Σ0 + Σ M )] ln |M | > I (μ; φ)ln |M | + S[N (0 , Σ0)] ln |M | ≤ I(μ; φ) (159) Adding in the white noise channel, this becomes 

S(ϕt) = 

S[N (μ, Σ0 + Σ M + σ2 

> t

I)] ln |M | > I (μ; φ)ln |M | + S[N (0 , Σ0 + σ2 

> t

I)] ln |M | ≤ I(μ; φ) (160) So, the mutual information is 

I(ϕt; φ) = 

S[N (μ, Σ0 + Σ M + σ2 

> t

I)] − S[N (0 , σ 2 

> t

I)] ln |M | > I (μ; φ)ln |M | + S[N (0 , Σ0 + σ2 

> t

I)] − S[N (0 , σ 2 

> t

I)] ln |M | ≤ I(μ; φ) (161) Plugging in the Gaussian entropy formula 

I(ϕt; φ) = 



T r [ln 



I + 1

> σ2
> t

(Σ 0 + Σ M )



] ln |M | > I (μ; φ)ln |M | + T r [ln 



I + 1

> σ2
> t

Σ0



] ln |M | ≤ I(μ; φ) (162) This can also be written 

I(ϕt; φ) = min(ln |M | + T r [ln 



I + 1

σ2

> t

Σ0



], T r [ln 



I + 1

σ2

> t

(Σ 0 + Σ M )



]) (163) This leads to the formula for the posterior entropy 

S[P (φ|ϕ)] = max(0 , ln |D| |M | − T r 

h

ln 



I + 1

σ2

> t

Σ0

i

, ln |D| − T r 

h

ln 



I + 1

σ2

> t

(Σ 0 + Σ M )

i

)

(164) So, we see that the speciation transition has the exact same behavior as a memoriza-tion/generalization transition in terms of a discontinuity in the derivative of the posterior entropy with the noise level. You can see an example of this in sub-figure 3b where the posterior entropy seems to plateau twice. More generally, we could imagine a Hierarchical Gaussian model where a set of means M1 is drawn from N (0 , ΣM1 ) to generate a mixture of Gaussian distribution with covariance of each Gaussian ΣM2 from which I draw as set of means M2, and so on until the final set of means MN . In this case, we will have a posterior entropy like 

S[P (φ|ϕ)] = max(0 , max n≤N ln |D| 

Πni=1 |Mi| − T r [ln I + 1

σ2

> t

Σ0 + 1

σ2

> tN

X

> i=n+1

ΣMi

!

]) (165) where each change of the maximum n corresponds to a speciation transition. 

## F Generalization in Local Score Models and the critical scale 

As we have discussed above, Bayes-optimal diffusion models generalize only when the channel through which they are observing their image lets pass fewer than log |D| bits of information, pre-venting them from being able to resolve down to a single training set image. Increasing the noise 35 level clearly decreases this mutual information, and extensive prior work has gone into studying the location of the memorization/generalization transition as a function of this parameter [6, 47]. Another parameter directly relevant to the capacity of the channel, however, is the locality scale 

[13, 32, 48]. When diffusion models have locality biases, they may only effectively observe a small proportion of the overall image. Under such constraints, the effective amount of information that they may in principle use to deduce the underlying image will be substantially lower than the global information contained in the image. We thus ask the following question: where is the threshold of criticality, as a function of both the noise level and the locality scale ?In this section, for conciseness, we will use the invariant noise-to-signal ratio to characterize the noise level at a time t:

σ2 

> t

= 1 − ¯αt

¯αt

(166) We will consider a family of patches ΩL,x indexed uniquely by the parameter L, which satisfies 

L = p|ΩL,x |. For square patches [13] this quantity is simply the length of one side of the patch. We will demand that ΩL′,x ⊃ ΩL,x if L′ > L . The critical scale Lc(σt) is then defined as the scale where a Local Score model using the patch ΩL,x achieves the average collapse condition (40) at a noise level σt:

ln |D| = I(φ; ϕt, Ωx,L ) (167) Without making any assumptions about the data-generating distribution, we are in a position to make a very general statement. Firstly, increasing the scale of a patch given a fixed noise scale monoton-ically increases the mutual information I(φ; ϕt;Ω ), while increasing the noise scale at a fixed patch size monotonically decreases this mutual information. It follows from implicit differentiation of the collapse condition (167) with respect to L that the values of Lc(σt) must be monotonically increas-ing with noise level. This is phenomenologically consistent with the observed coarse-to-fine scaling of diffusion models [13, 14, 32, 40]. We can formalize this into this theorem. 

Theorem F.1 (Monotonicity of Length Scale) Lc(σt) is a monotonically increasing function. 

Proof If L < L ′, then ΩL′,x ⊃ ΩL,x . This implies that the observation CL,x,t can be viewed as first restricting to the patch ΩL′,x and then restricting to the patch ΩL,x . This leads to the Markov chain 

φ → ϕt → ϕt, Ωx,L ′ → ϕt, Ωx,L (168) The information processing inequality implies 

I(φ; ϕt, Ωx,L ) ≤ I(φ; ϕt, Ωx,L ′ ) (169) Now we need to use the fact that for a projection we can switch the order of restricting the domain and adding the noise. For a positive increment ∆tφ → φΩx,L → ϕt, Ωx,L → ϕt+∆ t, Ωx,L (170) This implies 

I(φ; ϕt+∆ t, Ωx ) ≤ I(φ; ϕt, Ωx,L ) (171) The collapse condition (40) then implies the mutual information is held constant at the transition size ϕt, Ωx,L (σt) . Since σt is monotonically increasing in t, this implies 

|Ωx,L c(σt)| ≤ | Ωx,L c(σt+∆ σ)| (172) 

Lc(σt) ≤ Lc(σt + ∆ σt) (173) 

□

F.1 What scales do we need vs. what scales do we have : the spectral scale vs. the critical scale 

Existing theories of locality [32, 40] in diffusion models emphasize a quantity which we term the 

spectral scale . Naturalistic images have a well-known power-law falloff in their power spectral density [39]: 

P (k) ≈ A|k|−α (174) 36 with the power-law exponent α ≈ 2 − ϵ, with ϵ typically in the range 0.1-0.3. The α = 2 case is the exactly scale invariant case, where each logarithmic frequency interval has exactly the same total variance. The observed α < 2 implies that natural images are nearly scale-invariant but slightly ‘UV tilted,’ showing a bit more power at higher frequencies than at lower frequencies. When performing denoising diffusion, this power-law distribution is mixed with white noise, with a flat power spectral density S(k) = σ2 

> t

. There is a natural length scale in this problem, defined as the inverse of the spatial frequency where the noise power equals the signal power: 

Lspec =

 σ2

> t

A

1/α 

(175) For exactly scale-invariant data with α = 2 , this reduces to 

Lspec ∼ σt (176) i.e., the spectral scale is linearly proportional to the standard deviation of the added noise. Another useful way of interpreting the spectral scale is as the approximate radius of the optimal linear denoiser, which for stationary translationally-invariant data is known as the Wiener filter [41]. The optimal linear denoiser is the minimizer of the denoising objective (14) under the constraint that the model should be an affine function Mt[ϕt] = ˆWtϕt + bt. This is a standard linear regression problem, and for a distribution P0(φ) with mean μ and covariance Σ, the optimal ˆWt and bt are given by, respectively, 

ˆWt = (¯ αtΣ + (1 − ¯αt)I)−1(√¯αtΣ) (177) 

bt = ( I − ˆWt)√¯αt μ. (178) The rows of ˆWt can be interpreted as denoising filters applied at a particular location. When the data statistics are translationally-invariant, the matrix ˆWt implements a convolution with a single filter, the Wiener filter. Convolution operators correspond to multiplication in Fourier space; the action of 

ˆWt for such data is given by 

ˆWt(k) ˆϕt(k) = 

√¯αtΣ( k)¯αtΣ( k) + (1 − ¯αt) ˆϕt(k) (179) where Σ( k) = Var [ ˆ φ(k)] = P (k) is the variance of the kth Fourier mode of the unnoised images under P0(φ). This filter generally has infinite support, so to make sense of its ‘size’ one has to make a definitional choice; the most natural characteristic width is the point where the signal and noise power meet in the denominator of the filter, i.e. 

¯αtΣ( k) = (1 − ¯αt) (180) Plugging in the power-law expression (174) for P (k) = Σ( k) and rearranging yields the expression for the ‘fourier space radius’ |kspec |. Setting Lspec = |kspec |−1 yields the spectral scale (175). Because of the relatively compact support of this filter, it can be inferred that a strictly local de-noiser with a locality scale ≥ Lspec should perform well with a relatively small error. Indeed, [32] established that calibrating an LS model according to the radius of the Wiener filter yields models that perform capably even to moderately large image sizes. Conversely, we should expect that any reasonably capable denoiser should be able to attend at least to roughly the radius of this filter. If the critical scale is much smaller than the spectral scale ( Lspec ≫ Lc), a Bayes-optimal local denoiser 

cannot perform well: it will either incur catastrophic memorization by using a scale L ≫ Lspec 

comparable to Lspec , or its denoising estimate will be robust but highly inaccurate due to using a scale L < L c ≪ Lspec . Figure 4a illustrates both of these failure modes: when the scale the model uses is smaller than the spectral scale, the outputs of the denoiser are robust but suboptimal. Con-versely, when the scale the model uses is larger than the critical scale, the model’s output incurs catastrophic statistical error. Realistic data are highly non-Gaussian, with higher-order dependencies that exist and longer scales than implied by the two-point statistics. Indeed, [32] finds that localizing based on the spectral scale breaks down on the highly non-Gaussian dataset MNIST. Nonetheless, achieving a radius of at least 

the spectral scale is clearly necessary for any performant local denoiser, and appears to be sufficient for a reasonable baseline level of denoising accuracy. A natural question then arises: is the critical 

37 scale for a Bayes-optimal local denoiser large enough to meet or exceed the spectral scale ? While by no means obvious, we show in this section that, both theoretically and empirically, the critical scale typically does scale similarly to the spectral scale. This remarkable concordance between the scales needed for denoising and the scales at which robust denoising is possible (using a non-exponential dataset size) is at the heart of why local Bayes optimal models succeed at ‘reasonable’ generalization on high-dimensional image data. 

F.2 A concrete calculation: the isotropic Gaussian 

The simplest case to analyze is the isotropic Gaussian first analyzed by [6]. Given a patch Ω, the mutual information between the Ω-restricted image ϕt;Ω , and the underlying unnoised image φ is given by 

I(ϕt;Ω ; φ) = |Ω|

2 ln 



1 + s2

σ2

> t



(181) where s2 is the variance of φ, |Ω| is the number of pixels in the patch. The critical scale Lc =p|Ωmem | is then given by equating this quantity to the number of nats ln |D| needed to resolve down to a single training set image: 

ln |D| = L2

> c

2 ln 



1 + s2

σ2

> t



(182) which upon rearranging yields 

Lc =

vuut 2 ln |D| 

ln 



1 + s2

> σ2
> t

 (183) While presented in slightly different form, under the identification |Ω| = L2 

> c

= d this result repro-duces exactly the ‘collapse time’ result of [6]. At large σ2 

> t

, this quantity becomes 

Lc → σt

r 2 ln |D| 

s2 (184) i.e. we find that at high noise levels, the critical scale is directly proportional to the noise level. This asymptotic scaling is promising: it suggests that the requisite spectral scaling (176) may be achieved by a generalizing Bayes-optimal denoiser even for asymptotically large image sizes, without scaling the dataset. As we will see, the form derived above is very general across a wide range of images. 

F.3 Scaling bounds 

If we have a more specific model for our data distribution where I(ϕt;Ω ; φ) is tractable, we can in principle compute the location of the critical threshold exactly. This computation can also be performed numerically; we illustrate some such results in figure 4. However, doing so directly is often challenging, and extracting an analytic solution for Lc(σt) from the collapse condition (167) may not be feasible even when the mutual information I(ϕt;Ω ; φ) is available. In such cases, however, we can produce a number of useful bounds. The first is an immediate corollary of the additive noise bound: 

Corollary F.2 Assume an additive white noise channel with a locality constraint ϕΩ,t , and suppose 

φ is drawn from an arbitrary distribution P with mean μ and covariance Σ. We define the distribu-tion Q1 = N (μ, Σ) to be the Gaussian distribution with identical second-order statistics to P , and 

Q2 = N (μ, Diag [Σ]) to be the Gaussian distribution with identical diagonal covariance and zero off-diagonal elements. We then have 

Lc;P (σt) ≥ Lc;Q1 (σt) ≥ Lc;Q2 (σt) (185) 

Proof The Gaussian bound (thm. E.2) entails 

I(ϕt, Ω; φ ∼ P ) ≤ I(ϕt, Ω; φ ∼ Q1) (186) 38 We further have a closed formula for the mutual information of a Gaussian variable 

I(ϕt, Ω; φ ∼ Q1) = Tr 



ln 



I + Σ

σ2

> t

 

(187) Since T r ln( ·) is a strictly concave function and diag (Σ) ≺ Σ in the ordering of semi-definite matrices, T r [ln 



I + Σ

> σ2
> t



] ≤ T r [ln 



I + Diag (Σ) 

> σ2
> t



]

I(ϕt, Ω; φ ∼ Q1) ≤ I(ϕt, Ω; φ ∼ Q2) (188) From this it follows that, for L > L ′, I(ϕt, ΩL ; φ) ≥ I(ϕt, ΩL′ ; φ). Consequently, we find that at 

Lc;Q1 ,

ln |D| = I(ϕt, ΩLc ;Q1 ; φ ∼ Q1) ≥ I(ϕt, ΩLc ;P ; φ ∼ P ) (189) so we deduce that Lc;Q1 ≤ Lc;P . Applying the same argument for Q2 and Q1 yields Lc;Q2 ≤ Lc;Q1 .

□

These bounds alone are sufficient to answer our most basic question in the affirmative: for data with exactly scale-invariant power laws , the relationship (184) provides a lower bound on the critical scale. This implies that for any data distribution with translation invariance, bounded pixel-wise variance, and a scale-invariant power law |k|−2, we have Lc(σt) ≥ CL spec (σt) for arbitrarily large image sizes even with a fixed dataset size. We also find that these Gaussian bounds are very strong predictors of the critical scale: as shown in figure 4b, the patch mutual information and concomitant critical scales for even highly non-Gaussian datasets like MNIST tend to be well-predicted by their Gaussian bounds. Beyond these Gaussian bounds, a much more general scaling theorem is available, which covers a wide range of naturalistic images: 

Theorem F.3 Suppose we have a translationally-invariant field φ with bounded pointwise variance and asymptotically image-size-extensive entropy density lim |Ω|→∞ 1 

> |Ω|

S(φΩ) = ˜S. Then, for ln |D| 

which is O(1) with respect to σt,

Lc(σt) ∼ O σt

pln |D|  (190) 

Proof The inequality L(σt) ≥ C σ t

pln |D| follows immediately from corollary F.2. To show the reverse direction, we use the result 

I(ϕΩ; φ) = S(ϕΩ) − S(ϕΩ|φ) = S(ϕΩ) − S(σtηΩ) (191) where σtηΩ is the added noise in the patch Ω. We can produce a lower bound on the information 

S(ϕΩ) via the entropy power inequality, which states that for any variables X, Y ∈ Rd,

S(Y + X) ≥ d

2 ln 



e 2   

> dS(Y)

+ e 2 

> dS(X)



= d

2 ln 



1 + e 2  

> d[S(X)−S(Y)]



+ S(Y ) (192) Applying this with X = φΩ and Y = ση yields 

S(ϕΩ) − S(σtηΩ) ≥ |Ω|

2 ln 



1 + e 2 

> |Ω|S(φΩ)

2πeσ 2

> t



(193) Since σ2 

> t

is going off to infinity and L(σt) ≥ C σ t

pln |D| , |Ω| = L2 is going off to infinity as well. Therefore, we can replace S(φΩ)/|Ω| with its limit. 

I(ϕΩ; φ) ≥ |Ω|

2 ln 



1 + e2 ˜S+o(1) 

2πeσ 2

> t



(194) Applying this to the collapse condition (40) we find that 

ln |D| ≥ L2 

> c

(σt)2

e2 ˜S

2πeσ 2

> t

+ o( 1

σ2

> t

) (195) This is in the form of the information condition scaling for an isotropic Gaussian with pointwise variance e2 ˜S 

> 2πe

, which asymptotically as σt → ∞ yields the result 

Lc(σt) + o( 1

σ2

> t

) ≤ 2σt e− ˜S pπe ln |D| (196) This completes the proof. □

39 F.4 Deviations from scale-invariance 

For power laws α > 2, the spectral scale (175) increases sublinearly with σt. The Gaussian bounds show that Lc ≥ Cσ t, which as σt → ∞ shows that Lc asymptotically dominates over the spectral scale and thus is asymptotically learnable with a Bayes-optimal local denoiser. This case, while instructive, is less phenomenologically interesting for natural images. For power laws α < 2,the bulk entropy scales extensively, so the conditions for theorem 5.2 are met and we find Lc ∼

σt

pln |D| . The spectral scale Lspec ∼ σ2/α t thus grows faster with σt than the critical scale at fixed 

ln |D| . This implies that to match the spectral scale up to arbitrarily large image sizes, we need to scale the dataset size according to 

ln |D| ∼ σ 2ϵ 

> 2−ϵ
> t

∼ Lϵspec (197) While this scaling is superpolynomial with regards to image size (and therefore experiences a mild form of the curse of dimensionality), it is significantly subexponential, i.e. it significantly improves over the naive |D| ∼ const d scaling characteristic of the empirical score function. The right hand side in practice exhibits a very weak dependence– e.g., for ϵ = 0 .1, taking Lspec = 32 to L = 1024 

results in Lϵspec ∼ 1.4 to Lϵspec = 2 . The exact impact of this scaling on the required |D| will depend on the constants of proportionality, which are in general problem-dependent. In our experiments, we see little evidence of this ϵ-dependent growth of the spectral scale relative to the critical scale, although such growth may become more pronounced for very large-scale images. 

F.5 Empirical analysis of the Posterior Entropy on Real and Toy Datasets 

In figure 3, we show comparisons between the real entropy of the posterior distribution and the theoretical prediction for two toy models of data and two real datasets. We take 5 × 5 image patches from 32 × 32 images and calculate the posterior distribution according to (24) and cal-culate − P 

> φ∈D

P (φ|ϕΩx,L =5 ) ln P (φ|ϕΩx,L =5 ) by summing over all training samples as the noise strength σ2 

> t

is varied between 10 −3 and 10 1.5. The entropy is then calculated with dataset sizes 

28 = 256 , 210 = 1024 , 212 = 4096 , and 214 = 16,384. Each dataset size is plotted in its own color. In sub-figure (a), training data samples were drawn from a Gaussian distribution with pixel covari-ance ⟨φ(x)φ(y)⟩ = 1 

> |x−y|2−α

for x̸ = y with ⟨φ(x)2⟩ = 1 . The distance between neighboring pixels is set to 1. The theory curve uses the expression for the mutual information of Gaussian variables (134). In sub-figure (b), a mixture of 16 Gaussians was sampled in the fashion described in section E.4. Section E.4 also describes the formula for the theory curve. All covariances were taken to be isotropic with the variance of the means equal to 1 and a variance of the Gaussians equal to 0.15. In sub-figures (c) and (d), data samples were drawn uniformly from the datasets CIFAR10 and CelebA and then downscaled to 32 × 32 color images. The theory curves were calculated by plugging the empirical patch covariance matrix into the equation for the Gaussian mutual information (134). 

F.6 Empirical analysis of the critical scale and spectral scale 

To test the qualitative validity of our theoretical analyses, we ran a number of experiments on real-istic data distributions. Firstly, we wanted to directly test the validity of the collapse condition (40). To do so, we first took the Gaussianized version of an exemplar dataset (in our case BWCeleba32), defined as the Gaussian distribution whose mean and covariance match the first and second order statistics of the dataset. The reason for this choice is that it furnishes a dataset with naturalistic statis-tics, for which the underlying patch mutual information I(ϕΩL,t ; φ) can be computed analytically using the formula (134). We first computed the theoretical mutual information for a large range of length scales L and noise parameters σt; the results are plotted in figure 5 as the blue ‘Theoretical’ surface. As expected, these results grow without bound as the noise level decreases/as the length scale increases. We then sampled a dataset D of 10 4 samples from this Gaussianized prior, and eval-uated the mutual information of the noised patch relative to the discrete prior induced by this dataset; the result of this evaluation is plotted in figure 5 as the orange ‘Empirical’ surface. We found, as expected, that the empirical finite-dataset result closely matches the theoretical infinite-dataset result when the latter is below the critical threshold (illustrated by the grey surface in fig. 5). When the latter exceeds the critical threshold, the empirical surface ‘peels off,’ never reaching its theoretical upper bound of ln |D| .40 Figure 5: A plot of the mutual information I(ϕΩ; φ) on a Gaussianized version of the BWCeleba32 dataset, as a function of both the noise level σ2 and the patch length L, relative to the prior for a) the theoretical distribution b) a finite sample of 10 4 examples, as a function of noise level and patch scale. Plotted as well is the critical threshold I = ln |D| .To test the concordance between the critical scale and the spectral scale, we compute numerically the location of a sub critical threshold, defined by ln |D|− ln d∗ = I(ϕΩL ; φ). Intuitively, this defines the necessary amount of information to resolve down to an ‘effective sample size’ of d∗ from a dataset of size |D| . Because this threshold is below the critical threshold, we expect the empirical estimator for I(ϕΩL ; φ) to be robust and match the theoretical infinite-data mutual information with a high degree of accuracy, as demonstrated in the plot (5) in the case where the exact mutual information is known analytically. We take d∗ = 10 for our experiments. We compute the subcritical threshold curve by sweeping the empirical mutual information over σt and L and computing numerically the intersection of this surface with the threshold surface. We also compute the Gaussian bound analytically over the same σt, L domain, using the patch covariance statistics. We find that the Gaussian bound is usually a strong predictor of the (sub)critical scale. Finally, for each dataset, we extract the curve of the spectral scale Lspec (σt) as a function of the noise level. To do this, we identify at each σt the Fourier band |k∗(σt)| wherein the average power spectral density for a mode in that band equals the added noise power σ2 

> t

, and take Lspec (σt) = |k∗(σt)|−1. We generally find that this curve generally tracks the critical scale remarkably well, validating our thesis that there is a concordance between the spectral and critical scales on realistic datasets. Finally, to illustrate the impact of the scale on the performance of the Bayes-optimal local denoiser, we took two images, one from the denoiser’s training set and an unrelated image from the test set, and applied noise to each at a noise level of σt = 1 . We then applied an optimal local denoiser to each image at length scales L. At small scales ( L = 5 ), the Bayes-optimal local denoiser is robust, performing comparably on the training and test set, but produces poor quality images. At the test set-optimal scale, near but below the critical scale, ( L = 7 , boxed in green), the denoising estimate is both accurate and robust on both the training set and test set. Past this scale, statistical error begins to accumulate on the denoising estimate of test image, while the model’s performance on the training image increases. At very large scales ( L = 17 , 33 ), the LS model denoises the training image perfectly, while failing catastrophically on the test set image. This result is illustrated in panel 4a in figure 4. 41 G Why scaling the patch size with the spectral scale is necessary for a local denoiser 

Our work has emphasized the critical importance of using a local denoiser with a length scale that corresponds to the spectral scale. We can make the rationale for this somewhat more precise in the context of Gaussian power-law data. Roughly speaking, any filter family that achieves this scale will be able to reconstruct the power spectral density P (k) ∼ | k|−α of the target distribution; conversely, a filter family that scales according to a different law will produce data distributed according to a different power law spectral density. For a power-law Gaussian distribution with power spectral density P (k) = A|k|−α, the Wiener filter in Fourier space is given by 

ˆw(σ2, k ) = 11 + A−1σ2|k|α (198) This satisfies an interesting invariance condition, which we call the renormalization condition :

ˆw(βσ 2, k ) = ˆ w(σ2, β 1/α k) (199) In real space, this corresponds to the condition 

w(βσ 2, x ) = β−2/α w(σ2, β −1/α x) (200) The content of this statement is that the family of Wiener filters is scale-equivariant: the filter used for a given noise level σ2 is simply a rescaled and dilated version of the filter at any other given noise level. The Wiener filter also satisfies two other important properties: it is normalized (i.e. it integrates to 1), and it goes to 0 in Fourier space as k → ∞ (spectral falloff ). The normalization property in particular is important for a consistent denoiser: it entails in particular that as the noise level goes to 0, the estimate of the image produced by the filter converges to the true image, as opposed to a rescaled version of it. In our paper, we have focused on the question of strictly local denoising , where the filter mass is constrained to lie entirely within a particular noise-dependent radius, which we expect to be proportional to the spectral scale. We might wonder what sort of distributions the resulting optimal localized filters generate. It turns out that the three properties of renormalization , normalization ,and falloff , on their own are enough to guarantee that a diffusion model, parameterized by a linear denoiser with any filter shape ˆm(σ2, k ), will obtain the correct spectral power law ∼ | k|−α (with a uniform error, relative to the optimal filter, on the global amplitude A). Conversely, if the filter family satisfies the renormalization property with a renormalization condition that is for a different 

power value α′̸ = α (e.g. if it is scale-equivariant but localized to a radius that is not proportional to the spectral scale), this filter family will generate a power law with an incorrect exponent. This result is captured below: 

Theorem G.1 Suppose we parameterize a diffusion model with a linear denoising model 

Mt[ϕt]( x) = m(σ2 

> t

, x ) ⋆ ϕ t(x) (201) 

where m(σ2, x ) is a filter whose Fourier transform (with respect to x) satisfies the renormalization condition 

ˆm(βσ 2, k ) = ˆ m(σ2, β 1/α k) (202) 

the normalization condition Z

m(σ2, x ) dx = 1 (203) 

and two falloff conditions for some p, q > 0.

lim  

> |k|→∞

|k|p ˆm(σ2, k ) = 0 lim  

> |k|→ 0

|k|−q ( ˆ m(σ2, k ) − 1) = 0 

These falloff conditions can also be written in terms of σ2 for some p′, q ′ > 0.

lim 

> σ2→∞

(σ)2p′

ˆm(σ2, k ) = 0 lim 

> σ2→0

(σ)−2q′

( ˆ m(σ2, k ) − 1) = 0 

Then the diffusion model will generate a Gaussian distribution with a power spectral density 

P (k) ∼ | k|−α (204) 42 Proof For the purposes of this proof, we will use the ‘variance-exploding’ parameterization: 

ϕt = ϕ0 + σtη (205) where σt → ∞ as t → ∞ . An analogous proof should be available using other parameterizations, such as the variance-preserving parameterization. The probability flow equation for this parameter-ization is 

ddt ϕt = 12 (∂t log σ2 

> t

)( ϕt − Mt[ϕt]) (206) which, for the linear denoiser (201), reduces to 

ddt ϕt = 12 (∂t log σ2 

> t

)



1 − (m(σ2 

> t

, x ) ⋆)



ϕt (207) The linear operator (1 − m(σ2 

> t

, x ) ⋆)) is diagonalized in the Fourier basis, so we can solve (207) by solving the evolution of each Fourier component ˆϕt(k) independently. This yields 

∂t ˆϕt(k) = 12 (∂t log σ2 

> t

)(1 − ˆm(σ2, k )) ˆϕt(k) (208) Integrating this equation gives 

ˆϕT (k) = ˆϕ0(k) exp 12

Z T

> 0

(∂t log σ2 

> t

)(1 − ˆm(σ2 

> t

, k )) dt 

!

(209) Or, reparameterizing with the substitution u = log σ2 

> t

,

ˆϕT (k) = ˆϕ0(k) exp 12

Z log σ2

> T
> 0



1 − ˆm(u, k )



du 

!

(210) In variance-exploding flows, the samples ϕ0 are generated by taking samples ϕT ∼ σT η and flowing these samples backwards. We will consider the limit where T → ∞ . Substituting this into the equation (210) and rearranging, we find that the variance of ˆϕ0(k) (i.e. the power spectral density 

P (k)) satisfies 

P (k) = lim   

> T→∞

σ2 

> T

exp −

Z log σ2

> T
> −∞



1 − ˆm(u, k )



du 

!

(211) or in logarithmic terms 

log P (k) = lim  

> T→∞



log σ2 

> T

−

Z log σ2

> T
> −∞

(1 − ˆm(eu, k )) du 



(212) 

=

Z ∞

> 0

ˆm(eu, k )du +

Z 0

> −∞

( ˆ m(eu, k ) − 1) du (213) One can easily verify that the falloff conditions imply both of these integrals converge by the power test. To show the expected PSD (204) holds for this distribution, we can differentiate this quantity with respect to |k|. The dominated convergence theorem allows us to swap integration and differen-tiation to obtain 

∂log |k| log P (k) = 

Z ∞−∞ 

∂log |k| ˆm(eu, k ) du (214) The renormalization condition (202) implies that we can interchange the derivative of the filter with respect to |k| with a derivative with respect to u:

∂log |k| ˆm(σ2, k ) = α ∂ log σ2 ˆm(σ2, k ) (215) Substituting this into (214) and evaluating the integral with respect to u, we obtain 

∂log |k| log P (k) = α[ ˆ m(σ2 = ∞, k ) − ˆm(σ2 = 0 , k )] (216) 43 The renormalization condition implies that ˆm(σ2 = 0 , k ) = ˆ m(σ2, k = 0) ; applying the normaliza-tion condition (203) yields ˆm(σ2 = 0 , k ) = 1 . Meanwhile, the renormalization condition implies the asymptotic falloff of ˆm(σ2 

> T

, k ) as σ2 

> T

→ ∞ coincides with the limit lim |k|→∞ ˆm(σ2, k ) = 0 .Thus (216) evaluates to 

∂log |k| log P (k) = α(0 − 1) = −α (217) which proves the claim. □

To complete our analysis, we need to prove that, for a power-law Gaussian, the conditional expecta-tion E[φ|ϕΩL(σ2),x ], with L(σ2) ∝ Lspec (σ2), satisfies the three properties. This is indeed the case, as we show in the following theorem. 

Theorem G.2 For φ an image drawn from a power-law Gaussian with PSD |k|−α, the model 

Mt[ϕt] = E[φ|ϕΩL(σ2),x ] = m(σ2, x ) ⋆ ϕ , with L(σ2) ∝ σ2/α , defines a filter family m(σ2, x )

that satisfies the normalization and falloff conditions, as well as the renormalization condition with parameter α.

Proof The optimal linear denoising model given a patch Ω for its center pixel x is given by 

(Σ Ω,x ) · (Σ Ω,Ω + σ2 

> t

I)−1ϕt (218) where ΣΩΩ is the block covariance (under the underlying distribution P0(φ) for the pixels within Ω,and ΣΩ,x is the row of this block covariance associated with the covariance between each pixel and the center pixel. Letting C(x) ∝ | x|α−2 be the two-point correlation function of the field (whose Fourier transform is the power spectral density P (k)), the operator ΣΩΩ + σ2 

> t

I can be understood as the convolution operator with the kernel 

1(y ∈ Ω) C(y) + σ2 

> t

δ(y) (219) while ΣΩ,x corresponds to the function C(y − x). In Fourier space, the resulting filter ˆm(σ2, k ) is given by 

ˆm(σ2, k ) = P (k) ⋆ ˆ1Ω

P (k) ⋆ ˆ1Ω + σ2

> t

= 11 + σ2 

> t

[P (k) ⋆ ˆ1Ω]( k)−1 (220) where ˆ1Ω is the Fourier transform of the indicator function 1(y ∈ Ω) , so that P (k)⋆ˆ1Ω is the Fourier transform of C1Ω. We will now assume that ΩL(σ2) is a patch whose geometry scales according to the noise level with the length scale L(σ2) = σ2/α t . This entails that the indicator function satisfies 

1ΩL(βσ 2) (y) = 1ΩL(σ2) (β−1/α y) (221) or, in Fourier space, 

ˆ1ΩL(βσ 2) (k) = β2/α ˆ1ΩL(σ2) (β1/α k) (222) From this, we observe 

[ ˆP ⋆ ˆ1ΩL(σ2) ]( β1/α k) = 

Z

ˆP (β1/α k − v)ˆ1ΩL(σ2) (v) dv 

= β2/α 

Z

P (β1/α (k − u)) ˆ1ΩL(σ2) (β1/α u) du 

Using (222) and the property P (β1/α k) = P (k)β−1, this becomes 

[ ˆP ⋆ ˆ1ΩL(σ2) ]( β1/α k) = β−1[ ˆP ⋆ ˆ1Ω( βL (σ2)) ]( k) (223) From this we find that 

ˆm(βσ 2, k ) = 



1 + (βσ 2)[ ˆP ⋆ ˆ1Ω( βL (σ2)) ]( k)

−1

=



1 + (βσ 2)

β[ ˆP ⋆ ˆ1Ω( L(σ2)) ]( β1/α k)

−1

(224) 

=



1 + σ2

[ ˆP ⋆ ˆ1Ω( L(σ2)) ]( β1/α k)

−1

= ˆ m(σ2, β 1/α k) (225) 44 Figure 6: Plots of the Median r2 metric (left) and the MSE metric (right) averaged over comparisons between several independently trained DiTs (top two panels) and UNets (bottom two panels) and their corresponding analytical models, across four datasets Celeba64, FashionMNIST, CIFAR10, and MNIST. Each model is trained on a reduced dataset of 10 4 samples. The Median r2 metric starts high across all datasets and gradually decreases over the course of training, while the MSE generally starts low and then gradually increases over the course of training. On some datasets these metrics are more level than others, or even decreasing as in MSE on FashionMNIST; however, the generally decreasing trend is mostly consistent. So ˆm satisfies the renormalization condition with parameter α. Since lim σ2→0 m(σ2, k ) = 1 and 

lim σ2→∞ m(σ2, k ) = 0 , it follows from the renormalization conditions that the falloff and normal-ization conditions are satisfied. □

Our results do not establish the result that E[φ|ϕΩL(σ2),x ], with L(σ2) parameterized by an incorrect 

power law ∼ σ 2 

> α′

with α′̸ = α, will generate an incorrect power law in the power spectral density, because the restricted statistics are not appropriately self-similar with noise, and therefore this esti-mator does not define a scale-equivariant family of filters satisfying the renormalization condition. We generally expect, however that if the length scale is significantly less than the spectral scale, that this family will nonetheless generate a power law that is asymptotically incorrect at large |k|.

## H Empirical Results in Early Training 

H.1 Methods 

In all experiments, we use a cosine noise schedule and a uniformly discretized reverse process. Our neural networks diffusion models employ a 100-step reverse process. Due to the high computational cost of the analytical denoisers, we employ a 20-step reverse process for each of our analytical diffusion models. We used an H100 GPU node with 80GB RAM for all experiments. To calibrate the locality scale in our experiments, rather than calibrating based on agreement with the neural network [13, 14] or using an absolute threshold on the Wiener filter [32], we simply select a validation set ( ∼ 2000 images) from the training set, and select a patch scale at each time step 45 Table 1: Quantitative agreement between DiTs trained on a subset of size 10 4 and corresponding (LS/ELS) analytical models given the same dataset subset. Median r2 values and MSE values are computed using a fixed subset of 100 samples and averaged over 4 model trials. Error bars are given by 2σ/ √n for n = 400 the sample size and σ the sample standard deviation for the given statistic. Selected metrics are taken from 30 epochs and 400 epochs respectively; the full trajectory of the median r2 and MSE losses are shown in plots 6. Dataset Med. r2 (30 eps.) MSE (30 eps.) Med. r2 (400 eps.) MSE (400 eps.) Celeba64 0.905 ( ±0.003) 0.064 ( ±0.003) 0.869 ( ±0.005) 0.076 ( ±0.004) 

CIFAR10 0.906 ( ±0.005) 0.036 ( ±0.002) 0.881 ( ±0.007) 0.046 ( ±0.002) 

MNIST 0.899 ( ±0.007) 0.066 ( ±0.003) 0.812 ( ±0.009) 0.113 ( ±0.005) 

FashionMNIST 0.935 ( ±0.011) 0.076 ( ±0.008) 0.918 ( ±0.009) 0.068 ( ±0.005) 

Table 2: Quantitative agreement between UNets trained on a subset of size 10 4 and corresponding (LS/ELS) analytical models given the same dataset subset. Median r2 values and MSE values are computed using a fixed subset of 100 samples and averaged over 4 model trials. Error bars are given by 2σ/ √n for n = 400 the sample size and σ the sample standard deviation for the given statistic. Selected metrics are taken from 30 epochs and 400 epochs respectively; the full trajectory of the median r2 and MSE losses are shown in plots 6. Dataset Med. r2 (30 eps.) MSE (30 eps.) Med. r2 (400 eps.) MSE (400 eps.) Celeba64 0.893 ( ±0.005) 0.069 ( ±0.004) 0.866 ( ±0.005) 0.078 ( ±0.004) 

CIFAR10 0.901 ( ±0.005) 0.039 ( ±0.002) 0.870 ( ±0.007) 0.048 ( ±0.002) 

MNIST 0.847 ( ±0.011) 0.104 ( ±0.006) 0.800 ( ±0.011) 0.122 ( ±0.005) 

FashionMNIST 0.929 ( ±0.012) 0.082 ( ±0.009) 0.916 ( ±0.011) 0.077 ( ±0.006) 

of denoising based on the best validation loss for the analytical denoising model employing the remainder of the dataset. This naturally selects patch sizes around the criticality threshold, which is the largest scale (and therefore most informative) before the onset of catastrophic statistical error (fig. 4a). For MNIST, FashionMNIST, and Celeba64, we use an LS model with patch geometries chosen using the Wiener filter binarization of [32]. However, our procedure for picking the patch length differs from that used in this paper because we are only selecting the family of patches given by Ωk representing the mask of top-k coefficients of the Wiener filter; however, rather than setting 

k using the coefficients of the Wiener filter, we calibrate it (independently for each pixel) using the cross validation procedure described above. We start our calibration at the lowest noise level, and incrementally increase the patch size k in increments of max(4 , ⌊0.05 k⌋) pixels until the validation loss at that pixel location fails to decrease. We make the assumption based on our theoretical analysis that the optimal scale should be nondecreasing with noise level, and thus initialize the scale at the next noise level at the optimal scale from the previous iteration. For CIFAR10, we find instead that the better analytical model at early training is the ELS rather than LS. We use the boundary-broken ELS prescription given in [13] with a square patch geometry, and calibrate the scale of this patch using the same procedure described above. For the ELS, however, due to computational constraints, we only use one global scale, rather than a pixel-specific scale. For our neural network experiments, we use a DiT with a patch size of 4 × 4, hidden dimension of 512, 8 attention heads, and a 4-to-1 MLP ratio per block, with 8 transformer blocks, as well as an input and output convolutional stem. We use a UNet with four scales, with channel dimensions (128, 256, 256, 512) at each scale, and use an attention layer with 8 heads at the 16x16 image resolution layers. We train the models using AdamW with betas (0.9,0.95) and weight decay of 5e-2. We use an initial learning rate of 2e-4, with cosine annealing. We train for 400 epochs on dataset subsets of size 10 4 with a batch size of 64 for the DiTs and 32 for the UNets. One of the difficult features of early-training diffusion models is that, at standard (large) learning rates, their outputs have a propensity to oscillate. The global hues of generated images are par-ticularly strongly affected by this oscillation, which can significantly damage the appearance of early-training consistency if not managed. This instability phenomenon has been noted elsewhere [49–51], and a number of techniques have been proposed to mitigate this effect. To get consistent results in early training, we find best results are obtained combining the following two techniques: 46 • Weight averaging: exponential moving average weighting of past checkpoints is a com-monly employed technique in diffusion modeling to improve checkpoint performance. We use a slightly simpler approach of linearly weight averaging the checkpoints taken from the end of each of the 5 previous epochs. We find that this helps average out fluctuations and stabilize the model’s output significantly. • v-prediction: this technique was designed [52] to keep the target variance consistent at all noise levels. We find in our work that this choice significantly stabilizes early training outputs, relative to other standard parameterizations ( ˆϵ- and ˆx0-prediction). Following the literature on analytical diffusion models [13, 14, 32], we use pixel-space metrics (median pixelwise r2 and MSE) to characterize the distance between our neural and analytical model outputs. While the quantitative signals we use show the effects of increased training in the form of slightly worse scores, the effect is somewhat less evident in the statistics than the qualitative perceptual effect would suggest. We do not propose a new metric here, but feel that there is room to develop metrics that capture the more perceptually and phenomenologically relevant dimensions of model training. In our table 1 and our plots 6 we report the average of these metrics taken across four iterations and both subsets D1 and D2 that we are training with. 

H.2 Samples 

We find empirically that the rate of training is somewhat different depending on the architecture and dataset, with maximal similarity to the outputs of the analytical models not always occurring at the same point (see fig 6 for the evolution of the quantitative metrics). In the samples below, we highlight samples taken from the epoch showing approximately the point of maximum agreement between theory and experiment for each diffusion model/dataset; examples of the evolution of the samples further in training are shown in fig 11. For each sample figure including 2, the samples for the epochs displayed are: • MNIST: UNet, 10; DiT, 15. • FashionMNIST: UNet, 10; DiT, 15. • CIFAR10: UNet, 10; DiT, 30. • Celeba64: UNet, 10; DiT, 30. These epochs are on the reduced datasets of size 10 4 used in training, so the equivalent number of epochs on a larger dataset for peak agreement would likely be smaller. 47 D2 D1D2 D1D2 D1

Figure 7: Further samples comparing DiTs and UNets early in training, trained on two disjoint subsets of 10 4 samples from Celeba64, and the outputs of calibrated local score models. UNet samples are from 10 epochs of training, DiT samples are from 30 epochs of training. Samples not curated for quality. 

> D2 D1D2 D1D2 D1

Figure 8: Further samples comparing DiTs and UNets early in training, trained on two disjoint subsets of 10 4 samples from CIFAR10, and the outputs of calibrated local score models. UNet samples are from 10 epochs of training, DiT samples are from 30 epochs of training. Samples not curated for quality. 48 D2 D1D2 D1D2 D1

Figure 9: Further samples comparing DiTs and UNets early in training, trained on two disjoint subsets of 10 4 samples from FashionMNIST, and the outputs of calibrated local score models. UNet samples are from 10 epochs of training, DiT samples are from 15 epochs of training. Samples not curated for quality. 

> D2 D1D2 D1D2 D1

Figure 10: Further samples comparing DiTs and UNets early in training, trained on two disjoint subsets of 10 4 samples from MNIST, and the outputs of calibrated local score models. UNet samples are from 10 epochs of training, DiT samples are from 15 epochs of training. Samples not curated for quality. 49 D2 D1D2 D1

Figure 11: As training progresses, models evolve from ‘patch mosaic’ style outputs [13] towards se-mantically coherent, nonlocally consistent generations, which nevertheless remain robust to dataset shifts. Leftmost columns are analytical, middle columns are DiT after 30 epochs on a 10 4 sample subset, and rightmost is after 300 epochs on a 10 4 sample subset. 50
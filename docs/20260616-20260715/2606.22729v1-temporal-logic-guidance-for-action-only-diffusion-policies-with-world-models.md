---
title: Temporal Logic Guidance for Action-Only Diffusion Policies with World Models
title_zh: 基于世界模型的仅动作扩散策略的时间逻辑引导
authors: "Moritz Zoellner, Anastasios Manganaris, Rohan Paleja"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22729v1"
tags: ["query:world-model"]
score: 9.0
evidence: 将扩散策略与学习的世界模型结合，实现时序逻辑引导的动作生成
tldr: "现有动作扩散策略推断时难以选择行为模式，而基于时序逻辑的指引方法需联合生成动作和状态，复杂且耗时。本文提出一种结合世界模型的新方法，仅用动作扩散策略，通过可微鲁棒性梯度引导生成过程。在搬运任务上，成功率达100%且约束违规仅4%，低于基线的80%以上。该方法无需重训练，显著提升了约束遵循性，为受控多模态行为提供了高效解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22729v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22729v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 631, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22729v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 161, \"label\": \"Table\"}]"
motivation: 动作扩散策略在推断时缺乏行为模式选择能力，现有时序逻辑指引方法需联合生成状态，增加复杂度和运行时间。
method: 提出结合世界模型的动作扩散策略引导方法，用世界模型计算STL鲁棒性梯度并注入扩散过程，实现可微约束引导。
result: "在Robomimic搬运任务中，任务成功保持100%，约束违规从基线超80%降至4%。"
conclusion: 方法无需重训练即可有效引导动作扩散策略满足时序逻辑约束，兼顾任务成功和约束遵循，并有望扩展至复杂约束。
---

## 摘要
扩散策略能够实现多模态机器人行为，但在推理时选择行为模式的能力有限，尽管在人机交互环境中这种控制是可取的。针对这种控制缺失，之前的解决方案利用信号时序逻辑（STL）来表达人类意图，并为扩散策略推理提供相应的引导。然而，这些方法只能引导同时生成未来动作和状态的扩散策略，这增加了复杂性和运行时间。我们提出了一种新的引导方法，用于仅动作扩散策略，该方法使用一个独立学习的世界模型来实现STL鲁棒性的可微评估，然后将其梯度注入扩散过程。这在不重新训练的情况下将行为引导至满足约束，提高了约束遵循度，同时保持了任务性能。在Robomimic的Can Transport任务上，我们的方法保持了100%的任务成功率，同时将基线方法超过80%的约束违反率降低到4%。我们还讨论了提高鲁棒性和处理更复杂约束的扩展方向。

## Abstract
Diffusion policies enable multimodal robot behavior but offer limited ability to choose among behavior modes at inference time, even though such control is desirable in human-robot settings. Prior solutions to this lack of control have utilized Signal Temporal Logic (STL) to express human intentions and provide corresponding guidance for diffusion policy inference. However, these approaches can only guide diffusion policies that jointly generate future actions and states, increasing both complexity and runtime. We propose a novel guidance method for action-only diffusion policies that uses a separate learned world model to enable differentiable evaluation of STL robustness, with its gradient then injected into the diffusion process. This steers behavior toward constraint satisfaction without retraining, improving constraint adherence while preserving task performance. On the Can Transport task from Robomimic, our method maintains 100% task success while reducing constraint violations from over 80% for baseline methods to 4%. We also discuss extensions toward improved robustness and more complex constraints.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：现代机器人扩散策略（diffusion policies）能生成多样的行为模式，但在推理时缺乏可控性，无法根据用户偏好在不同行为模式间做出选择。现有利用信号时序逻辑（STL）进行推理引导的方法要求策略同时生成动作和状态，这增加了模型复杂度和运行成本，且不适用于目前主流的仅动作扩散策略。
- **整体含义**：本文旨在提出一种新的推理时引导方法，仅依赖动作序列预测和单独训练的世界模型，即可实现对仅动作扩散策略的STL约束引导，使机器人行为在保持任务成功的同时显著降低约束违规，无需重训练，并为人机交互中的灵活偏好表达提供支持。

### 2. 论文提出的方法论
- **核心思想**：将动作扩散策略与独立可微世界模型结合，通过世界模型将动作序列转换为预测的状态轨迹，进而计算STL鲁棒性梯度，将该梯度注入扩散去噪过程，实现无重训练的约束满足行为引导。
- **关键技术细节与流程**：
  - 设扩散策略 π(at:t+H | st) 基于当前状态 st 生成长度为 H 的动作序列。给定一个关于状态轨迹的STL规范 ϕ，目标是引导采样出的动作序列，使诱导轨迹满足 ϕ。
  - 传统扩散去噪更新（步 k）为：ak−1 = μθ(ak, st, k) + σkϵ。
  - 本文的推理时引导修改为：ak−1 = μθ(ak, st, k) + λ∇ak J(ϕ, ak, st) + σkϵ，其中 λ 为引导强度。
  - 目标函数 J 定义为J(ϕ, at:t+H, st) = ρϕ(ŝ t+1:t+H)，ρϕ 为STL鲁棒性度量，ŝ t+1:t+H 是由单独训练的世界模型 ŝ t+1 = fθ(st, at) 迭代展开得到的状态预测序列。
  - 由于STL鲁棒性对状态轨迹可微、状态轨迹通过世界模型对动作可微，因此可计算 ∇ak J 并注入扩散过程，引导行为满足约束。最后对去噪完成的动作序列 a0 额外执行少量梯度上升以进一步提高约束满足度。

### 3. 实验设计
- **数据集/场景**：使用Robomimic基准中的Can Transport任务，机械臂需抓取一个罐子并将其放入盒子。训练数据包含混合人类示教，以捕获多样化行为。
- **Benchmark与对比方法**：
  - 基础扩散策略（Base Policy）：无引导的标准策略。
  - 采样-排序基线（Sample & Rank）：基于相同STL鲁棒性目标，对多条采样轨迹排序选择最优者。
- 评估指标：约束违规率（Viol. %）、任务成功率（Succ. %）、平均罐体倾斜角度（Avg. Tilt）。

### 4. 资源与算力
- 文中未明确提及所使用GPU型号、数量或具体训练时长等算力信息。

### 5. 实验数量与充分性
- 定量对比实验：在50次滚动（rollout）上评估三种方法的约束满足和任务成功，结果汇总于表 I。
- 定性分析：展示策略多模态行为示例（图2a），以及有无引导时的轨迹对比（图2b）。
- 实验具有一定的针对性，但仅在一个任务（Can Transport）和单一STL约束（罐体保持竖直）下评估，缺乏多任务、多约束类型的充分验证，消融研究（如引导强度λ的影响、世界模型质量的影响）也未展示。

### 6. 论文的主要结论与发现
- 提出的方法能在保持100%任务成功率的同时，将约束违规率从基线方法的80%以上降至4%，显著优于基础策略和采样-排序方法。
- 基于梯度的引导比基于采样的选择更有效，可能恢复数据分布中较少出现的合规行为模式。
- 此方法无需更改策略训练，可为仅动作扩散策略提供灵活的推理时STL引导。

### 7. 优点
- 首次实现仅动作扩散策略的STL推理时引导，填补了现有方法必须联合生成状态才能应用STL的空白。
- 解耦世界模型，使STL评估的预测范围可独立于策略的动作预测范围，增加了灵活性。
- 方法可微且无需重训练，能保持任务成功并大幅降低约束违规，且不依赖特定策略架构。
- 实验包含合理的基线和直观的定性展示，结果清晰有力。

### 8. 不足与局限
- **实验覆盖有限**：仅在单个任务和单一STL约束上评估，缺乏在其他复杂操作任务和多类型约束下的泛化证据。
- **无消融实验**：未分析世界模型预测精度、引导强度λ、梯度上升步数等关键超参数对性能的影响。
- **长时域约束未验证**：文中虽提到世界模型可用于长时域预测与自动机抽象，但当前实现仍限于较短的动作预测范围，未展示对长时域STL公式的支持。
- **世界模型依赖**：需要额外训练一个准确的世界模型，其品质会直接影响引导效果，但文中未讨论世界模型误差或分布偏移时的鲁棒性。
- **计算开销**：推理时需要世界模型展开与梯度回传，可能增加单步推理时间，但未与基线方法比较推理速度。

（完）

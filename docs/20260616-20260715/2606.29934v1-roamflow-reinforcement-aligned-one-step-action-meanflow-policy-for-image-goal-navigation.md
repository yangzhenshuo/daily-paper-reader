---
title: "RoamFlow: Reinforcement-Aligned One-Step Action MeanFlow Policy for Image-Goal Navigation"
title_zh: RoamFlow：用于图像目标导航的强化对齐一步动作平均流策略
authors: "Zixuan Zhang, Yuqi Chen, Junjie Gao, Siyuan Song, Yongzhou Pan, Beichen Wang, Mir Feroskhan"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29934v1"
tags: ["query:fewstepgen"]
score: 10.0
evidence: 基于MeanFlow的单步动作生成策略用于图像目标导航
tldr: 视觉目标导航要求智能体仅根据目标图像到达指定位置，现有深度强化学习方法直接映射观测到动作，难以建模长序列依赖，常导致次优轨迹。RoamFlow提出一种生成式框架，利用MeanFlow预测平均速度场进行轨迹合成，实现高效的一步动作生成，显著降低推理延迟。通过两阶段训练（先专家模仿后强化学习对齐），在Habitat仿真和真实机器人上均取得高效推理与强劲导航表现，为实时图像目标导航开辟了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29934v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 976, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29934v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1814, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29934v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29934v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 871, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29934v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1804, \"height\": 540, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29934v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 1017, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29934v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1842, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29934v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 869, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29934v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 840, \"height\": 251, \"label\": \"Table\"}]"
motivation: 现有图像目标导航的RL方法直接映射观测到动作，难以捕捉长程依赖，导致推理低效和轨迹欠优。
method: 提出基于MeanFlow的生成式导航策略，预测平均速度场实现一步动作生成，并采用专家模仿与强化学习两阶段训练。
result: 在Habitat模拟和真实机器人平台上，RoamFlow在保持高导航成功率的同时显著降低推理延迟，验证了方法的有效性。
conclusion: RoamFlow展示了生成式模型在实时导航任务中的潜力，为后续研究提供高效框架，并为实时图像目标导航提供生成式新思路。
---

## 摘要
图像目标导航是具身机器人领域的关键挑战，代理必须仅根据目标图像到达指定目标。虽然现有的强化学习方法将感知观测直接映射为动作，但它们难以建模长程依赖关系，常常导致次优轨迹。为了解决这一局限，我们提出RoamFlow，这是一种生成式导航框架，利用MeanFlow预测平均速度场以进行轨迹合成，实现高效少步生成并降低推理延迟。我们进一步采用两阶段训练策略，结合专家模仿实现稳定初始化，并通过强化学习进行任务特定策略优化。在Habitat仿真环境和真实机器人平台上的大量实验表明，RoamFlow在实时约束下实现了高效推理，同时保持了强大的导航性能。

## Abstract
Image-goal navigation is a key challenge in embodied robotics, where an agent must reach a target specified solely by a goal image. While existing reinforcement learning approaches map perceptual observations directly to actions, they struggle to model long-horizon dependencies, often leading to suboptimal trajectories. To address this limitation, we propose RoamFlow, a generative navigation framework that leverages MeanFlow to predict the average velocity field for trajectory synthesis, enabling efficient few-step generation and reducing inference latency. We further adopt a two-stage training strategy that combines expert imitation for stable initialization with reinforcement learning for task-specific policy refinement. Extensive experiments in both Habitat simulation and real-world robotic platforms demonstrate that RoamFlow achieves efficient inference while maintaining strong navigation performance under real-time constraints.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：图像目标导航要求机器人仅凭一张目标图像在未知环境中移动到达目标位置，是具身智能的核心任务。
- **现有方法局限**：传统强化学习方法（RL）将观测直接映射为单步动作，缺乏对长远规划的建模，导致轨迹次优；基于扩散模型或流匹配的生成式策略虽然能生成多步动作序列、捕捉多模态分布，但推理迭代次数多、延迟高，难以在资源受限的平台上实时部署。
- **整体含义**：本文提出 **RoamFlow**，利用平均流（MeanFlow）将轨迹生成转化为单步过程，并结合两阶段训练（模仿学习 + 强化学习），在保证轨迹质量的同时大幅降低推理时间，为实时图像目标导航提供一种实用高效的生成式策略。

### 2. 方法论
- **核心思想**：
  - 抛弃扩散模型的迭代去噪或流匹配的逐步数值积分，改用 **MeanFlow** 预测**平均速度场**，直接在一个前向传播中将高斯噪声映射为完整的动作轨迹序列。
  - 采用两阶段训练：先通过模仿学习（IL）从专家演示中学习基本导航先验，再通过在线强化学习（RL）微调以对齐任务目标（如高效到达、避障）。

- **架构细节（图2）**：
  - **观测编码**：当前RGB‑D图像与目标RGB图像分别经过EfficientNet‑B0编码，再通过Transformer融合，形成条件隐变量 \(C_t\)。
  - **RoamFlow 策略**：以 \(C_t\) 和噪声为输入，通过U‑Net预测平均速度场 \(u_\theta\)，经过少量（甚至1步）传输步骤生成轨迹动作序列 \(A_t = \{a_t, ..., a_{t+T-1}\}\)。传输过程为 \(X_{k+1}=X_k + U_\theta(X_k, Z, t_k)\Delta t_k\)。
  - **轨迹评估器**：一个MLP为每个候选轨迹打分，选择最高分轨迹执行，分数监督信号来自几何安全性与平滑性构建的目标 \(\tilde{s}(\tau)\)。

- **训练流程（算法1）**：
  - **阶段1：离线模仿学习**
    - 训练MeanFlow生成器，优化目标：
      \[
      \mathcal{L}_{MF} = \mathbb{E}_{x_t, r, t} \left[\|u_\theta(x_t, r, t) - u(x_t, r, t)\|^2\right]
      \]
      其中 \(u(x_t, r, t)\) 是真实平均速度（位移/时间）。
    - 同时训练轨迹评估器，最小化回归损失 \(\mathcal{L}_{eval} = \mathbb{E}[ (s_\phi(o,\tau) - \tilde{s}(\tau))^2 ]\)。
  - **阶段2：在线强化学习微调**
    - 固定观测编码器和轨迹评估器，仅更新生成器参数（包含引入可学习噪声的方差网络）。
    - 将轨迹生成建模为随机传输链，得到可计算的对数概率 \(\log \pi_{\bar{\theta}}(\tau_t|o_t)\)。
    - 使用PPO优化，奖励包括成功奖励、进度奖励、碰撞惩罚和步数惩罚，目标为：
      \[
      \mathcal{L}_{PPO} = \mathbb{E}_t[\min(r_t A_t, \text{clip}(r_t, 1-\epsilon, 1+\epsilon) A_t)]
      \]
      其中重要性比率 \(r_t = \pi_{\bar{\theta}}(\tau_t|o_t) / \pi_{\bar{\theta}_{old}}(\tau_t|o_t)\)。

### 3. 实验设计
- **数据集与环境**：
  - 模拟环境：Gibson 和 Matterport3D (MP3D) 室内场景。训练数据来自场景中的 Hybrid A* 专家轨迹，并加入真实数据集 GoStanford 和 Scand 以减小 sim‑to‑real 差距。
  - 真实平台：Unitree Go2 四足机器人，搭载 Jetson Orin NX 和 RealSense D435i。
- **评估基准**：
  - 指标：成功率 (SR)、成功加权的路径长度 (SPL)、碰撞率 (CR)、推理时间 (ms)。
  - 对比方法：
    - 生成式导航基线：NoMaD（扩散）、FlowNav（流匹配）、NaviDiffusor（代价引导扩散）、NaviBridger（扩散桥）、NavDP（扩散策略）。
    - 蒸馏加速变体：NoMaD + 蒸馏、FlowNav + 蒸馏。
- **实验设置**：
  - 所有模型在同一数据划分上训练，在相同的未见过的 Gibson 测试集和跨域未微调的 MP3D 上测试。统一使用拓扑图作为全局规划器，各方法作为局部规划器。

### 4. 资源与算力
- **GPU**：使用单张 NVIDIA RTX 6000 Ada GPU。
- **训练时间**：
  - 模仿学习阶段：约 20 小时。
  - 强化学习阶段：约 30 小时。
- **推理效率**：最终模型的推理延迟仅约 19.6 ms（模拟）和 37.2 ms（真实机器人），远低于多数扩散基线。

### 5. 实验数量与充分性
- **对比实验**（表 I）：在 Gibson 和 MP3D 两个数据集上与 7 种方法（含蒸馏版本）对比，涵盖成功率、SPL、碰撞率和推理时间，指标全面。
- **消融实验**（表 II）：系统剥离 IL 预训练、RL 微调、轨迹评估器（对比随机选择）的影响，验证每个组件的贡献。
- **真实机器人实验**（表 III）：针对 3 种场景共 20 次运行，报告成功率和碰撞次数，验证 sim‑to‑real 迁移能力。
- **训练动态**（图 3）：展示不同预训练策略的奖励曲线，说明两阶段训练的优势。
- **定性展示**（图 4、图 5）：模拟与真实环境下的轨迹选择可视化。
- **充分性与公平性**：实验覆盖多个 SOTA 基线、两种不同特性的数据集、消融和真实验证，对比公平。但真实实验样本量（20 runs）和场景多样性有限，此外跨域测试时未对 MP3D 微调，仅体现泛化能力。

### 6. 主要结论与发现
- RoamFlow 在 Gibson 测试集上取得最高成功率（68.7%）和 SPL（61.9%），碰撞率最低（10.9%），推理时间仅 19.6 ms。
- 在跨域 MP3D 上同样保持领先（SR 56.1%, SPL 47.1%），验证了泛化性。
- 两阶段训练有效：RL 微调在 IL 初始化基础上显著降低碰撞率、提高成功率；轨迹评估器进一步提升安全性。
- 真实实验中，RoamFlow 实现 100% 成功率、0.1 次/回碰撞，推理延迟满足实时要求。

### 7. 优点
- **高效的单步生成**：利用 MeanFlow 避免迭代采样，实现极低推理延迟，适合边缘设备。
- **训练策略创新**：将模仿学习与 RL 有机结合，先建立合理先验，再通过环境交互微调，克服纯模仿学习的退化问题和 RL 的冷启动困难。
- **轻量安全模块**：轨迹评估器仅用于推理时的重排序，不改变生成分布，以极小成本提高安全性。
- **实验全面且扎实**：涵盖模拟对比、消融、真实平台，并与蒸馏加速的基线进行对比，突出了本方法的效率与质量平衡。

### 8. 不足与局限
- **真实实验规模有限**：仅 20 次运行、3 种场景，统计显著性较弱，难以全面反映极端或动态环境下的鲁棒性。
- **环境依赖性**：训练和评估主要基于静态室内环境，未涉及动态障碍物或复杂语义交互。
- **生成式机制限制**：MeanFlow 虽推理快，但假设轨迹分布可由平均速度场一步描述，在高度多模态或极长规划中可能限制多样性。
- **计算资源透明度**：只提及单卡 GPU 和总时长，未给出具体显存、同步频率等细节，复现时可能需要额外调参。
- **组件耦合性**：轨迹评估器的训练依赖手工定义的安全目标 \(\tilde{s}\)，迁移到新环境可能需要重新设计。

（完）

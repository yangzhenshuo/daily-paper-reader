Title: Flow Duality and Source Geometry for Categorical Generation

URL Source: https://arxiv.org/pdf/2609.10863v1

Published Time: Fri, 11 Sep 2026 00:18:39 GMT

Number of Pages: 16

Markdown Content:
# Flow Duality and Source Geometry for Categorical Generation 

## Etrit Haxholli 

## etrit.ks@gmail.com 

## Preprint 

Abstract 

Continuous and discrete flow matching are usually treated as separate constructions. This paper identifies a duality between them: projecting continuous convex-interpolant paths with one-hot targets through a position-wise argmax yields discrete convex-interpolant paths. The result requires source laws with appropriate coordinate symmetry and boundary regularity, and it makes the continuous source distribution an explicit design choice for categorical generation. We derive the induced discrete interpolation behavior for Gaussian, bounded-uniform, and centered negative-exponential sources, showing that different source geometries lead to qualitatively different transition timing and vocabulary-size dependence. Small visual diagnostics and a short language-modeling pilot suggest that these source-design effects can also appear in learned transports and early generative quality. 

# 1 Introduction 

Generative modeling over discrete objects such as language, code, and symbolic sequences is commonly formulated through discrete diffusion or discrete flow matching. These models define a time-indexed process on a finite state space and learn probability velocities or denoising distributions that move mass from a simple source distribution to the data distribution. In parallel, continuous diffusion and flow matching construct deterministic or stochastic trajectories in Euclidean space, where the geometry of the path can be studied directly. Recent work on diffusion duality (Sahoo et al., 2025) has shown that certain discrete diffusion processes can be understood as projections of underlying continuous processes. This raises a natural question for flow matching: does an analogous duality hold for continuous flows with convex interpolants? In particular, if a continuous flow is defined over one-hot token representations (Lee et al., 2026) and then projected back to tokens through the argmax map, what discrete flow is induced? This paper focuses exclusively on that question. We study continuous flows whose conditional trajectories are convex interpolants between a continuous source and a one-hot encoded target. Applying the argmax map position-wise along such trajectories yields a process on the finite vocabulary. Our main result shows that, under an argmax-compatible lifted coupling and product coordinate-permutation-invariant continuous sources with no mass on tie hyperplanes and no atoms at the relevant pairwise-gap thresholds, this projected process is a discrete flow with convex interpolants and uniform source. When the per-position source laws are identical, the induced interpolation coefficient is shared across positions; otherwise the same statement holds with position-dependent coefficients. This duality makes the continuous source distribution a mathematically meaningful design choice. In one-hot categorical generation, the target coordinate must compete under argmax against the source coordinates. For a Gaussian source, this competition becomes more difficult as the vocabulary size grows, because the target coordinate must overtake the maximum of many noise coordinates. The induced discrete coefficient therefore becomes delayed at large vocabulary size. Alternative sources can change this coefficient and, potentially, the geometry of the transport. We enrich the duality result by deriving the induced discrete coefficient for three source families: Gaussian, bounded uniform, and centered negative-exponential sources. The Gaussian coefficient is vocabulary-dependent and delayed. For fixed vocabulary size, the bounded uniform coefficient is controlled by the support width. The centered negative-exponential coefficient has a simple form independent of vocabulary size. We also 1arXiv:2609.10863v1 [cs.LG] 9 Sep 2026 include toy trajectory visualizations comparing Gaussian and shifted uniform sources in a two-point target problem. These plots are not intended as large-scale empirical evidence, but they illustrate qualitatively that source family, placement, and scale can affect both the induced discrete coefficient and learned continuous path geometry. The paper is organized as follows. Section 2 introduces background and notation. Section 3 presents the duality theorem, its supporting lemmas, and the source-coefficient extensions. Section 4 concludes. All duality proofs and coefficient derivations are given in the appendix. 

# 2 Background and Notation 

A summary of Discrete Flow Matching is provided below. 

## 2.1 Discrete Flow Matching 

To expand the design space of discrete diffusion models, Campbell et al. (2024); Gat et al. (2024) introduce discrete flow matching. We follow the approach and notation of Gat et al. (2024). In discrete sequence modeling, a sequence (state) x consists of L elements (x1, x 2, . . . , x L). Each position i contains an element xi

from a vocabulary V with |V| = V , which we identify with [V ] = {1, . . . , V }. Throughout the non-degenerate case is V ≥ 2; when V = 1 the transition coefficient is irrelevant. Thus, the set of possible sequences is 

D = [ V ]L. Two sequences are neighbors if they differ in only one position. We denote by pi(xi) the marginal of p at position i, i.e., 

pi(xi) = X

> x−i

p(x), where x−i = ( x1, . . . , x i−1, x i+1 , . . . , x L).

The following delta function notation will be particularly useful, 

δy (x) = 

> L

Y

> i=1

δyi (xi), where δyi (xi) = 

(

1 if xi = yi

0 if xi̸ = yi . (1) 

2.1.1 Probability Flows and Velocities 

In discrete flow matching (Gat et al., 2024), the goal is to define or learn a flow pt(x) : [0 , 1] × [V ]L → [0 , 1] 

constrained by P  

> x∈[V]L

pt(x) = 1 that transforms source (reference) distributions X0 ∼ p to target (data) distributions X1 ∼ q. The flow is completely defined by the choice of a probability velocity ut(x) : [0 , 1) × D → RL×V , with ut(x) = ( u1 

> t

(x), . . . , u Lt (x)) and uit(x) ∈ RV . For a candidate token a ∈ [V ], write 

uit(a, x ) := uit(x)[ a]. These rates satisfy uit(a, x ) ≥ 0 for a̸ = xi and P  

> a∈[V]

uit(a, x ) = 0 . The infinitesimal, first-order update of the probability over states when going from time t to t + ϵ is defined independently for each position as pit+ϵ|t(a | x) = δxi (a) + ϵu it(a, x ) + o(ϵ). Therefore, we can see that as in the framework of Markov chains, the probability over the states in the next step depends solely on the current state, and that ut plays a similar role to a transition-rate matrix Qt, completely determining the flow. As such, if we approximate the probability velocity ut(x) using a neural network ut(x, t ; θ) : [ V ]L × [0 , 1] → RL×V , we can sample from p and generate data from q, using the previous update rule. Before modeling the probability velocity ut(x) however, one must first design an appropriate flow pt(x) that has a suitable, practically learnable corresponding ut(x).

2.1.2 Conditional Probability Flows 

Since at time t = 0 and t = 1 we must have p0 = p and p1 = q respectively, we are already restricted regarding the endpoints of the flow. A trivial way to satisfy such constraints is to define 

pt(x) = X

> x0,x 1∈D

pt(x|x0, x 1)π(x0, x 1), (2) 2where p0(x|x0, x 1) = δx0 (x), p1(x|x0, x 1) = δx1 (x) and π(X0, X 1) is an arbitrary joint distribution of X0, X1

satisfying the marginals constraints p(x) = P 

> y∈D

π(x, y ), q(y) = P 

> x∈D

π(x, y ). Since the probability veloci-ties update the probability independently for each position, it is natural to define pt(x|x0, x 1) independently for each dimension as in Gat et al. (2024): 

pt(x|x0, x 1) = 

> L

Y

> i=1

pit(xi|x0, x 1), (3) where pit(xi|x0, x 1) = (1 − kt)δxi 

> 0

(xi) + ktδxi 

> 1

(xi), with k0 = 0 , k 1 = 1 and increasing kt. (4) It is clear that this definition of pt(x|x0, x 1) satisfies the conditions p0(x|x0, x 1) = δx0 (x) and p1(x|x0, x 1) = 

δx1 (x). Assume additionally that k is absolutely continuous on [0 , 1] . Then Gat et al. (2024) show that component i of the conditional probability velocity uit(a, x | x0, x 1) corresponding to the flow defined in Equations (3) and (4), for almost every time with kt < 1, is 

uit(a, x | x0, x 1) = ˙kt

1 − kt

h

δxi 

> 1

(a) − δxi (a)

i

. (5) Furthermore, they show that the probability velocity corresponding to the unconditional flow pt(x) can be written as 

uit(a, x ) = X

> x0,x 1∈D

uit(a, x | x0, x 1) p0,1|t(x0, x 1 | x), (6) which in the case of Equations (4) and (5) implies, uit(a, x ) = ˙kt

> 1−kt

h

pi

> 1|t

(a | x, t ) − δxi (a)

i

. One then approxi-mates uit(a, x ) by modeling the time-conditioned posterior predictor pi

> 1|t

(a | x, t ; θ) with a neural network using the cross-entropy loss, 

LDFM = −Et∼U (0 ,1) Ex0,x 1∼π(x0,x 1)Ext∼pt|0,1(·| x0,x 1)

> L

X

> i=1

log pi

> 1|t

(xi 

> 1

| xt, t ; θ). (7) It should be mentioned that in Gat et al. (2024), the definition of pit(xi|x0, x 1) is given in a more general form, but here we focus on this specific case for simplicity. 

2.1.3 Source and Target Distributions 

As mentioned, points X0 and X1 are sampled from a joint distribution π(x, y ), i.e. (X0, X 1) ∼ π(X0, X 1),satisfying the marginals constraints p(x) = P 

> y∈D

π(x, y ), q(y) = P 

> x∈D

π(x, y ). As a special case, the training pairs X0 and X1 can be sampled independently, (X0, X 1) ∼ p(X0)q(X1). Common instantiations of source distribution p are: (i) adding a special token value often referred to as a mask token, denoted here by m. This uses an enlarged vocabulary Vmask = [ V ] ∪ { m}, outside the strict [V ]L setup unless m is already included in the vocabulary. The source is X0 = ( m, . . . , m ) almost surely, with X1 ∼ q under the chosen coupling. (ii) using uniform distribution over D, which is equivalent to drawing each xi independently to be some value in [V ] with equal probability, denoted pu(xi).

2.1.4 Continuous Flows 

Let ˜π be a probability measure on RLV × RLV , with first and second marginals ˜p0 and ˜p1. We refer to ˜p0 as the continuous source and ˜p1 as the continuous target. This measure notation is important in the present setting because the target may be supported on one-hot vectors and therefore need not admit a Lebesgue density. For a nondecreasing interpolant coefficient ˜kt : [0 , 1] → [0 , 1] satisfying ˜k0 = 0 and ˜k1 = 1 , define 

Tt(z0, z 1) = (1 − ˜kt)z0 + ˜ktz1. (8) 3The continuous flow is the pushforward 

˜pt = ( Tt)# ˜π. (9) Equivalently, conditional on a coupled endpoint pair (z0, z 1), the trajectory is deterministic and has the form 

zt = (1 − ˜kt)z0 + ˜ktz1. (10) These are continuous flows with convex interpolants: the conditional trajectories are straight line segments between coupled endpoints, possibly with a time reparameterization through ˜kt. When a classical continuous velocity field is discussed, we additionally assume ˜k is absolutely continuous, hence differentiable almost everywhere. 

2.1.5 Diffusion duality 

Sahoo et al. (2025) showed that a uniform-state discrete diffusion process can be realized as the argmax projection of an underlying Gaussian diffusion. In this view, the continuous latent process evolves in Euclidean space while the observed categorical state is obtained by applying argmax to each vocabulary block. This makes it possible to transfer continuous-diffusion ideas to discrete generation, including lower-variance training curricula and consistency-style distillation. The result motivates the question studied here: whether an analogous projection principle holds for more general continuous flows with convex interpolants rather than diffusion noising processes. 

2.1.6 Two-time denoiser and flow maps 

Lee et al. (2026) study continuous flows over one-hot token embeddings and distinguish the instantaneous flow from its flow map. Let Φs,t denote the map that transports a continuous state from time s to time t,and define the average velocity 

vs,t (x) := Φs,t (x) − xt − s . (11) Their two-time denoiser is the reparameterization 

δs,t (x) := x + (1 − s)vs,t (x), (12) which can be inverted to recover the flow map: 

Φs,t (x) = 1 − t

1 − s x + t − s

1 − s δs,t (x). (13) For one-hot targets, δs,t lies on the probability simplex, so it can be represented by a token-wise softmax and trained with cross-entropy and KL objectives (e.g. the semi-group condition). At s = t it reduces to the usual endpoint denoiser, while at t = 1 it gives the final clean prediction map. 

2.1.7 Notation summary 

For reference, the main probability symbols used below are summarized here: Symbol Meaning 

˜pt continuous marginal law on RLV 

pt = (arg max) # ˜pt discrete argmax pushforward law on [V ]L

pt|0,1 discrete conditional law of Xt given (X0, X 1)

pit|0,1 per-position conditional law at position iq target/data law on [V ]L

π(x1 | x0) discrete coupling kernel used in the lifted construction 

¯π(x0, x 1) induced discrete joint p0(x0)π(x1 | x0)Φs,t continuous flow map from time s to time tvs,t average velocity associated with Φs,t 

δs,t two-time denoiser of Lee et al. (2026) 

Dt endpoint denoiser, recovered as the diagonal case of δs,t 

43 Duality 

We now isolate the flow-duality statement. In what follows, we demonstrate that applying the argmax 

operator position-wise to each point along a continuous flow with convex interpolants results in a corresponding discrete convex-interpolant flow, provided the continuous coupling is constructed through the discrete argmax source as described below. Moreover, if the continuous source is coordinate-permutation invariant and has no mass on tie hyperplanes, then the corresponding discrete source distribution is uniform. This establishes a connection between discrete convex-interpolant conditional paths with uniform sources and continuous convex-interpolant paths with coordinate-permutation invariant source distributions. Our result generalizes the diffusion-duality viewpoint of Sahoo et al. (2025) to the setting of flows. All proofs can be found in Appendix A. 

## 3.1 The Duality Between Discrete and Continuous Flows with Convex Inter-polants 

In what follows, ˜pt denotes the continuous marginal law at time t; when this law admits a density, we use the same symbol for that density by abuse of notation. We define a continuous flow as in Section 2.1.4, where each block zi ∈ RV corresponds to the i-th position: 

zi := z[( i − 1) V + 1 : iV ] ∈ RV , i = 1 , . . . , L. (14) We define the per-position mapping 

arg max : RV → [V ] (15) as follows. First, for u ∈ RV , let 

S(u) := {j ∈ [V ] : uj = max   

> k∈[V]

uk}. (16) We then define 

arg max( u) := min S(u). (17) Thus ties are broken by taking the smallest index. We extend this to sequences by 

arg max : RLV → [V ]L, (arg max( z)) i := arg max( zi), i = 1 , . . . , L. (18) Finally, we denote by B(·) the Borel σ-algebra, and by P([ V ]L) the power set of [V ]L.Now, we first verify that arg max is measurable and thus induces a pushforward of any probability measure on RLV to a probability measure on [V ]L.

Lemma 3.1. The mapping arg max : ( RLV , B(RLV )) → ([ V ]L, P([ V ]L)) is measurable. 

As a consequence, for any probability measure μ on (RLV , B(RLV )) , (arg max) #μ is a well-defined probability measure on ([ V ]L, P([ V ]L)) . In particular, for the continuous marginal ˜pt we define 

pt := (arg max) # ˜pt. (19) For time t = 0 we have the following result: 

Lemma 3.2 (Uniform discrete source) . For continuous source distributions ˜p0 = QLi=1 ˜pi

> 0

, assume each ˜pi 

> 0

is coordinate-permutation invariant and has no ties almost surely, i.e. 

˜pi

> 0

 {u ∈ RV : ∃k̸ = ℓ, u k = uℓ} = 0 . (20) 

Then the pushforward p0 = (arg max) # ˜p0 is the uniform distribution on [V ]L. In particular, for each position 

i and each token j ∈ [V ],

pi

> 0

(j) = 1

V , (21) 

where x0 := arg max( z0) and z0 ∼ ˜p0.

5For time t = 1 , we have the special case z1 = onehot( x1). So the arg max map only undoes the onehot 

encoding arg max( z1) = arg max(onehot( x1)) = x1.The factorization results below use the following argmax-compatible lifting of a discrete coupling. First sample Z0 ∼ ˜p0 and set X0 = arg max( Z0). Then sample X1 from a discrete coupling kernel π(· | X0)

using randomness conditionally independent of Z0 given X0. When the endpoint is intended to be the data distribution q, we assume this kernel has marginal X1 ∼ q, i.e. 

q(x1) = X 

> x0∈[V]L

p0(x0) π(x1 | x0) = 1

V L

X 

> x0∈[V]L

π(x1 | x0). (22) Equivalently, the induced discrete joint is ¯π(x0, x 1) = p0(x0)π(x1 | x0). Finally set Z1 = onehot( X1). Then define the continuous convex interpolant 

Zt = (1 − ˜kt)Z0 + ˜ktZ1, Xt = arg max( Zt). (23) This construction allows an arbitrary discrete coupling kernel between X0 and X1 (with the desired endpoint marginal imposed when needed) subject to the argmax-compatible conditional-independence rule above, but it is not an arbitrary continuous coupling ˜π(z0, z 1): conditional on X0, the endpoint X1 depends on Z0 only through X0 = arg max( Z0). The kernel π(x1 | x0) may correlate target positions, but once x1 is fixed, the remaining randomness in Xt comes from the conditionally independent source blocks Zi 

> 0

| Xi 

> 0

= xi

> 0

. This conditional-independence structure is what preserves the per-position factorization used below. Given that we know how the source and target distributions are transformed, the remaining object is the per-position conditional pit|0,1(xit | xi

> 0

, x i

> 1

).The following result is crucial. It is precisely where the conditional independence Z0 ⊥ X1 | X0 is used: without it, conditioning on X1 could introduce dependence among the within-cell source blocks. 

Lemma 3.3 (Factorization across positions) . Under the lifted coupling construction above, for each t ∈ [0 , 1] 

and any pair (x0, x 1) in the support of the lifted discrete coupling, the conditional distribution of xt given 

(X0, X 1) = ( x0, x 1) factorizes across positions: 

pt|0,1(xt | x0, x 1) = 

> L

Y

> i=1

pit|0,1(xit | xi

> 0

, x i

> 1

), (24) 

for some per-position conditionals pit|0,1(· | xi

> 0

, x i

> 1

) on [V ].

The only remaining missing piece is the form of pit|0,1(xit | xi

> 0

, x i

> 1

). This is answered by the following theorem which also summarizes previous findings: 

Theorem 3.4 (Discrete conditional path induced by a continuous flow) . Assume V ≥ 2 and let ˜k : [0 , 1] →

[0 , 1] be nondecreasing with ˜k0 = 0 and ˜k1 = 1 . For each t, write ct := ˜kt/(1 − ˜kt), with the convention 

ct = + ∞ when ˜kt = 1 . Assume that the continuous source factorizes across positions, ˜p0 = QLi=1 ˜pi

> 0

,that each per-position source law ˜pi 

> 0

is coordinate-permutation invariant, and that if Zi ∼ ˜pi

> 0

, then Zi has no ties almost surely and, for every a̸ = b, the pairwise gap Zia − Zib has no atom at ct for each t with 

˜kt < 1. Assume also that the coupling is the argmax-compatible lifted coupling described above, in particular satisfying L(X1 | Z0) = π(· | arg max Z0): Z0 ∼ ˜p0, X0 = arg max( Z0), sample X1 from π(· | X0) using randomness conditionally independent of Z0 given X0 (equivalently, L(X1 | Z0) = π(· | arg max Z0)), and set Z1 = onehot( X1). For each t ∈ [0 , 1] , define Zt = (1 − ˜kt)Z0 + ˜ktZ1 and ˜pt := L(Zt). Then the law 

pt = (arg max) # ˜pt is a probability distribution on [V ]L with the following properties: (i) The source p0 is uniform on [V ]L.(ii) If the lifted discrete kernel has endpoint marginal 

q(x1) = X 

> x0∈[V]L

p0(x0) π(x1 | x0) = 1

V L

X 

> x0∈[V]L

π(x1 | x0), (25) 

then the terminal marginal satisfies p1 = q.

6(iii) Interpolation probabilities factor per position, 

pt|0,1(xt | x0, x 1) = 

> L

Y

> i=1

pit|0,1(xit | xi

> 0

, x i

> 1

). (26) 

(iv) The unconditional discrete path is the mixture 

pt(x) = X 

> x0,x 1∈[V]L

pt|0,1(x | x0, x 1) ¯ π(x0, x 1), ¯π(x0, x 1) = p0(x0)π(x1 | x0). (27) 

(v) For any pair (x0, x 1) in the support of the lifted discrete coupling and each position i, there exists 

kit ∈ [0 , 1] such that 

pit|0,1(xit | xi

> 0

, x i

> 1

) = (1 − kit) δxi 

> 0

(xit) + kit δxi 

> 1

(xit). (28) 

For xi

> 0

̸ = xi

> 1

, this coefficient is 

kit = P



Zixi

> 0

− Zixi

> 1

≤ ct arg max( Zi) = xi

> 0



, (29) 

where Zi ∼ ˜pi

> 0

. For distinct tokens, t 7 → kit is nondecreasing, and ki 

> 0

= 0 , ki 

> 1

= 1 under the conventions 

c0 = 0 and c1 = + ∞. When xi 

> 0

= xi

> 1

, the two delta masses coincide, so the value of the coefficient is immaterial; for notational consistency we assign it the same value as in the distinct-token case. Thus 

kit depends only on ˜kt and the i-th continuous source law ˜pi

> 0

, not on (x0, x 1). If the per-position source laws are identical, then kit = kt for all i.In other words, the per-position conditionals of the discrete conditional path generated by the continuous one coincide with the standard convex-interpolant form used in discrete flow matching. In the common-source case, the usual single scalar coefficient kt is shared across positions. 

## 3.2 Source-dependent coefficients induced by the duality 

Theorem 3.4 shows that the projected continuous flow has the same conditional form as a discrete convex-interpolant flow. In the common per-position source setting, the remaining question is how the shared scalar coefficient kt depends on the continuous source. This coefficient is the probability, conditional on the source token and target token being distinct, that the target coordinate has overtaken the source coordinate by time 

t. Thus the source distribution does not merely choose where the continuous trajectories begin; it determines the effective time dependence of the induced discrete flow. For the following source families, assume V ≥ 2 and write 

ct := ˜kt

1 − ˜kt

, (30) with the convention that ct = + ∞ when ˜kt = 1 . The derivations are provided in Appendix B. 

Gaussian source. For i.i.d. standard Gaussian coordinates, the induced coefficient is 

kGauss  

> t

= V

Z ∞−∞ 

φ(m)Φ( m)V −2 Φ( m) − Φ( m − ct) dm, (31) where φ and Φ denote the standard normal density and distribution function. Equivalently, if 

ρGauss  

> t

= EG∼N (0 ,1) 

Φ( G + ct)V −1 , (32) then 

kGauss  

> t

= V ρ Gauss  

> t

− 1

V − 1 . (33) This form makes the vocabulary dependence explicit: as V grows, the target coordinate has to compete with the maximum of many Gaussian coordinates, delaying the induced transition. 7Bounded uniform source. For i.i.d. uniform coordinates on an interval of width ϵ > 0, the induced coefficient is 

kUnif  

> t

= V r t − rVt

V − 1 , rt = clip 

 ct

ϵ , 0, 1



. (34) Here clip( a, 0, 1) = min {max {a, 0}, 1}. For fixed vocabulary size V , the coefficient depends on the width of the interval, not on its location. Shifting the interval affects the geometry of the continuous trajectories, while the induced discrete coefficient is controlled by the support width and V . Because of the clipping, this coefficient can reach 1 before the endpoint: under the linear schedule ˜kt = t, we have ct = t/ (1 − t), so rt = 1 

once t ≥ ϵ/ (1 + ϵ). After that time the induced conditional path has already reached the target, and any velocity formula containing (1 − kUnif  

> t

)−1 should be interpreted only on the interval where kUnif  

> t

< 1.

Centered negative-exponential source. Let β > 0 and Z = β−1 − E where E ∼ Exp( β); the shift makes E[Z] = 0 . For i.i.d. coordinates of this form, the induced coefficient is 

kNegExp  

> t

= 1 − exp( −βc t). (35) Unlike the Gaussian coefficient, this expression has no explicit vocabulary-size dependence. Figures 1 and 2 visualize these coefficients under the linear interpolant ˜kt = t. Figure 1 compares the three source families at GPT-2 vocabulary scale, while Figure 2 isolates the vocabulary dependence of the Gaussian source. 

Figure 1: Induced coefficient kt for Gaussian, bounded uniform with ϵ = 1 , and centered negative-exponential with β = 1 under the linear interpolant ˜kt = t and vocabulary size V = 50 ,257 .8Figure 2: Vocabulary-size dependence of the Gaussian induced coefficient under the linear interpolant ˜kt = t.Larger vocabularies shift the effective transition later in time. 

## 3.3 Tiny 10k-step generative check 

As a small sanity check, we ran short 10k-step OpenWebText pilot trainings comparing the Gaussian source to a centered negative-exponential source. In both cases we trained a two time denoising model Lee et al. (2026) with identical architecture and hyperparameters. Following the evaluation decomposition advocated by Pynadath et al. (2026), we combine generative perplexity and sample entropy into the plug-in estimate 

bDKL = log(GenPPL) − bH, (36) where log(GenPPL) estimates the cross-entropy against the reference language model and bH is the reported unigram entropy, used as a surrogate for joint sequence entropy per token as in Pynadath et al. (2026). This estimate should be read only as a single operating-point diagnostic, not as a replacement for a full generative frontier. Table 1: Tiny 10k-step OpenWebText pilot. Lower GenPPL, cross-entropy, and estimated KL are better; higher sample entropy is better. One step generation. Single training run. Source GenPPL ↓ log(GenPPL) ↓ Entropy ↑ bDKL ↓

Gaussian 428.88 6.061 4.148 1.913 Centered neg.-exp. 258.51 5.555 4.022 1.533 

U (−4, −3) 244.68 5.497 4.05 1.447 In this early-training run, the centered negative-exponential source reduces estimated KL by 0.380 nats relative to the Gaussian source, a 19.9% relative reduction, while sample entropy decreases only modestly. The result is preliminary, but it is consistent with the source-geometry prediction that changing the continuous source can alter generative behavior before long training. 9(a) Shifted bounded uniform source. (b) Gaussian source. 

Figure 3: Toy tracked trajectories for a learned flow in a two-point target problem. The theorem’s conditional convex interpolants are straight line segments; these plots instead visualize trajectories of a learned marginal toy transport tracked over a fixed integration grid. 

## 3.4 Toy trajectory geometry 

The coefficient kt describes the induced discrete path associated with the conditional convex interpolants. Figure 3 instead shows trajectories produced by a learned marginal toy flow in a two-point target experiment with vocabulary size two and sequence length one. The same small neural vector-field model, loss, and integration grid are used for both sources. The plot is purely diagnostic: it visualizes learned transport trajectories, not the theorem’s conditional straight-line interpolants. The comparison also confounds source family with source placement and scale relative to the one-hot targets, so it should be read only as a qualitative illustration of possible geometric effects. 

## 3.5 Reverse lifts and mask-source variants 

Conversely, a discrete convex-interpolant flow with uniform source admits endpoint-compatible continuous lifts once a coordinate-permutation invariant continuous source law and a lifting rule are fixed. Such a lift is not unique: many continuous laws have the same argmax pushforward, and the lifted pushforward recovers the original discrete path when the induced coefficient matches the original discrete coefficient. One convenient construction is to sample z0 from the chosen continuous source, set x0 = arg max( z0), sample x1

according to the discrete kernel π(x1 | x0), and set z1 = onehot( x1). Combining this conditional law of z1

given z0 with the chosen source law defines a continuous coupling ˜π, and the convex interpolant then defines a continuous flow ˜pt.Let plift  

> t

:= (arg max) # ˜pt be the discrete pushforward of this constructed continuous lift. Equality between this pushforward plift  

> t

and a prescribed discrete flow pt is not automatic; it holds when the interpolation coefficient induced by ˜kt and the chosen continuous source matches the coefficient of the prescribed discrete flow. A related lifting can be constructed for multi-mask sources, but it requires a separate argument because the source is no longer coordinate-permutation invariant over all enlarged coordinates. One possible construction extends the vocabulary from [V ] to [2 V ], as well as RLV to R2LV , and uses independent coordinates with separated uniform supports, for example U(a, b ) on data-token coordinates [1 : V ] and U(c, d ) on mask 10 coordinates [V + 1 : 2 V ] with c > b . This construction should be viewed as a mask-specific lift in the enlarged space, rather than a direct application of Lemma 3.2 in RLV .

# 4 Conclusion 

This paper isolates the flow-duality component of a broader study of categorical generation. The central result is that continuous flows with convex interpolants, one-hot encoded targets, an argmax-compatible lifted coupling, and product coordinate-permutation-invariant continuous sources induce discrete conditional paths with convex-interpolant conditionals after position-wise argmax projection. The supporting lemmas show that the argmax map is measurable, that symmetric continuous sources push forward to uniform discrete sources, and that the projected conditional law factors across positions. The source-coefficient calculations show why this duality is not only formal. The continuous source determines the scalar coefficient of the induced discrete flow. For Gaussian sources, this coefficient is delayed as vocabulary size grows. For bounded uniform sources, for fixed vocabulary size, the coefficient is controlled by support width. For centered negative-exponential sources, the coefficient has a simple vocabulary-independent form. These formulas suggest that source design should be treated as a structural part of continuous categorical flow modeling, not as a neutral implementation choice. By removing the optimal-transport and consistency components, this version highlights the main mathemat-ical mechanism: under the lifted-coupling assumptions, argmax projection turns continuous convex-interpolant flows into discrete convex-interpolant conditional paths, and the source law determines the effective discrete path. This provides a clean basis for future work on source design in flow-map language models and other continuous approaches to categorical generation. 11 A Duality Proofs 

## A.1 Proof of Lemma 3.1 

Proof. Fix j ∈ [V ]. Define 

A(j) := {u ∈ RV : arg max( u) = j}. (37) By the definition of arg max with ties broken by the smallest index, 

A(j) = 

 \

> k<j

{u ∈ RV : uj > u k}

 ∩

 \

> k>j

{u ∈ RV : uj ≥ uk}

 . (38) This is a finite intersection of open or closed half-spaces in RV , and hence A(j) is Borel. For a sequence x = ( x1, . . . , x L) ∈ [V ]L, its preimage under arg max is 

> −1

arg max( {x}) = 

> L

Y

> i=1

A(xi) ⊂ (RV )L ∼= RLV . (39) A finite product of Borel sets is Borel in the product space, so arg max −1({x}) is Borel for every x ∈ [V ]L.Therefore arg max is measurable as a map from (RLV , B(RLV )) to the discrete space ([ V ]L, P([ V ]L)) .

## A.2 Proof of Lemma 3.2 

Proof. Fix a position i. Since ties have probability zero, arg max( Zi

> 0

) is almost surely unique. For any two indices j, k ∈ [V ], choose a coordinate permutation σ sending j to k. We use the convention (σu )r = uσ−1(r),so arg max( σu ) = σ(arg max u) whenever the argmax is unique. By coordinate-permutation invariance, Zi

> 0

and σZ i 

> 0

have the same law. Therefore 

P(arg max( Zi

> 0

) = j) = P(arg max( σZ i

> 0

) = k) = P(arg max( Zi

> 0

) = k). (40) All tokens have equal probability, and the probabilities sum to one, so each is 1/V .Because ˜p0 = QLi=1 ˜pi 

> 0

is a product measure and the argmax is applied independently at each position, the coordinates of X0 = arg max( Z0) are independent uniform random variables on [V ]. Hence p0 is the uniform distribution on [V ]L.

## A.3 Proof of Lemma 3.3 

Proof. By construction, Z0 = ( Z10 , . . . , Z L 

> 0

) has independent components and Xi 

> 0

= arg max( Zi

> 0

) depends only on Zi

> 0

. Hence, conditional on X0 = x0, the random variables (Zi

> 0

)Li=1 remain independent, with Zi

> 0

distributed as ˜pi 

> 0

conditioned on arg max( Zi

> 0

) = xi

> 0

.Under the lifted coupling construction, X1 is sampled from the discrete kernel π(· | X0); hence Z0 ⊥ X1 |

X0. Therefore 

L(Z0 | X0 = x0, X 1 = x1) = L(Z0 | X0 = x0),

so conditioning additionally on X1 = x1 does not introduce dependence among the variables (Zi

> 0

)Li=1 . Moreover, 

Zi 

> 1

= onehot( xi

> 1

) is deterministic given X1 = x1.Thus, conditional on (X0, X 1) = ( x0, x 1), the pairs (Zi

> 0

, Z i

> 1

) are independent across positions. Since 

Zit = (1 − ˜kt)Zi 

> 0

+ ˜ktZi 

> 1

(41) is a measurable function of (Zi

> 0

, Z i

> 1

), the variables (Zit )Li=1 are conditionally independent given (X0, X 1).Finally, Xit = arg max( Zit ) depends only on Zit , so (Xit )Li=1 are conditionally independent given (X0, X 1),which gives the claimed factorization. We now describe the form of each per-position conditional. 12 A.4 Proof of Theorem 3.4 

Proof. By the lifted coupling construction, we sample Z0 ∼ ˜p0, set X0 = arg max( Z0), sample X1 ∼ π(· | X0),set Z1 = onehot( X1), and form the convex interpolant 

Zt = (1 − ˜kt)Z0 + ˜ktZ1.

For a realization (z0, z 1) of (Z0, Z 1), write zt = (1 − ˜kt)z0 + ˜ktz1. The cases ˜kt = 0 and ˜kt = 1 are the source and target endpoints and are immediate. In particular, if the lifted kernel has X1 ∼ q, then 

Z1 = onehot( X1) implies p1 = q after argmax projection. We therefore consider 0 < ˜kt < 1 and write 

ct = ˜kt/(1 − ˜kt). For convex interpolants in continuous flows we have 

zt = (1 − ˜kt)z0 + ˜ktz1, hence zit = (1 − ˜kt)zi 

> 0

+ ˜ktzi

> 1

.

Write j0 := xi 

> 0

and j1 := xi 

> 1

for the maximizing indices. If j0 = j1, then the boosted coordinate is already the source argmax, so the argmax remains j0 for all t and the conditional law is simply δxi 

> 0

= δxi 

> 1

. Suppose therefore that j0̸ = j1.Because zi 

> 1

is one-hot, the interpolated vector has the form 

zit [j] = (1 − ˜kt)zi

> 0

[j] (j̸ = j1), zit [j1] = (1 − ˜kt)zi

> 0

[j1] + ˜kt.

Among coordinates j̸ = j1, the maximum is attained at j0, because arg max( zi

> 0

) = j0. Thus arg max( zit ) can only be either j0 or j1. More explicitly, on the event arg max( zi

> 0

) = j0 we have max j̸ =j1 zi

> 0

[j] = zi

> 0

[j0], so, up to the equality event where tie-breaking may matter, 

arg max( zit ) = j1 ⇐⇒ (1 − ˜kt)zi

> 0

[j1] + ˜kt > (1 − ˜kt)zi

> 0

[j0] ⇐⇒ zi

> 0

[j0] − zi

> 0

[j1] < ct.

The tie event at time t is exactly {zi

> 0

[j0] − zi

> 0

[j1] = ct}, which has probability zero by the pairwise-gap non-atomicity assumption. Thus using ≤ instead of < gives the same transition probability. We use the convention that ct = + ∞ when ˜kt = 1 .If xi 

> 1

= xi

> 0

, then δxi 

> 0

= δxi 

> 1

and the conditional law is simply the same point mass regardless of the value assigned to kit. Hence the mixture representation is degenerate in the equal-token case, and the coefficient only needs to be defined for xi

> 1

̸ = xi

> 0

.Assume therefore that xi

> 1

̸ = xi

> 0

. At a time t there is a probability kit(xi

> 0

, x i

> 1

) that arg max( zit ) = xi 

> 1

and a probability 1 − kit(xi

> 0

, x i

> 1

) that arg max( zit ) = xi

> 0

, namely 

pit|0,1(xit | xi

> 0

, x i

> 1

) = (1 − kit(xi

> 0

, x i

> 1

)) δxi 

> 0

(xit) + kit(xi

> 0

, x i

> 1

) δxi 

> 1

(xit).

We now show that this coefficient does not depend on the particular distinct pair (xi

> 0

, x i

> 1

), but only on ˜kt and 

˜pi

> 0

. For xi

> 1

̸ = xi

> 0

, define 

kit(xi

> 0

, x i

> 1

) = ˜pi

> 0

n 

zi 

> 0

arg max( zi

> 0

) = xi

> 0

, z i

> 0

[xi

> 0

] − zi

> 0

[xi

> 1

] ≤ ct

o 

˜pi

> 0

zi 

> 0

arg max( zi

> 0

) = xi

> 0

 . (42) For the equal-token case, the two delta masses coincide, so the value of the coefficient is immaterial; we use the same value as in the distinct-token case for notational consistency. To see why kit(xi

> 0

, x i

> 1

) does not depend on the particular distinct values of xi 

> 0

and xi

> 1

, use a permutation argument. Let (a, b ) and (a′, b ′) be any two ordered pairs with a̸ = b and a′̸ = b′. Choose a coordinate permutation σ with σ(a) = a′ and σ(b) = b′, again acting by (σu )r = uσ−1(r). Since Zi d

= σZ i under the coordinate-permutation invariance assumption, the denominator events 

{arg max( Zi) = a} and {arg max( σZ i) = a′}

have the same probability. The same permutation maps the numerator event 

{arg max( Zi) = a, Z ia − Zib ≤ ct}

13 onto 

{arg max( σZ i) = a′, (σZ i)a′ − (σZ i)b′ ≤ ct}.

Thus the numerator and denominator in (42) are invariant under the map, and the ratio is identical for all distinct token pairs. Therefore kit(xi

> 0

, x i

> 1

) is constant across distinct (xi

> 0

, x i

> 1

); denote this constant by 

kit ∈ [0 , 1] . We conclude 

pit|0,1(xit | xi

> 0

, x i

> 1

) = (1 − kit) δxi 

> 0

(xit) + kit δxi 

> 1

(xit).

with kit depending only on ˜kt and the source ˜pi

> 0

, not on (xi

> 0

, x i

> 1

). If all per-position source laws are identical, then all kit are equal; in that case we write the common coefficient as kt.

Gaussian special case. In the case of the source being i.i.d. standard normal entries, write Φ and φ for the standard normal CDF and pdf, and take ˜kt = t. Then 

˜pi

> 0

 {zi 

> 0

| arg max( zi

> 0

) = xi

> 0

} = 1

V .

Moreover, for xi

> 1

̸ = xi 

> 0

and ct := t 

> 1−t

, the numerator in (42) can be written as 

˜pi

> 0

n 

zi 

> 0

arg max( zi

> 0

) = xi

> 0

, z i

> 0

[xi

> 0

] − zi

> 0

[xi

> 1

] ≤ ct

o 

=

Z ∞−∞ 

φ(m) Φ( m)V −2  Φ( m) − Φ( m − ct) dm, 

and therefore the (token-independent) mixing weight admits the explicit integral representation 

kt = V

Z ∞−∞ 

φ(m) Φ( m)V −2  Φ( m) − Φ( m − ct) dm, ct = t

1 − t .

# B Source coefficient derivations 

Theorem 3.4 gives the conditional discrete transition 

pit|0,1(xit | xi

> 0

, x i

> 1

) = (1 − kt)δxi 

> 0

(xit) + ktδxi 

> 1

(xit), (43) for xi

> 0

̸ = xi

> 1

. This appendix derives kt for the source families used in Section 3.2. The plots use the linear interpolant ˜kt = t, while the derivations below are written in terms of the general ratio ct = ˜kt/(1 − ˜kt).Throughout, fix a position i, write V ≥ 2 for the vocabulary size, and let 

ct = ˜kt

1 − ˜kt

. (44) Condition on the source argmax being coordinate a and the target being coordinate b̸ = a. The target wins at time t precisely when 

Za − Zb ≤ ct (45) under the additional conditioning that Za is the maximum coordinate of the source. 

## B.1 Gaussian source 

Let Z1, . . . , Z V be i.i.d. standard normal. Consider the joint event that coordinate a has value m and is the argmax. Its unnormalized density is 

φ(m)Φ( m)V −1, (46) which integrates to 1/V . Given Za = m and the event that a is the maximum, the remaining coordinates are i.i.d. normal variables truncated above by m. The event that the target coordinate b is within ct of the 14 maximum is Zb ≥ m − ct. Therefore the unnormalized mass of source points for which the source argmax is 

a and the target coordinate wins by time t is 

Z ∞−∞ 

φ(m)Φ( m)V −2 Φ( m) − Φ( m − ct) dm. (47) The conditioning event arg max( Z) = a has probability 1/V . Dividing by 1/V gives 

kGauss  

> t

= V

Z ∞−∞ 

φ(m)Φ( m)V −2 Φ( m) − Φ( m − ct) dm. (48) An equivalent form follows by fixing the target coordinate but not conditioning on the source argmax. Let 

ρGauss  

> t

= P(fixed target coordinate wins before conditioning on the source argmax ) = EG∼N (0 ,1) 

Φ( G+ct)V −1.

(49) Since the source is uniform after argmax projection, 

ρGauss  

> t

= 1

V + V − 1

V kGauss  

> t

, (50) and hence 

kGauss  

> t

= V ρ Gauss  

> t

− 1

V − 1 . (51) 

## B.2 Bounded uniform source 

Let ϵ > 0 and let Z1, . . . , Z V be i.i.d. uniform on an interval [a, a + ϵ]. The location a cancels in all coordinate comparisons, so only the width ϵ matters. Define 

rt = min 

n

max 

n ct

ϵ , 0

o

, 1

o

. (52) First compute the unconditional target-win probability. Given the target coordinate value Zb = z, all other coordinates must be at most z + ct. For 0 ≤ ct ≤ ϵ,

ρUnif  

> t

= 1

ϵ

Z a+ϵ−ct

> a

 z + ct − aϵ

V −1

dz + 1

ϵ

Z a+ϵa+ϵ−ct

1 dz. (53) Changing variables gives 

ρUnif  

> t

= rt + 1 − rVt

V . (54) Using ρt = 1 /V + ( V − 1) kt/V yields 

kUnif  

> t

= V r t − rVt

V − 1 . (55) 

## B.3 Centered negative-exponential source 

Let E1, . . . , E V be i.i.d. exponential variables with rate β, and set 

Zj = 1

β − Ej . (56) The shift makes the source centered but does not affect argmax comparisons. The target wins unconditionally if every non-target coordinate is at most Zb + ct. Let Y = β−1 − Zb = Eb. Then Y ∼ Exp( β). If Y < c t, the threshold exceeds the upper endpoint of the support, so the target wins with probability one. If Y ≥ ct, then 

P(Zj ≤ Zb + ct) = exp( −β(Y − ct)) . (57) 15 Therefore 

ρNegExp  

> t

= P(Y < c t) + E exp  −(V − 1) β(Y − ct)1{Y ≥ct}

 (58) 

= 1 − e−βc t +

Z ∞

> ct

βe −βy e−(V −1) β(y−ct)dy (59) 

= 1 −



1 − 1

V



e−βc t . (60) Using again ρt = 1 /V + ( V − 1) kt/V , we obtain 

kNegExp  

> t

= 1 − e−βc t . (61) This coefficient is independent of the vocabulary size V .

# References 

Andrew Campbell, Jason Yim, Regina Barzilay, Tom Rainforth, and Tommi Jaakkola. Generative flows on discrete state-spaces: Enabling multimodal flows with applications to protein co-design. In Proceedings of the 41st International Conference on Machine Learning , Proceedings of Machine Learning Research, volume 235, pages 5453–5512, 2024. Itai Gat, Tal Remez, Neta Shaul, Felix Kreuk, Ricky T. Q. Chen, Gabriel Synnaeve, Yossi Adi, and Yaron Lipman. Discrete flow matching. In Advances in Neural Information Processing Systems , 2024. Chanhyuk Lee, Jaehoon Yoo, Manan Agarwal, Sheel Shah, Jerry Huang, Aditi Raghunathan, Seunghoon Hong, Nicholas M. Boffi, and Jinwoo Kim. Flow Map Language Models: One-step Language Modeling via Continuous Denoising. arXiv preprint arXiv:2602.16813 , 2026. Subham Sekhar Sahoo, Justin Deschenaux, Aaron Gokaslan, Guanghan Wang, Justin T. Chiu, and Volodymyr Kuleshov. The diffusion duality. In Proceedings of the 42nd International Conference on Machine Learning ,Proceedings of Machine Learning Research, volume 267, pages 52584–52619, 2025. Patrick Pynadath, Jiaxin Shi, and Ruqi Zhang. Generative frontiers: Why evaluation matters for diffusion language models. arXiv preprint arXiv:2604.02718 , 2026. 16